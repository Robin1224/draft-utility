---
phase: 04-draft-ui-disconnect-resilience
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/atoms/ChampionCard.svelte
  - src/lib/components/atoms/TimerDisplay.svelte
  - src/lib/components/atoms/DraftSlot.svelte
  - src/lib/components/atoms/StatusBanner.svelte
  - src/lib/components/molecules/TurnIndicator.svelte
  - src/lib/components/molecules/TeamDraftColumn.svelte
  - src/lib/components/molecules/ChampionGrid.svelte
autonomous: true
requirements:
  - DRAFT-07

must_haves:
  truths:
    - "ChampionCard renders name, role badge, and handles selected/picked/banned/disabled states"
    - "TimerDisplay shows large seconds countdown and shrinking progress bar that turns red at ≤10s"
    - "DraftSlot shows empty (dashed border) or filled (champion name + action label) state"
    - "StatusBanner auto-dismisses after 3 seconds with green-600 positive or red-600 warning styling"
    - "TurnIndicator shows turn label, captain name, and embeds TimerDisplay"
    - "TeamDraftColumn shows team label, ban slots, and pick slots using DraftSlot atoms"
    - "ChampionGrid renders 28 champions in responsive 4-col (mobile) / 7-col (desktop) grid with Submit button for active captain"
  artifacts:
    - path: "src/lib/components/atoms/ChampionCard.svelte"
      provides: "Champion card atom with all state variants"
      contains: "opacity-40"
    - path: "src/lib/components/atoms/TimerDisplay.svelte"
      provides: "Countdown + progress bar with urgency at ≤10s"
      contains: "red-600"
    - path: "src/lib/components/atoms/DraftSlot.svelte"
      provides: "Pick/ban slot with empty/filled states"
      contains: "dashed"
    - path: "src/lib/components/atoms/StatusBanner.svelte"
      provides: "Auto-dismissing transient banner"
      contains: "setTimeout"
    - path: "src/lib/components/molecules/TurnIndicator.svelte"
      provides: "Turn label + captain name + TimerDisplay"
      contains: "TimerDisplay"
    - path: "src/lib/components/molecules/TeamDraftColumn.svelte"
      provides: "Team ban and pick slot columns"
      contains: "DraftSlot"
    - path: "src/lib/components/molecules/ChampionGrid.svelte"
      provides: "28-champion responsive grid with selection and submit"
      contains: "ChampionCard"
  key_links:
    - from: "src/lib/components/molecules/TurnIndicator.svelte"
      to: "src/lib/components/atoms/TimerDisplay.svelte"
      via: "component import and props passing (turnEndsAt, timerMs)"
      pattern: "TimerDisplay"
    - from: "src/lib/components/molecules/ChampionGrid.svelte"
      to: "src/lib/components/atoms/ChampionCard.svelte"
      via: "rendered for each champion"
      pattern: "ChampionCard"
    - from: "src/lib/components/molecules/TeamDraftColumn.svelte"
      to: "src/lib/components/atoms/DraftSlot.svelte"
      via: "ban slots and pick slots"
      pattern: "DraftSlot"
---

<objective>
Build all UI atoms and molecules for the draft board: ChampionCard, TimerDisplay, DraftSlot, StatusBanner (atoms) and TurnIndicator, TeamDraftColumn, ChampionGrid (molecules). Independent of server changes; can run in parallel with Plan 02.

Purpose: Fulfills DRAFT-07 visual requirements. Provides all building blocks DraftBoard (Plan 04) assembles.
Output: 7 Svelte 5 component files following UI-SPEC and existing app conventions.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-draft-ui-disconnect-resilience/04-UI-SPEC.md
@.planning/phases/04-draft-ui-disconnect-resilience/04-CONTEXT.md

<interfaces>
<!-- Snapshot shape during drafting (from RESEARCH.md): -->
draftState = {
  script: { team: 'A'|'B', action: 'pick'|'ban' }[],
  turnIndex: number,
  turnEndsAt: string,  // ISO timestamp
  timerMs: number,
  paused?: boolean,
  graceEndsAt?: string
}
actions = [{ turn_index, team, action, champion_id, created_at }]
teams = { A: LobbyMember[], B: LobbyMember[] }

<!-- classes.json shape (28 champions): -->
[{ "id": string, "name": string, "role": string }]

<!-- Color tokens (UI-SPEC): -->
bg-bg-primary    // slate-50 — card backgrounds
bg-bg-secondary  // slate-200 — column backgrounds, slot borders
text-text-primary
text-text-secondary
amber-500        // active/selected accent
red-600          // urgency ≤10s
green-600        // positive status

<!-- Typography (UI-SPEC): -->
text-sm  font-normal      // 14px body — champion names
text-xs  font-semibold    // 12px label — section headers, turn action label
text-xl  font-semibold    // 20px heading — TurnIndicator, TeamDraftColumn label
text-5xl font-semibold    // ~48px display — timer countdown number

<!-- Existing atom reference: src/lib/components/atoms/Phases.svelte -->
<!-- Existing molecule reference: src/lib/components/molecules/TeamColumn.svelte -->
<!-- Svelte 5 runes: $state, $derived, $props, $effect -->
<!-- All components use Tailwind v4 utility classes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atom components — ChampionCard, TimerDisplay, DraftSlot, StatusBanner</name>
  <files>
    src/lib/components/atoms/ChampionCard.svelte,
    src/lib/components/atoms/TimerDisplay.svelte,
    src/lib/components/atoms/DraftSlot.svelte,
    src/lib/components/atoms/StatusBanner.svelte
  </files>
  <read_first>
    - src/lib/components/atoms/Phases.svelte (Svelte 5 atom structure and Tailwind patterns)
    - src/routes/layout.css (color token names and font declarations)
    - .planning/phases/04-draft-ui-disconnect-resilience/04-UI-SPEC.md (state matrix, copywriting, color, typography — full spec)
  </read_first>
  <action>
    Create each file using the Svelte 5 runes syntax ($props, $state, $derived, $effect) matching the project's established patterns.

    **src/lib/components/atoms/ChampionCard.svelte:**
    Props: champion ({id, name, role}), state ('default'|'selected'|'picked'|'banned'|'disabled'), onclick (function).
    - Minimum height 44px (WCAG touch target per UI-SPEC).
    - default: bg-bg-primary border-bg-secondary cursor-pointer hover:border-amber-500.
    - selected: bg-bg-primary border-amber-500 ring-2 ring-amber-500 cursor-pointer, aria-pressed=true.
    - picked/banned/disabled: opacity-40 cursor-not-allowed pointer-events-none border-bg-secondary bg-bg-primary.
    - Name: text-sm font-normal text-text-primary.
    - Role badge: text-xs font-semibold text-text-secondary bg-bg-secondary rounded-full px-2 py-0.5.
    - Use a <button> element; disabled attribute set when not interactive; pass onclick only when interactive.

    **src/lib/components/atoms/TimerDisplay.svelte:**
    Props: turnEndsAt (string ISO), timerMs (number, default 30000).
    - secondsLeft derived via $effect with setInterval at 250ms tick. MUST return () => clearInterval(id) to prevent leak (Svelte 5 $effect cleanup).
    - urgency = $derived(secondsLeft <= 10).
    - barWidth = $derived(timerMs > 0 ? Math.max(0, Math.min(100, (secondsLeft * 1000 / timerMs) * 100)) : 100).
    - Countdown number: text-5xl font-semibold leading-none, text-text-primary normally, text-red-600 when urgency.
    - Progress bar: h-1 rounded-full track bg-bg-secondary; inner fill width={barWidth}%, bg-amber-500 normally, bg-red-600 when urgency, transition-[width] duration-200.

    **src/lib/components/atoms/DraftSlot.svelte:**
    Props: action ('pick'|'ban'), championName (string|null, null = empty), team ('A'|'B').
    - Empty: border-2 border-dashed border-bg-secondary rounded-md p-3 min-h-[44px]; shows action label in text-xs font-semibold text-text-secondary uppercase.
    - Filled: border border-bg-secondary bg-bg-secondary rounded-md p-3; champion name text-sm text-text-primary; action label text-xs font-semibold text-text-secondary uppercase below it.

    **src/lib/components/atoms/StatusBanner.svelte:**
    Props: message (string), variant ('positive'|'warning', default 'positive'), onDismiss (function, optional).
    - $effect: setTimeout 3000ms; calls onDismiss?.() on expiry; returns () => clearTimeout(id) for cleanup.
    - Position: fixed top-4 left-1/2 -translate-x-1/2 z-50 rounded-md px-4 py-2 shadow-md text-sm font-semibold.
    - positive: bg-white border border-green-600 text-green-600.
    - warning: bg-white border border-red-600 text-red-600.
  </action>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && ls src/lib/components/atoms/ChampionCard.svelte src/lib/components/atoms/TimerDisplay.svelte src/lib/components/atoms/DraftSlot.svelte src/lib/components/atoms/StatusBanner.svelte 2>&1 && grep -l "opacity-40" src/lib/components/atoms/ChampionCard.svelte && grep -l "red-600" src/lib/components/atoms/TimerDisplay.svelte && grep -l "dashed" src/lib/components/atoms/DraftSlot.svelte && grep -l "setTimeout" src/lib/components/atoms/StatusBanner.svelte && echo "ALL ATOMS OK"</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under src/lib/components/atoms/
    - ChampionCard.svelte contains `opacity-40` (disabled state)
    - ChampionCard.svelte contains `aria-pressed`
    - TimerDisplay.svelte contains `clearInterval` (cleanup) and `red-600` (urgency)
    - TimerDisplay.svelte contains `setInterval` and `return () => clearInterval`
    - DraftSlot.svelte contains `border-dashed` (empty state)
    - StatusBanner.svelte contains `setTimeout` and `clearTimeout`
    - StatusBanner.svelte contains `green-600` and `red-600` (variant styles)
  </acceptance_criteria>
  <done>4 atoms created; all state variants implemented; setInterval cleanup correct; verify command exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Molecule components — TurnIndicator, TeamDraftColumn, ChampionGrid</name>
  <files>
    src/lib/components/molecules/TurnIndicator.svelte,
    src/lib/components/molecules/TeamDraftColumn.svelte,
    src/lib/components/molecules/ChampionGrid.svelte
  </files>
  <read_first>
    - src/lib/components/molecules/TeamColumn.svelte (molecule reference: Svelte 5 patterns, prop conventions)
    - src/lib/catalog/classes.json (first few entries to confirm id/name/role shape)
    - .planning/phases/04-draft-ui-disconnect-resilience/04-UI-SPEC.md (layout, interaction contract, copywriting)
    - src/lib/components/atoms/ChampionCard.svelte (props: champion, state, onclick — just created in Task 1)
    - src/lib/components/atoms/TimerDisplay.svelte (props: turnEndsAt, timerMs — just created in Task 1)
    - src/lib/components/atoms/DraftSlot.svelte (props: action, championName, team — just created in Task 1)
  </read_first>
  <action>
    **src/lib/components/molecules/TurnIndicator.svelte:**
    Props: draftState ({script, turnIndex, turnEndsAt, timerMs}), teams ({A: LobbyMember[], B: LobbyMember[]}).
    - Derives currentTurn = draftState.script[draftState.turnIndex].
    - Derives activeTeam = currentTurn.team ('A'|'B').
    - Derives captainName by finding the member in teams[activeTeam] where is_captain=true; falls back to "Captain".
    - Heading: text-xl font-semibold text-text-primary, content = "Team {activeTeam} — {currentTurn.action === 'pick' ? 'Pick' : 'Ban'}" (per UI-SPEC copywriting: title-case team name + em dash + action).
    - Sub-label: text-sm text-text-secondary below heading, content = "{captainName}'s turn".
    - Below: TimerDisplay component passing turnEndsAt={draftState.turnEndsAt} timerMs={draftState.timerMs}.
    - Layout: flex flex-col items-center gap-2 py-4 text-center.

    **src/lib/components/molecules/TeamDraftColumn.svelte:**
    Props: team ('A'|'B'), label (string e.g. "Team A"), script ({team,action}[]), actions (draft_action[]), members (LobbyMember[]).
    - Derives banSlots: script entries for this team where action='ban', in script order. Each slot: check if actions has an entry at that turn_index; if yes, filled; if no, empty.
    - Derives pickSlots: same for action='pick'.
    - The number of slots is determined by the script (count of ban turns for this team, count of pick turns for this team).
    - To build slots: filter script with index to get [{ scriptIndex, action }] entries for this team. Then for each, look up actions[scriptIndex]?.champion_id.
    - Champion names: the snapshot does not include champion names in actions — only champion_id. The ChampionGrid is responsible for resolving names. Pass champion_id as championName for now (Plan 04 will pass the full catalog to resolve ids to names).
    - Layout: flex flex-col gap-4.
    - Team label: text-xl font-semibold text-text-primary mb-2.
    - Bans section: text-xs font-semibold text-text-secondary uppercase mb-1 label "BANS", then DraftSlot for each ban slot.
    - Picks section: text-xs font-semibold text-text-secondary uppercase mb-1 label "PICKS", then DraftSlot for each pick slot.

    **src/lib/components/molecules/ChampionGrid.svelte:**
    Props: champions ([{id,name,role}]), usedIds (string[] — champion_ids already picked or banned), isActiveCaptain (boolean), currentAction ('pick'|'ban'), onSubmit (function({championId, action})).
    - selectedId: $state(null) — only one champion can be selected at a time.
    - On champion card click (only when isActiveCaptain=true and champion not in usedIds): set selectedId to that champion's id; if already selected, deselect (set to null).
    - cardState for a champion: 'disabled' if in usedIds; 'selected' if id === selectedId; 'default' otherwise. Non-captains always see 'disabled' for used, 'default' for available (no selection possible).
    - Grid: grid grid-cols-4 md:grid-cols-7 gap-4.
    - Submit button: visible only when isActiveCaptain=true AND selectedId != null. Text: "Submit {currentAction === 'pick' ? 'Pick' : 'Ban'}". Styling: bg-amber-500 hover:bg-amber-400 text-white font-semibold rounded-md px-6 py-2 mt-4 w-full. On click: call onSubmit({ championId: selectedId, action: currentAction }); reset selectedId to null.
    - When timer at 0 (turnEndsAt in past by > 1s), disable submit button (awaiting server advance): this is implicit — the server will reject the pick anyway, but add `disabled` class + cursor-not-allowed when selectedId is null.

    Note: DraftBoard in Plan 04 will pass `champions` (from imported classes.json), `usedIds` (derived from snapshot.actions), `isActiveCaptain` (derived from snapshot), `currentAction` (from snapshot.draftState.script[turnIndex].action), and `onSubmit` callback that calls the pickBan RPC.
  </action>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && ls src/lib/components/molecules/TurnIndicator.svelte src/lib/components/molecules/TeamDraftColumn.svelte src/lib/components/molecules/ChampionGrid.svelte 2>&1 && grep -l "TimerDisplay" src/lib/components/molecules/TurnIndicator.svelte && grep -l "DraftSlot" src/lib/components/molecules/TeamDraftColumn.svelte && grep -l "ChampionCard" src/lib/components/molecules/ChampionGrid.svelte && echo "ALL MOLECULES OK"</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under src/lib/components/molecules/
    - TurnIndicator.svelte imports and uses TimerDisplay component
    - TurnIndicator.svelte contains `em dash` or `—` in the heading template (turn indicator format per UI-SPEC)
    - TeamDraftColumn.svelte imports and uses DraftSlot component
    - TeamDraftColumn.svelte contains `BANS` and `PICKS` section labels
    - ChampionGrid.svelte imports and uses ChampionCard component
    - ChampionGrid.svelte contains `grid-cols-4` and `md:grid-cols-7` (responsive grid per UI-SPEC)
    - ChampionGrid.svelte contains `Submit` in the button text template
    - ChampionGrid.svelte contains `selectedId` state variable
    - ChampionGrid.svelte contains `isActiveCaptain` prop
  </acceptance_criteria>
  <done>3 molecules created; TurnIndicator composes TimerDisplay; TeamDraftColumn renders DraftSlot slots; ChampionGrid has responsive grid + submit button + selection state</done>
</task>

</tasks>

<verification>
All 7 component files exist in correct directories.
ChampionCard: opacity-40, aria-pressed present.
TimerDisplay: setInterval cleanup (return () => clearInterval), red-600 urgency present.
DraftSlot: border-dashed empty state present.
StatusBanner: setTimeout, clearTimeout, green-600, red-600 present.
TurnIndicator: imports TimerDisplay, has em dash heading format.
TeamDraftColumn: imports DraftSlot, has BANS/PICKS labels.
ChampionGrid: imports ChampionCard, grid-cols-4 md:grid-cols-7, Submit button, selectedId state.
</verification>

<success_criteria>
- All 7 Svelte component files created following UI-SPEC and Svelte 5 runes conventions
- No imports from non-existent modules
- All visual state variants implemented as specified
- Timer cleanup prevents interval leak
- ChampionGrid enforces single-select and confirm pattern (no double-click shortcut per D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-draft-ui-disconnect-resilience/04-03-SUMMARY.md`
</output>
