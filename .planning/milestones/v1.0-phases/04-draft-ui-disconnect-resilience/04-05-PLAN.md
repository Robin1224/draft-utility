---
phase: 04-draft-ui-disconnect-resilience
plan: 05
type: execute
wave: 3
depends_on:
  - 04-04
files_modified: []
autonomous: false
requirements:
  - DRAFT-07
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04

must_haves:
  truths:
    - "All participants see draft board with live countdown and pick/ban history during a real draft session"
    - "Active captain can pick or ban a champion and all participants see the update immediately"
    - "Disconnecting a captain browser triggers the pause overlay on all connected clients within seconds"
    - "Reconnecting the captain browser within 30s clears the overlay and resumes the draft"
    - "Letting grace expire with an eligible team member promotes them to captain and resumes the draft"
    - "Timer-driven auto-advance updates the board for all clients reactively (not only on next interaction)"
  artifacts:
    - path: "src/lib/components/molecules/DraftBoard.svelte"
      provides: "Verified working in browser"
    - path: "src/lib/components/molecules/PauseOverlay.svelte"
      provides: "Verified showing grace countdown to all participants"
    - path: "src/live/room.js"
      provides: "Verified onUnsubscribe fires and publishes paused snapshot"
  key_links:
    - from: "Captain browser disconnect"
      to: "PauseOverlay on spectator client"
      via: "onUnsubscribe → updateDraftState(paused=true) → ctx.publish → lobby stream → DraftBoard conditional"
      pattern: "paused"
    - from: "Captain reconnect"
      to: "Overlay dismissed"
      via: "lobby stream init → clears paused → ctx.publish resumed snapshot → DraftBoard conditional false"
      pattern: "paused"
---

<objective>
Manual verification checkpoint: test the full draft board and disconnect resilience flows in a real browser with multiple clients.

Purpose: Visual rendering, multi-client coordination, and real-time behavior cannot be verified by automated commands. This checkpoint validates all Phase 4 requirements before closing the phase.
Output: User confirmation that all behaviors work as specified, or issue list for gap closure.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-draft-ui-disconnect-resilience/04-CONTEXT.md
@.planning/phases/04-draft-ui-disconnect-resilience/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and dev server pre-flight</name>
  <files></files>
  <read_first>
    - .planning/phases/04-draft-ui-disconnect-resilience/04-VALIDATION.md (sampling rate: npm test -- --run must be green before checkpoint)
  </read_first>
  <action>
    Run the full automated test suite to confirm all tests pass before handing off to the user for manual verification:

    ```bash
    npm test -- --run --reporter=verbose
    ```

    If tests fail, fix the failures before proceeding to the checkpoint task.

    Also confirm build compiles without errors:
    ```bash
    npm run check 2>&1 | tail -30
    ```

    Report any type errors or build warnings to the user before proceeding.
  </action>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run` exits 0 (no failures)
    - Output shows 0 failed tests
    - Build or check command produces no import errors for new component files
  </acceptance_criteria>
  <done>All automated tests green; no build errors before manual checkpoint begins</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual draft UI and disconnect resilience verification</name>
  <files></files>
  <action>Present the verification instructions below to the user and wait for their response before proceeding.</action>
  <verify>User types "approved" confirming all 20 verification steps passed, or "issues: [description]" listing failures.</verify>
  <done>User has confirmed all Phase 4 behaviors work correctly or identified issues for gap closure.</done>
  <what-built>
    Complete draft board UI (DraftBoard, ChampionGrid, TurnIndicator, TeamDraftColumn with pick/ban history, TimerDisplay with urgency) and full disconnect resilience (pause overlay, grace countdown, captain promotion, reconnect hydration).
  </what-built>
  <how-to-verify>
    **Setup (do once):**
    1. Start the dev server: `npm run dev`
    2. Open two browser windows (Window A = captain, Window B = spectator/second client). Use an incognito window for Window B if needed.
    3. In Window A: sign in, create a room, configure a short timer (10s for faster testing), and start the draft.

    **Verify DRAFT-07 — Draft board visible:**
    4. Confirm both windows show the DraftBoard (not the lobby layout) after draft starts.
    5. Confirm TurnIndicator shows "Team A — Ban" (or Pick, depending on script) with the captain's name below.
    6. Confirm the countdown number ticks down every second. At ≤10s, confirm number and progress bar turn red.
    7. In Window A (if you are the active captain): click a champion card — confirm amber ring appears. Click "Submit Ban" (or "Submit Pick") — confirm champion grays out in both windows and the next turn begins.
    8. Let the timer expire without picking in Window A — confirm the turn auto-advances in both windows reactively without requiring an interaction.
    9. After several turns, confirm pick/ban history slots fill in both TeamDraftColumn panels.

    **Verify DISC-01 — Pause overlay on captain disconnect:**
    10. When it is the captain's turn in Window A: close the Window A tab entirely (simulate disconnect).
    11. In Window B: confirm the PauseOverlay appears within 2-3 seconds showing "Draft paused", the captain's name, and a grace countdown ticking down from 30.

    **Verify DISC-02 — Captain reconnect resumes draft:**
    12. Within 30 seconds: reopen Window A (sign in again if needed, navigate back to the draft room URL).
    13. Confirm: overlay dismisses in both windows. Draft board resumes. Status banner "Reconnected — syncing…" appears briefly in Window A.

    **Verify DISC-02 — Member promotion when grace expires:**
    14. Set up a room with 2 players on the same team (both signed in). Start draft.
    15. Disconnect the active captain (close Window A tab).
    16. In Window B: watch the grace countdown reach 0. Confirm overlay dismisses and status banner "[Name] is now captain for Team X. Draft resumes." appears for 3 seconds.
    17. Confirm the promoted player can now make picks in Window B.

    **Verify DISC-03 — Draft cancelled when no member available:**
    18. Set up a room where the active team has only one player (the captain). Disconnect that captain.
    19. Let grace expire. Confirm all clients see "Draft cancelled" state (heading + body copy + "Return to lobby" button).

    **Verify DISC-04 — Reconnect hydration (no stale state):**
    20. Mid-draft: close and reopen Window B (spectator). Confirm it immediately shows the current draft state (correct pick/ban history, correct turn indicator) without requiring a page refresh.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all behaviors work as described.
    Type "issues: [description]" if any behavior does not match. List each failing scenario so gap closure can be planned.
  </resume-signal>
</task>

</tasks>

<verification>
Manual verification covers all DRAFT-07 and DISC-01 through DISC-04 requirements.
Automated pre-flight: npm test -- --run exits 0.
</verification>

<success_criteria>
- User confirms "approved" after testing all 20 verification steps
- Draft board renders and updates reactively for all participants
- Disconnect/grace/reconnect/promotion/cancellation flows work as specified
- Reconnect shows authoritative server state without stale rendering
</success_criteria>

<output>
After completion, create `.planning/phases/04-draft-ui-disconnect-resilience/04-05-SUMMARY.md`
</output>
