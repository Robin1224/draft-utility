---
phase: 06-post-draft-review
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/components/molecules/DraftReview.svelte.spec.js
autonomous: true
requirements:
  - POST-01

must_haves:
  truths:
    - "A browser-project spec file exists for DraftReview.svelte with it.todo stubs covering POST-01 UI behaviors"
  artifacts:
    - path: "src/lib/components/molecules/DraftReview.svelte.spec.js"
      provides: "Failing (todo) browser test stubs for DraftReview"
      contains: "it.todo"
  key_links:
    - from: "src/lib/components/molecules/DraftReview.svelte.spec.js"
      to: "src/lib/components/molecules/DraftReview.svelte"
      via: "import — component does not yet exist; stubs are todos so the suite stays green"
      pattern: "DraftReview"
---

<objective>
Create the Wave 0 test scaffold for DraftReview.svelte so that Plan 02 (which creates the component) has a runnable browser-project spec file to verify against.

Purpose: Nyquist compliance requires every auto task that produces UI code to have an automated verify command. DraftReview.svelte is created in Plan 02, Task 1. This Wave 0 plan creates the spec file with it.todo stubs so the command `npm run test -- --project=client src/lib/components/molecules/DraftReview.svelte.spec.js` is runnable before the component exists (todos are skipped, not failing).

Output: One new spec file with it.todo stubs. The file runs immediately without errors.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-post-draft-review/06-CONTEXT.md
@.planning/phases/06-post-draft-review/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DraftReview.svelte.spec.js with it.todo stubs</name>
  <files>src/lib/components/molecules/DraftReview.svelte.spec.js</files>
  <action>
Create `src/lib/components/molecules/DraftReview.svelte.spec.js` as a browser-project Vitest spec file.

The file must use `it.todo` stubs only — the component does not exist yet. This keeps the suite green (todos are skipped) while satisfying the Nyquist rule for Plan 02 Task 1.

Stubs must cover the following POST-01 UI behaviors:
- Renders two columns: Team A and Team B headings
- Renders a "Bans" section label in Team A column when bans exist
- Renders a "Picks" section label in Team A column when picks exist
- Resolves champion_id to display name via classes.json
- Skips null champion_id entries (timeout slots — no empty DraftSlot rendered)
- Hides empty Bans/Picks sections when no actions of that type exist
- Renders fallback text "Draft ended without picks or bans." when both teams have no resolved actions

```javascript
// @ts-nocheck
import { describe, it } from 'vitest';

// Wave 0 stubs — DraftReview.svelte is created in Plan 02.
// These are it.todo placeholders; convert to full tests after the component exists.

describe('DraftReview.svelte', () => {
  it.todo('renders "Team A" and "Team B" column headings');

  it.todo('renders "Bans" section label in Team A column when Team A has bans');

  it.todo('renders "Picks" section label in Team A column when Team A has picks');

  it.todo('resolves champion_id to display name via classes.json');

  it.todo('skips null champion_id entries — no DraftSlot rendered for timeout rows');

  it.todo('hides the Bans section when Team A has no bans');

  it.todo('hides the Picks section when Team A has no picks');

  it.todo('renders fallback text "Draft ended without picks or bans." when all actions have null champion_id');
});
```
  </action>
  <verify>
    <automated>npm run test -- --project=client src/lib/components/molecules/DraftReview.svelte.spec.js</automated>
  </verify>
  <done>Spec file exists; `npm run test -- --project=client src/lib/components/molecules/DraftReview.svelte.spec.js` exits 0 with 8 todos reported and 0 failures.</done>
</task>

</tasks>

<verification>
Confirm file was created:
`ls src/lib/components/molecules/DraftReview.svelte.spec.js`

Confirm todos are present:
`grep -c "it.todo" src/lib/components/molecules/DraftReview.svelte.spec.js`
Expected: 8

Run browser spec suite to confirm 0 failures:
`npm run test -- --project=client src/lib/components/molecules/DraftReview.svelte.spec.js`
</verification>

<success_criteria>
- `src/lib/components/molecules/DraftReview.svelte.spec.js` exists with 8 it.todo stubs
- `npm run test -- --project=client src/lib/components/molecules/DraftReview.svelte.spec.js` exits 0 with 0 failures (todos are skipped, not failing)
- Full suite `npm run test` still exits 0 (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-post-draft-review/06-00-SUMMARY.md`
</output>
