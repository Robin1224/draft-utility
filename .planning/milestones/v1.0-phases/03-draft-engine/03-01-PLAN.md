---
phase: 03-draft-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/catalog/classes.spec.js
  - src/lib/draft-script.spec.js
  - src/lib/server/draft.spec.js
  - src/live/draft.spec.js
  - src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js
autonomous: true
requirements:
  - LIST-01
  - DRAFT-01
  - DRAFT-02
  - DRAFT-03
  - DRAFT-04
  - DRAFT-05
  - DRAFT-06
  - HOST-01

must_haves:
  truths:
    - "All 5 spec files exist and run with failing/pending tests (RED state)"
    - "Test commands from VALIDATION.md are executable without 'file not found' errors"
    - "Each spec file targets the correct requirement IDs"
  artifacts:
    - path: "src/lib/catalog/classes.spec.js"
      provides: "LIST-01 test stubs"
    - path: "src/lib/draft-script.spec.js"
      provides: "DRAFT-01 test stubs"
    - path: "src/lib/server/draft.spec.js"
      provides: "DB layer + unique constraint stubs"
    - path: "src/live/draft.spec.js"
      provides: "DRAFT-02 through DRAFT-06 + timer test stubs"
    - path: "src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js"
      provides: "HOST-01 settings panel visibility stub"
  key_links:
    - from: "src/live/draft.spec.js"
      to: "src/live/draft.js"
      via: "vi.mock + createTestEnv"
      pattern: "createTestEnv"
    - from: "src/lib/server/draft.spec.js"
      to: "src/lib/server/draft.js"
      via: "mock db object"
      pattern: "mock.*db"
---

<objective>
Create all Wave 0 test scaffold files for Phase 3. These are the failing-test stubs that must exist before any implementation runs — the Nyquist contract from 03-VALIDATION.md.

Purpose: Establish RED state for every requirement before writing implementation. This gives the executor a clear target for each subsequent plan wave and prevents "no test exists" verification failures.
Output: 5 spec files — all with `todo` or `skip` stubs that run without import errors, ready to be filled in as implementation completes.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-draft-engine/03-VALIDATION.md
@.planning/phases/03-draft-engine/03-RESEARCH.md

<interfaces>
<!-- Existing test patterns to replicate -->

From src/live/room.spec.js:
```javascript
// @ts-nocheck
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { LiveError } from 'svelte-realtime/server';
import { createTestEnv } from 'svelte-realtime/test';
import * as rooms from '$lib/server/rooms.js';
vi.mock('$lib/server/rooms.js', async (importOriginal) => { ... });
const env = createTestEnv();
// env.register('room', roomModule);
// const client = env.connect({ role: 'player', id: 'user-1', name: 'X' });
// await client.call('room/rpcName', ...args)
```

From src/lib/server/rooms.spec.js:
```javascript
// @ts-nocheck
import { describe, it, expect } from 'vitest';
// Mock db pattern:
const db = {
  insert: () => ({ values: (v) => ({ returning: () => Promise.resolve([...]) }) }),
  select: () => ({ from: () => ({ where: () => ({ limit: () => Promise.resolve([]) }) }) }),
  update: () => ({ set: () => ({ where: () => ({ returning: () => Promise.resolve([]) }) }) })
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create catalog + draft-script + DB layer spec stubs</name>
  <read_first>
    - /Users/robinvanderheijden/Documents/GitHub/draft-utility/src/live/room.spec.js (test pattern)
    - /Users/robinvanderheijden/Documents/GitHub/draft-utility/src/lib/server/rooms.spec.js (mock db pattern)
    - /Users/robinvanderheijden/Documents/GitHub/draft-utility/.planning/phases/03-draft-engine/03-VALIDATION.md (test commands)
  </read_first>
  <files>
    src/lib/catalog/classes.spec.js,
    src/lib/draft-script.spec.js,
    src/lib/server/draft.spec.js
  </files>
  <action>
Create three spec stub files. Each must be importable (no missing module errors) at Wave 0 — use `vi.todo` or `it.todo` for stubs that reference modules not yet created.

**src/lib/catalog/classes.spec.js** — LIST-01:
```javascript
// @ts-nocheck
import { describe, it, expect } from 'vitest';

// classes.json does not exist yet — stub with todo
describe('classes catalog (LIST-01)', () => {
  it.todo('has exactly 28 entries');
  it.todo('every entry has id (kebab-case string), name (string), role (melee|ranged|support)');
  it.todo('melee group has 9 entries: Bakko, Jamila, Croak, Freya, Raigon, Rook, Ruh Kaan, Shifu, Thorn');
  it.todo('ranged group has 10 entries: Alysia, Ashka, Destiny, Ezmo, Iva, Jade, Jumong, Shen Rao, Taya, Varesh');
  it.todo('support group has 9 entries: Blossom, Lucie, Oldur, Pearl, Pestilus, Poloma, Sirius, Ulric, Zander');
});
```

**src/lib/draft-script.spec.js** — DRAFT-01:
```javascript
// @ts-nocheck
import { describe, it, expect } from 'vitest';

// draft-script.js does not exist yet — stub with todo
describe('DEFAULT_SCRIPT (DRAFT-01)', () => {
  it.todo('has exactly 10 turns');
  it.todo('first 4 turns are bans (2 per team alternating: A,B,A,B)');
  it.todo('last 6 turns are picks in snake order: A,B,B,A,A,B');
  it.todo('every turn has shape { team: "A"|"B", action: "pick"|"ban" }');
  it.todo('DEFAULT_TIMER_MS is 30000');
});
```

**src/lib/server/draft.spec.js** — DB layer (DRAFT-01 unique constraint, DRAFT-02):
```javascript
// @ts-nocheck
import { describe, it, expect, vi } from 'vitest';

// draft.js does not exist yet — stub with todo
describe('draft DB layer', () => {
  describe('writeDraftAction', () => {
    it.todo('inserts a row with room_id, turn_index, team, action, champion_id');
    it.todo('unique constraint violation (room_id, turn_index) is re-thrown as DUPLICATE_TURN');
  });

  describe('loadDraftSnapshot', () => {
    it.todo('returns null when room not found');
    it.todo('returns snapshot with draftState and actions array when phase is drafting');
  });

  describe('completeDraft', () => {
    it.todo('sets room phase to ended and ended_at to now');
  });
});
```
  </action>
  <verify>
    <automated>npm run test -- --project=server src/lib/catalog/classes.spec.js src/lib/draft-script.spec.js src/lib/server/draft.spec.js --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/catalog/classes.spec.js exists: `ls src/lib/catalog/classes.spec.js` exits 0
    - src/lib/draft-script.spec.js exists: `ls src/lib/draft-script.spec.js` exits 0
    - src/lib/server/draft.spec.js exists: `ls src/lib/server/draft.spec.js` exits 0
    - All three files contain `it.todo` stubs (grep `it.todo` each file returns matches)
    - Test run reports "todo" counts, zero "failed" for these files
  </acceptance_criteria>
  <done>Three spec files exist and run cleanly with all tests in todo state.</done>
</task>

<task type="auto">
  <name>Task 2: Create live draft RPC spec stubs + settings panel spec stub</name>
  <read_first>
    - /Users/robinvanderheijden/Documents/GitHub/draft-utility/src/live/room.spec.js (createTestEnv + vi.mock pattern)
    - /Users/robinvanderheijden/Documents/GitHub/draft-utility/.planning/phases/03-draft-engine/03-VALIDATION.md (full test map)
    - /Users/robinvanderheijden/Documents/GitHub/draft-utility/.planning/phases/03-draft-engine/03-RESEARCH.md (test example on line ~430)
  </read_first>
  <files>
    src/live/draft.spec.js,
    src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js
  </files>
  <action>
Create two spec stub files.

**src/live/draft.spec.js** — DRAFT-02 through DRAFT-06 + timer:

Use the same `createTestEnv` + `vi.mock` pattern as `src/live/room.spec.js`. Mock `$lib/server/rooms.js` and a forthcoming `$lib/server/draft.js`. All tests are `it.todo` at this stage since `src/live/draft.js` does not exist yet.

```javascript
// @ts-nocheck
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { LiveError } from 'svelte-realtime/server';
import { createTestEnv } from 'svelte-realtime/test';

// These modules do not exist yet — mock declarations only (vi.mock hoisted)
vi.mock('$lib/server/rooms.js', async (importOriginal) => {
  const actual = await importOriginal();
  return {
    ...actual,
    getRoomByPublicCode: vi.fn(),
    loadLobbySnapshot: vi.fn(),
    startDraftIfReady: vi.fn()
  };
});

vi.mock('$lib/server/draft.js', () => ({
  writeDraftAction: vi.fn(),
  loadDraftSnapshot: vi.fn(),
  completeDraft: vi.fn(),
  updateDraftState: vi.fn(),
  advanceTurnIfCurrent: vi.fn()
}));

describe('draft live RPCs (DRAFT-02 through DRAFT-06)', () => {
  const env = createTestEnv();

  afterEach(() => {
    env.cleanup();
    vi.clearAllMocks();
  });

  describe('startDraft with settings (DRAFT-02)', () => {
    it.todo('writes draft_state with turnIndex=0 and turnEndsAt timestamp to room');
    it.todo('publishes snapshot with phase:drafting and draftState');
    it.todo('non-host startDraft rejects with FORBIDDEN');
  });

  describe('pickBan — captain validation (DRAFT-03)', () => {
    it.todo('non-captain pickBan rejects with FORBIDDEN');
    it.todo('wrong team captain pickBan rejects with FORBIDDEN');
    it.todo('duplicate champion (already in draft_action) rejects with FORBIDDEN');
  });

  describe('pickBan — timer interaction (DRAFT-04)', () => {
    it.todo('timer callback is a no-op when turn already advanced (conditional update returns 0 rows)');
    it.todo('timer callback advances turn and publishes when still current');
  });

  describe('pickBan — draft completion (DRAFT-05)', () => {
    it.todo('pickBan on last turn calls completeDraft and sets phase to ended');
  });

  describe('startDraft with custom script (DRAFT-06)', () => {
    it.todo('startDraft with custom script payload uses that script instead of DEFAULT_SCRIPT');
    it.todo('startDraft with invalid script payload rejects with VALIDATION error');
  });
});
```

**src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js** — HOST-01:

Use vitest browser project (client). The component does not exist yet — all stubs are `it.todo`.

```javascript
// @ts-nocheck
import { describe, it } from 'vitest';

// DraftSettingsPanel.svelte does not exist yet
describe('DraftSettingsPanel (HOST-01)', () => {
  it.todo('renders turn timer input with default value 30');
  it.todo('renders script editor list with default 10-turn script');
  it.todo('renders "Add turn" button that appends a turn to the list');
  it.todo('renders "Remove" button on each row that removes that turn');
  it.todo('does not render when phase is not lobby (panel is removed, not disabled)');
});
```
  </action>
  <verify>
    <automated>npm run test -- --project=server src/live/draft.spec.js --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/live/draft.spec.js exists and contains `createTestEnv` import
    - src/live/draft.spec.js contains vi.mock declarations for both '$lib/server/rooms.js' and '$lib/server/draft.js'
    - src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js exists
    - grep `it.todo` src/live/draft.spec.js returns at least 8 matches
    - `npm run test -- --project=server src/live/draft.spec.js --reporter=dot` exits without "Cannot find module" errors
  </acceptance_criteria>
  <done>Both spec files exist and the server test suite runs cleanly with all draft tests in todo state.</done>
</task>

<task type="auto">
  <name>Task 3: Full test suite green with todo stubs</name>
  <read_first>
    - /Users/robinvanderheijden/Documents/GitHub/draft-utility/vite.config.js (test project config)
  </read_first>
  <files></files>
  <action>
Run the full test suite to confirm the new spec stubs do not break any existing passing tests. No file changes needed — this is a verification-only task.

If any existing tests are broken by the new mock declarations (e.g., hoisted vi.mock for '$lib/server/draft.js' leaking into other tests), fix the scope: move vi.mock calls inside describe blocks or add `vi.resetModules()` in afterEach as needed.

Expected result: all previously-passing tests still pass; new draft spec files show N todo + 0 failed.
  </action>
  <verify>
    <automated>npm run test -- --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits with 0 failures
    - Output contains "todo" entries for the new draft spec files
    - Previously passing test files (rooms.spec.js, room.spec.js, room-lifecycle.spec.js, join-parse.spec.js, page.server.spec.js) still show green
  </acceptance_criteria>
  <done>Full test suite green — zero regressions from adding Wave 0 spec stubs.</done>
</task>

</tasks>

<verification>
All 5 spec files exist at their specified paths.
`npm run test` passes with zero failures (todo tests are not failures).
No existing spec files regressed.
Each spec file has stubs matching the requirements from 03-VALIDATION.md Wave 0 Requirements section.
</verification>

<success_criteria>
- 5 spec stub files created
- `npm run test -- --project=server --reporter=dot` exits 0
- Zero new failures introduced into existing passing tests
- All draft test names are in todo state, ready to be activated as implementation completes
</success_criteria>

<output>
After completion, create `.planning/phases/03-draft-engine/03-01-SUMMARY.md`
</output>
