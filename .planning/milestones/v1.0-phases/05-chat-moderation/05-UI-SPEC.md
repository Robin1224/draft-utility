---
phase: 5
slug: chat-moderation
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-04
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for the chat-moderation phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Tailwind v4 with custom @theme tokens |
| Preset | not applicable |
| Component library | none |
| Icon library | Inline SVG (heroicons-style paths, hand-authored) |
| Font | Manrope (bundled TTF at src/lib/assets/fonts/Manrope.ttf) |

Source: layout.css @theme block + existing component audit.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge inline padding (gap-1, py-0.5) |
| sm | 8px | Compact element spacing, button vertical padding (gap-2, py-2) |
| md | 16px | Default element spacing, card padding (gap-4, px-4, mt-4) |
| lg | 24px | Section padding, major column gaps (gap-6, mt-6) |
| xl | 32px | Layout gaps between primary regions |
| 2xl | 48px | Major section breaks (py-16 for cancelled state) |
| 3xl | 64px | Page-level spacing (not used in this phase) |

Exceptions:
- Chat input send button touch target: minimum 44px height (min-h-[44px] — consistent with ChampionCard pattern).
- Mute button in spectator row: minimum 44px row height (min-h-11 — consistent with TeamColumn slot pattern).
- Sidebar panel: fixed or flex-shrink-0 width of 280px (matches TeamDraftColumn width in DraftBoard grid).

Source: existing component audit (TeamColumn, ChampionCard, DraftBoard grid proportions).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (font-normal) | 1.5 |
| Label | 12px (text-xs) | 600 (font-semibold) | 1.4 |
| Heading | 20px (text-xl) | 600 (font-semibold) | 1.2 |
| Display | 16px (text-base) | 600 (font-semibold) | 1.4 |

Notes:
- Chat message author name: 12px, font-semibold (text-xs font-semibold text-text-primary). Small size (12px) provides sufficient visual differentiation from body text without requiring a third weight.
- Chat message body: 14px, font-normal (text-sm font-normal text-text-secondary).
- Tab labels: 14px, font-normal for inactive tabs; font-semibold for active tab (text-sm font-semibold).
- Section heading "Spectators (N)": 16px, font-semibold — matches existing SpectatorsPanel toggle.
- "Mute" / "Unmute" button label: 14px, font-normal, red-600 — consistent with existing "Kick" button treatment.

Source: existing component audit (TeamColumn, SpectatorsPanel, LobbyHostBar, ChampionCard).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | slate-50 (--color-bg-primary) | Page background, chat panel background, message row backgrounds |
| Secondary (30%) | slate-200 (--color-bg-secondary) | Panel borders, tab underline, timestamp text backgrounds, input border, dividers |
| Accent (10%) | amber-500 | Active tab indicator underline, Send Message button background, Send Message button hover (amber-400) |
| Destructive | red-600 | "Mute" button label, "Unmute" button label — inline text buttons only, no filled backgrounds |

Accent reserved for:
1. Active tab underline indicator in ChatPanel tab bar (2px bottom border on the active tab).
2. Send Message button background (bg-amber-500 hover:bg-amber-400) — the only filled accent button in the chat surface.
3. Focus ring on chat input and send button (focus-visible:outline-amber-500) — consistent with SpectatorsPanel toggle focus pattern.

Text colors follow established token hierarchy:
- `text-text-primary` (slate-900): message author name, participant display names, active tab label.
- `text-text-secondary` (slate-700): message body text.
- `text-text-tertiary` (slate-500): timestamps, inactive tab labels, empty state body copy, character count hint.

Source: layout.css @theme + TeamColumn, LobbyHostBar, StatusBanner audit.

---

## Primary Visual Anchor

Primary visual anchor: Send Message button (amber-500 filled) — the only filled accent element in the chat surface. All other interactive elements use text-only or outline treatments.

---

## Component Inventory

New components to create for this phase:

### Atoms

| Component | File | Description |
|-----------|------|-------------|
| ChatMessage | src/lib/components/atoms/ChatMessage.svelte | Single message row: author (text-xs font-semibold), timestamp (text-xs text-text-tertiary), body (text-sm font-normal text-text-secondary). No avatars. |
| ChatInput | src/lib/components/atoms/ChatInput.svelte | Textarea (max 500 chars) + Send Message button. Shows character count at right edge when within 50 chars of limit (text-xs text-text-tertiary). Disabled state when no content. |
| MuteButton | src/lib/components/atoms/MuteButton.svelte | Inline text button: "Mute" (red-600) or "Unmute" (text-text-secondary). Sits alongside existing "Kick" button in spectator row. No icon — text only for v1. |

### Molecules

| Component | File | Description |
|-----------|------|-------------|
| ChatPanel | src/lib/components/molecules/ChatPanel.svelte | Right sidebar: tab bar (lobby phase shows "All" + role tab; draft phase shows role tab only) + scrollable message list + ChatInput at bottom. Width: 280px flex-shrink-0. |

### Extensions to Existing Components

| Component | Change |
|-----------|--------|
| SpectatorsPanel.svelte | Add MuteButton alongside Kick button per spectator row (host-only; hidden for non-host). Show muted indicator (text-xs text-text-tertiary italic "(muted)") on spectator name when is_muted is true. |
| src/routes/draft/[id]/+page.svelte | Add ChatPanel as right-side sibling column in the main layout flex/grid. |

---

## Layout Contract

### Lobby Phase — Sidebar Layout

```
┌─────────────────────────────────────────────────────┐
│  Main lobby area (teams, host bar, spectators)      │  ChatPanel (280px)
│                                                     │  ┌──────────────┐
│                                                     │  │ [ All ][ Team]│ ← tab bar
│                                                     │  │ messages...  │
│                                                     │  │ [input][Send]│
│                                                     │  └──────────────┘
└─────────────────────────────────────────────────────┘
```

- Two tabs: "All" (general, everyone) and "Team" (players) or "Spectator" (spectators).
- Spectators see "All" and "Spectator" tabs.
- Players see "All" and "Team" tabs.
- Active tab underlined with 2px amber-500 border-bottom.
- Inactive tab: text-text-tertiary, no underline.

### Draft Phase — Sidebar Layout

```
┌────────────────────────────────────────────────────────────┐
│  Draft board (TurnIndicator + TeamA + ChampionGrid + TeamB)│  ChatPanel (280px)
│                                                            │  ┌──────────────┐
│                                                            │  │  [Team]      │ ← single tab, no "All"
│                                                            │  │ messages...  │
│                                                            │  │ [input][Send]│
│                                                            │  └──────────────┘
└────────────────────────────────────────────────────────────┘
```

- Single tab only: "Team" (for players) or "Spectator" (for spectators).
- No "All" tab during the draft phase.
- Tab bar still rendered for visual consistency but with single tab (no tab switching UI).

Source: CONTEXT.md D-01, D-02, D-03.

---

## Interaction States

### ChatInput

| State | Visual |
|-------|--------|
| Empty | Send Message button disabled (opacity-50, cursor-not-allowed) |
| Typing | Send Message button enabled |
| At 451–500 chars | Character count shown (text-xs text-text-tertiary) |
| Submit | Input clears immediately on send; optimistic — no spinner |
| Rate-limited (message dropped) | No UI feedback — silent drop per D-12 |

### ChatMessage

| State | Visual |
|-------|--------|
| Own message | Author name: "You" (text-xs font-semibold text-text-primary) |
| Other message | Author name: displayName (text-xs font-semibold text-text-primary) |
| Slur-filtered | Never reaches client — no visual state needed |

### MuteButton (host view in SpectatorsPanel)

| State | Visual |
|-------|--------|
| Unmuted spectator | "Mute" button (text-sm font-normal text-red-600 hover:underline) |
| Muted spectator | "Unmute" button (text-sm font-normal text-text-secondary hover:underline) + "(muted)" label suffix on display name |
| Non-host view | MuteButton not rendered |

### ChatPanel — Tab Bar

| State | Visual |
|-------|--------|
| Active tab | text-text-primary font-semibold, 2px amber-500 underline |
| Inactive tab | text-text-tertiary font-normal, no underline |
| Tab hover | text-text-primary (no underline change on hover) |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Send Message" — chat input submit button |
| Empty state heading | "No messages yet" |
| Empty state body | "Be the first to say something." |
| Rate-limit error | Silent drop — no copy shown to user (D-12 behavior) |
| Slur reject (server) | Silent drop — no copy shown to user (CHAT-04 requirement) |
| Message too long (server reject) | "Message too long. Max 500 characters." — displayed as inline error below ChatInput, text-sm text-red-600 |
| Mute action | No confirmation — host clicks "Mute", spectator is immediately muted |
| Unmute action | No confirmation — host clicks "Unmute", spectator is immediately unmuted |
| Muted indicator in spectator list | "(muted)" — text-xs text-text-tertiary, italic, appended to display name |
| Tab label — general channel | "All" |
| Tab label — team channel (players) | "Team" |
| Tab label — spectator channel | "Spectator" |
| Chat panel heading (accessible) | aria-label="Chat" on the ChatPanel section element |

Destructive actions in this phase:
- "Mute" button: no confirmation dialog — immediate inline action, consistent with existing "Kick" button pattern in LobbyHostBar. The action is reversible (Unmute).
- "Kick" button (existing, not new): no change to existing pattern.

Source: CONTEXT.md D-14, D-15, D-16, D-17; REQUIREMENTS.md CHAT-03, CHAT-04.

---

## Accessibility Contract

- ChatPanel message list: `role="log"` with `aria-live="polite"` — new messages announced without interrupting user.
- ChatInput textarea: `aria-label="Message"` + `aria-describedby` pointing to character count span when visible.
- Send Message button: `aria-label="Send message"` when rendered as icon-only; keep text label "Send Message" for v1 (text button).
- Tab buttons: `aria-selected={true/false}` + `role="tab"` on each, `role="tablist"` on container.
- MuteButton: `aria-label="Mute {displayName}"` / `aria-label="Unmute {displayName}"` — uses display name for screen reader clarity.
- Focus ring: `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-amber-500` — consistent with SpectatorsPanel toggle pattern.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — project does not use shadcn |
| third-party | none | not applicable |

No third-party component registries are introduced in this phase. All components are hand-authored Svelte 5 atoms/molecules following established project patterns.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Right sidebar placement, tab topology (lobby vs draft) | CONTEXT.md D-01, D-02, D-03 |
| Channel topics and server-side authorization | CONTEXT.md D-05, D-06 |
| In-memory fanout only (no persistence) | CONTEXT.md D-07, D-08 |
| Slur filter: bundled JSON, NFKC normalization, 500-char cap | CONTEXT.md D-09, D-10, D-11 |
| Rate limiting: silent drop, in-memory | CONTEXT.md D-12, D-13 |
| Host does not see spectator chat | CONTEXT.md D-14 |
| Mute button in spectator list alongside Kick | CONTEXT.md D-15 |
| Mute state in-memory, no notification to muted user | CONTEXT.md D-16 |
| Muted visual indicator in spectator list | CONTEXT.md D-17 |
| Color tokens (bg-primary, bg-secondary, text-primary, etc.) | layout.css @theme + codebase audit |
| Amber-500 accent (buttons, focus ring) | TeamColumn.svelte, LobbyHostBar.svelte |
| Red-600 destructive (Kick pattern → Mute pattern) | LobbyHostBar.svelte |
| Typography scale (xs, sm, base, xl) | Codebase audit across all existing components |
| Font: Manrope | layout.css @font-face |
| 44px touch target minimum | ChampionCard.svelte min-h-[44px] |
| 280px sidebar width | DraftBoard.svelte grid-cols definition |
| Atomic/molecular component split | CONTEXT.md code_context section |
| No confirmation for Mute (reversible action) | CONTEXT.md D-14; LobbyHostBar Kick pattern |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
