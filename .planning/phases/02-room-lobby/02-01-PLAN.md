---
phase: 02-room-lobby
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/server/db/schema.js
  - drizzle/
  - src/lib/join-parse.js
  - src/lib/join-parse.spec.js
  - src/lib/server/rooms.js
  - src/lib/server/rooms.spec.js
autonomous: true
requirements: [ROOM-01]

must_haves:
  truths:
    - "Postgres stores rooms with unique public_code and immutable host_user_id"
    - "createRoom(hostUserId) persists a new lobby room and returns public_code"
    - "parseRoomCode extracts code from bare input or full URL with /draft/<code>"
  artifacts:
    - path: "src/lib/server/db/schema.js"
      provides: "room + room_member Drizzle tables"
      contains: "room"
    - path: "src/lib/server/rooms.js"
      provides: "createRoom, getRoomByPublicCode, generatePublicCode"
      contains: "createRoom"
    - path: "src/lib/join-parse.js"
      provides: "parseRoomCode for D-04 (client + server safe)"
      contains: "parseRoomCode"
  key_links:
    - from: "src/lib/server/rooms.js"
      to: "src/lib/server/db/schema.js"
      via: "drizzle insert/select"
      pattern: "db\\."
---

<objective>
Add Drizzle schema and server modules for rooms (short public codes per D-02), join input parsing per D-04, and tested CRUD helpers so the signed-in creator is persisted as non-transferable host (ROOM-01).

Purpose: HTTP and realtime layers in later plans depend on authoritative DB state and a single code generator/parser.
Output: migrations (or generated SQL), `rooms.js`, `join-parse.js`, unit specs.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-room-lobby/02-CONTEXT.md
@.planning/phases/02-room-lobby/02-RESEARCH.md

<interfaces>
From `src/lib/server/db/schema.js` (extend, do not remove `task` or auth re-export):
```js
import { pgTable, serial, integer, text } from 'drizzle-orm/pg-core';
export const task = pgTable('task', { ... });
export * from './auth.schema';
```

From `src/lib/server/db/index.js`:
```js
export const db = drizzle(client, { schema });
```

Alphabet for D-02 (from 02-RESEARCH.md): `'23456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz'`, length **7** via `customAlphabet`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dependencies + Drizzle schema + migration</name>
  <read_first>package.json, src/lib/server/db/schema.js, drizzle.config.js, .planning/phases/02-room-lobby/02-RESEARCH.md</read_first>
  <files>package.json, package-lock.json, src/lib/server/db/schema.js, drizzle/</files>
  <behavior>
    - Tests not required before schema lands; migration must apply cleanly when DATABASE_URL is set.
  </behavior>
  <action>
    1. Add runtime dependency `nanoid@^5` via `npm install nanoid` (production `dependencies`, not only devDependencies). If `svelte-realtime` / `svelte-adapter-uws` are missing from `dependencies`, add them explicitly so fresh installs match Phase 1 wiring (per 02-RESEARCH.md).
    2. In `schema.js`, define **`room`** table: `id` uuid primary key default `gen_random_uuid()`; `public_code` varchar(12) **notNull** **unique**; `host_user_id` text **notNull**; `phase` text **notNull** default `'lobby'` (app values: `lobby`, `drafting`, `review`, `ended`); `created_at` timestamp with timezone default `now()` notNull; `updated_at` timestamp with timezone default `now()` notNull; `ended_at` timestamp with timezone nullable.
    3. Define **`room_member`** table: `id` uuid pk default `gen_random_uuid()`; `room_id` uuid notNull references `room.id` on delete cascade; `user_id` text nullable; `guest_id` text nullable; `team` text nullable (use `'A'`, `'B'`, or null for spectator); `is_captain` boolean notNull default false; `joined_at` timestamp with timezone default `now()` notNull. Add index on `(room_id)` and unique partial or app-enforced rule: at most one row per `(room_id, user_id)` when `user_id` is non-null; same for `(room_id, guest_id)` when `guest_id` is non-null (implement via unique indexes where drizzle supports, or document enforcement in `rooms.js`).
    4. Run `npm run db:generate` with `DATABASE_URL` set; commit generated SQL under `drizzle/`. Document in task notes: local apply via `npm run db:push` or `npm run db:migrate` per team practice.
  </action>
  <acceptance_criteria>
    - `rg "nanoid" package.json` matches in `"dependencies"`.
    - `rg "pgTable\\('room'" src/lib/server/db/schema.js` returns a match.
    - `rg "public_code" src/lib/server/db/schema.js` returns a match.
    - `rg "room_member" src/lib/server/db/schema.js` returns a match.
    - `rg "host_user_id" src/lib/server/db/schema.js` returns a match.
    - A new file exists under `drizzle/` with `.sql` extension listing `CREATE TABLE` for `room` and `room_member` (or equivalent drizzle output).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run lint && npm run check</automated>
  </verify>
  <done>Schema and migration artifacts exist; nanoid is a listed production dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseRoomCode + unit tests (D-04)</name>
  <read_first>src/lib/server/db/schema.js, .planning/phases/02-room-lobby/02-CONTEXT.md</read_first>
  <files>src/lib/join-parse.js, src/lib/join-parse.spec.js</files>
  <behavior>
    - `parseRoomCode('  abc123x ') === 'abc123x'`
    - `parseRoomCode('https://example.com/draft/XYZ789a') === 'XYZ789a'`
    - `parseRoomCode('http://localhost:5173/foo/draft/CODE123/bar')` resolves to `CODE123` when `draft` segment precedes code (last segment after `draft` in path).
  </behavior>
  <action>
    Create **`src/lib/join-parse.js`** (no `$lib/server` imports — safe for `.svelte` client bundles) exporting `/** @param {string} raw */ export function parseRoomCode(raw)` implementing the algorithm from 02-RESEARCH.md (try `new URL`, split pathname, find `draft` segment, else return trimmed string). Create **`src/lib/join-parse.spec.js`** with vitest `describe`/`it` covering: bare code with whitespace; full https URL with `/draft/<code>`; invalid URL fallback to trimmed input; code with allowed charset length 7.
  </action>
  <acceptance_criteria>
    - `src/lib/join-parse.js` exports `parseRoomCode`.
    - `src/lib/join-parse.spec.js` contains `describe('parseRoomCode'`.
    - `npm run test:unit -- --run src/lib/join-parse.spec.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run test:unit -- --run src/lib/join-parse.spec.js</automated>
  </verify>
  <done>Join parsing is deterministic and covered by Vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: rooms.js — generate code, createRoom, getRoomByPublicCode</name>
  <read_first>src/lib/server/db/index.js, src/lib/server/db/schema.js, src/lib/join-parse.js</read_first>
  <files>src/lib/server/rooms.js, src/lib/server/rooms.spec.js</files>
  <behavior>
    - Mock db: `createRoom` sets `host_user_id` to passed id and returns object with `public_code` length 7 from allowed alphabet.
    - `getRoomByPublicCode` returns null when no row (mock).
    - Integration optional; unit tests use mocked `db` module.
  </behavior>
  <action>
    1. In `src/lib/server/rooms.js`, export `ROOM_CODE_ALPHABET = '23456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz'` and `CODE_LENGTH = 7`. Export `generatePublicCode()` using `import { customAlphabet } from 'nanoid'` with that alphabet and length.
    2. Export `topicForRoom(publicCode)` returning `'lobby:' + publicCode` (centralizes topic string for Phase 2 realtime).
    3. Export `async function createRoom(db, hostUserId)` — insert into `room` with `public_code` from generator, `host_user_id: hostUserId`, `phase: 'lobby'`. On unique violation on `public_code`, retry up to **5** times then throw. Returns `{ id, public_code, host_user_id, phase }`.
    4. Export `async function getRoomByPublicCode(db, code)` — normalize with `import { parseRoomCode } from '$lib/join-parse.js'` before query; select by `public_code`; return row or **null**. Do **not** apply expiry here (plan 02-06 adds lazy expiry).
    5. Add `src/lib/server/rooms.spec.js` with vi.mocked db or stub object verifying `host_user_id` immutability is enforced by API design (no update host function exported).
  </action>
  <acceptance_criteria>
    - `rg "export async function createRoom" src/lib/server/rooms.js` matches.
    - `rg "export async function getRoomByPublicCode" src/lib/server/rooms.js` matches.
    - `rg "topicForRoom" src/lib/server/rooms.js` matches.
    - `rg "customAlphabet" src/lib/server/rooms.js` matches.
    - `npm run test:unit -- --run src/lib/server/rooms.spec.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run test:unit -- --run src/lib/server/rooms.spec.js</automated>
  </verify>
  <done>ROOM-01 persistence helpers exist with tests; topic helper ready for 02-03.</done>
</task>

</tasks>

<verification>
Run `npm run test:unit -- --run --project server` from repo root; join-parse and rooms specs pass.
</verification>

<success_criteria>
Room and member tables exist in Drizzle schema with migration files; `createRoom` and `getRoomByPublicCode` are implemented and tested; `parseRoomCode` tested per D-04.
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-lobby/02-01-SUMMARY.md`
</output>
