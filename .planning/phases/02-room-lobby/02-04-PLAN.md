---
phase: 02-room-lobby
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - src/hooks.server.js
  - src/hooks.ws.js
  - src/live/room.js
  - src/live/room.spec.js
autonomous: true
requirements: [ROOM-06, HOST-02, HOST-03]

must_haves:
  truths:
    - "Host-only RPCs: startDraft, kickMember, movePlayer, cancelRoom (lobby end) enforce host_user_id === ctx.user.id"
    - "startDraft succeeds only when both teams have at least one captain and phase is lobby"
    - "Guests carry stable guestId from cookie readable at WS upgrade for kick targets"
    - "movePlayer rejected when room.phase !== 'lobby'"
  artifacts:
    - path: "src/hooks.ws.js"
      provides: "guestId on guest upgrade; refreshSession preserves guestId"
      contains: "guestId"
    - path: "src/live/room.js"
      provides: "startDraft, kickMember, movePlayer, cancelRoom RPCs"
      contains: "startDraft"
  key_links:
    - from: "hooks.server.js"
      to: "Cookie draft_guest"
      via: "event.cookies.set / get"
      pattern: "draft_guest"
    - from: "kickMember"
      to: "room_member delete"
      via: "db.delete"
      pattern: "kickMember|kick"
---

<objective>
Add stable **guest identity** for HOST-02, implement **host-only** lobby controls (kick, move player between teams before draft, start draft with captain gate per ROOM-06, cancel room for ROOM-08 handoff), and extend **refreshSession** so promoted guests retain `guestId`.

Purpose: Server-authoritative moderation and phase transition to `drafting` for Phase 3.
Output: updated hooks, live RPCs, expanded tests.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-room-lobby/02-CONTEXT.md (D-08 host grouping — RPCs exist; UI grouping in 02-05)
@.planning/phases/02-room-lobby/02-RESEARCH.md (Pitfall 1 guest kick)

<interfaces>
`kickMember` target identifier: **either** `userId` string **or** `guestId` string (mutually exclusive) in RPC args shape `(publicCode, { userId?: string, guestId?: string })`.

`movePlayer(publicCode, { userId, toTeam })` — `toTeam` `'A'` | `'B'`, only signed-in players with `userId`.

`startDraft(publicCode)` — sets `room.phase` to `'drafting'`, sets `updated_at`, publishes full snapshot via `topicForRoom` `set` event.

`cancelRoom(publicCode)` — host-only; sets `phase: 'ended'`, `ended_at: now()` (columns from 02-01 schema); publish `set` snapshot; used for ROOM-08 before lazy purge (02-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: draft_guest cookie + upgrade() guestId</name>
  <read_first>src/hooks.server.js, src/hooks.ws.js, src/lib/server/auth.js</read_first>
  <files>src/hooks.server.js, src/hooks.ws.js</files>
  <action>
    1. **`src/hooks.server.js`:** Compose a **new** handle **before** `svelteKitHandler` that ensures cookie **`draft_guest`**: if `event.cookies.get('draft_guest')` is missing, set to `crypto.randomUUID()` with options `{ path: '/', httpOnly: true, sameSite: 'lax', maxAge: 60 * 60 * 24 * 400 }` (~400 days). Use `event.cookies.get/set` from SvelteKit; do not break Better Auth handler — chain: `guestCookieHandle` then existing `handleBetterAuth`.
    2. **`src/hooks.ws.js` `upgrade({ headers })`:** Parse `Cookie` header for `draft_guest` (manual parse or use helper). For **guest** branch (`!session`): return `{ role: 'guest', guestId: <cookie value or new uuid if absent> }` — prefer cookie match HTTP layer; if missing at upgrade, generate uuid string (connection-scoped fallback).
    3. **`live.auth.refreshSession`:** When upgrading to player, set `ctx.user.guestId = ctx.user.guestId ?? previousGuest` if you stash pre-upgrade guest id, **or** read `draft_guest` from `ctx.platform?.req?.headers` cookie header again and attach `ctx.user.guestId` so the same browser tab keeps guest id after Discord sign-in (align D-14 with Pitfall 1).
  </action>
  <acceptance_criteria>
    - `rg "draft_guest" src/hooks.server.js` matches.
    - `rg "guestId" src/hooks.ws.js` matches.
    - `rg "refreshSession" src/hooks.ws.js` matches and file still exports `message` from `svelte-realtime/server`.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run check</automated>
  </verify>
  <done>Guests have stable `guestId` in WS userData for targeting kicks.</done>
</task>

<task type="auto">
  <name>Task 2: rooms.js mutations for kick, move, startDraft, cancel</name>
  <read_first>src/lib/server/rooms.js, src/lib/server/db/schema.js</read_first>
  <files>src/lib/server/rooms.js</files>
  <action>
    Add DB helpers (names at discretion):
    1. `assertHost(room, userId)` — throws if `room.host_user_id !== userId`.
    2. `kickMember(db, { roomId, hostUserId, targetUserId?, targetGuestId? })` — assert host; delete **one** `room_member` row matching target; throw if none.
    3. `movePlayer(db, { roomId, hostUserId, userId, toTeam })` — assert host; `room.phase` must be `'lobby'`; move player's `team`; **recompute captains** for both affected teams using earliest `joined_at` rule (ROOM-05).
    4. `startDraftIfReady(db, { roomId, hostUserId })` — assert host; verify team A and B each have **≥1** member with `is_captain === true` (and at least one player on each team); set `phase = 'drafting'`, `updated_at = now()`; return updated room row.
    5. `cancelRoomAsHost(db, { roomId, hostUserId })` — assert host; set `phase = 'ended'`, `ended_at = now()`, `updated_at = now()`.
    All functions used by live RPCs must be **transactional** where multiple writes occur.
  </action>
  <acceptance_criteria>
    - `rg "kickMember|delete.*room_member" src/lib/server/rooms.js` matches (kick implemented).
    - `rg "startDraftIfReady|startDraft" src/lib/server/rooms.js` matches.
    - `rg "movePlayer|move.*team" src/lib/server/rooms.js` matches.
    - `rg "cancelRoom|ended_at" src/lib/server/rooms.js` matches.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run check</automated>
  </verify>
  <done>Host mutations and phase transitions are encapsulated server-side.</done>
</task>

<task type="auto">
  <name>Task 3: live RPCs + tests</name>
  <read_first>src/live/room.js, src/hooks.ws.js, src/lib/server/rooms.js</read_first>
  <files>src/live/room.js, src/live/room.spec.js</files>
  <action>
    In `src/live/room.js`, export:
    - `kickMember(ctx, publicCode, payload)` — host + player only; call DB kick; `ctx.publish(topicForRoom(publicCode), 'set', await loadLobbySnapshot(...))`.
    - `movePlayer(ctx, publicCode, payload)` — same; reject guests.
    - `startDraft(ctx, publicCode)` — host; call `startDraftIfReady`; publish `set` snapshot including `phase: 'drafting'`.
    - `cancelRoom(ctx, publicCode)` — host; call `cancelRoomAsHost`; publish `set` snapshot.
    Each RPC: non-host → `LiveError('FORBIDDEN', ...)`. Wrong phase → appropriate `LiveError`.

    Extend `src/live/room.spec.js`:
    - Non-host `startDraft` rejects.
    - Host `startDraft` rejects when a team lacks captain (mock DB).
    - `movePlayer` rejects when phase is `drafting` (mock).

    Keep all new RPCs in `src/live/room.js`; do not move `live.auth.refreshSession` out of `hooks.ws.js` without preserving Phase 1 client imports (`$live/auth` or equivalent) unchanged.
  </action>
  <acceptance_criteria>
    - `rg "export const kickMember|export const movePlayer|export const startDraft|export const cancelRoom" src/live/room.js` — at least three of four match as `export const`.
    - `npm run test:unit -- --run src/live/room.spec.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run test:unit -- --run src/live/room.spec.js</automated>
  </verify>
  <done>ROOM-06 and HOST-02/03 behaviors enforced in RPC + tests.</done>
</task>

</tasks>

<verification>
`npm run test:unit -- --run --project server` passes.
</verification>

<success_criteria>
Host can kick, move (lobby only), start draft with two captains, cancel/end room; guests have `guestId` at upgrade.
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-lobby/02-04-SUMMARY.md`
</output>
