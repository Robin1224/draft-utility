---
phase: 02-room-lobby
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/routes/+page.server.js
  - src/routes/+page.svelte
  - src/lib/components/molecules/Create.svelte
  - src/lib/components/molecules/Join.svelte
  - src/routes/draft/[id]/+page.server.js
autonomous: true
requirements: [ROOM-01, ROOM-02]

must_haves:
  truths:
    - "Signed-in user can submit create from home and lands on /draft/<public_code>"
    - "Join accepts pasted full URL or bare code and navigates to /draft/<code> per D-01/D-04"
    - "draft/[id] load returns 404 when room code unknown"
  artifacts:
    - path: "src/routes/+page.server.js"
      provides: "createRoom action for authenticated users"
      contains: "createRoom"
    - path: "src/routes/draft/[id]/+page.server.js"
      provides: "load room by public_code"
      contains: "getRoomByPublicCode"
  key_links:
    - from: "Create.svelte"
      to: "?/createRoom"
      via: "form method POST"
      pattern: "createRoom"
    - from: "Join.svelte"
      to: "parseRoomCode"
      via: "before goto"
      pattern: "parseRoomCode"
---

<objective>
Wire HTTP entry points: authenticated room creation from home (ROOM-01), join navigation and server load for `/draft/[id]` (ROOM-02, D-01). Replace placeholder `/draft/create` navigation per CONTEXT.

Purpose: Users can reach the lobby route with a real persisted room.
Output: `+page.server.js` for home, `draft/[id]/+page.server.js`, updated Create/Join molecules, home page imports Create.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-room-lobby/02-CONTEXT.md
@.planning/phases/02-room-lobby/02-UI-SPEC.md

<interfaces>
From `src/lib/server/rooms.js` (plan 02-01): `createRoom(db, hostUserId)`, `getRoomByPublicCode(db, code)`.
From `src/lib/join-parse.js`: `parseRoomCode(raw)` (client-safe).
From `src/hooks.server.js`: `event.locals.user` set when session exists.
Use `import { resolve } from '$app/paths'` and `redirect` from `@sveltejs/kit` for redirects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home +page.server.js createRoom action</name>
  <read_first>src/hooks.server.js, src/lib/server/db/index.js, src/lib/server/rooms.js, src/routes/login/+page.server.js</read_first>
  <files>src/routes/+page.server.js</files>
  <action>
    Create `src/routes/+page.server.js` exporting:
    - `actions.createRoom` as default action: if `!event.locals.user?.id`, `return fail(401, { message: 'Sign in to create a room' })`. Else call `createRoom(db, event.locals.user.id)` from `$lib/server/rooms`, then `redirect(303, resolve('/draft/' + result.public_code))` (use `resolve` so base path is correct).
    - Optional: export empty `load` returning `{}` if needed for layout; keep minimal.
    Use `import { db } from '$lib/server/db'`.
  </action>
  <acceptance_criteria>
    - File `src/routes/+page.server.js` exists and contains `createRoom`.
    - `rg "redirect\\(303" src/routes/+page.server.js` matches.
    - `rg "locals\\.user" src/routes/+page.server.js` matches.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && rg "createRoom" src/routes/+page.server.js && npm run check</automated>
  </verify>
  <done>Unauthenticated create attempts fail; authenticated create redirects to `/draft/<code>`.</done>
</task>

<task type="auto">
  <name>Task 2: Create.svelte + Join.svelte + home page wire-up</name>
  <read_first>src/lib/components/molecules/Create.svelte, src/lib/components/molecules/Join.svelte, src/routes/+page.svelte</read_first>
  <files>src/lib/components/molecules/Create.svelte, src/lib/components/molecules/Join.svelte, src/routes/+page.svelte</files>
  <action>
    1. **Create.svelte:** Remove `goto('/draft/create')`. Use a `<form method="POST" action="?/createRoom" use:enhance>` (or standard POST) with a single submit button labeled **Create** — same Tailwind classes as current primary button. Import `enhance` from `$app/forms` if using progressive enhancement.
    2. **Join.svelte:** `import { parseRoomCode } from '$lib/join-parse.js'` then on submit: `const code = parseRoomCode(draftId); goto(resolve('/draft/' + code), { replaceState: true })`.
    3. **+page.svelte:** Render both **Create** and **Join** sections (stacked or grid) so home offers create + join per CONTEXT canonical refs; match existing layout with `Header` if needed.
  </action>
  <acceptance_criteria>
    - `Create.svelte` contains `action="?/createRoom"` or `?/createRoom` in formaction.
    - `Create.svelte` does not contain `/draft/create`.
    - `Join.svelte` contains `parseRoomCode` and `$lib/join-parse`.
    - `src/routes/+page.svelte` imports and renders `Create` component.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && ! rg "/draft/create" src/lib/components/molecules/Create.svelte && npm run check</automated>
  </verify>
  <done>Home supports create (auth) and join with URL or code per D-04.</done>
</task>

<task type="auto">
  <name>Task 3: draft/[id]/+page.server.js load</name>
  <read_first>src/lib/server/rooms.js, src/routes/draft/[id]/+page.svelte</read_first>
  <files>src/routes/draft/[id]/+page.server.js</files>
  <action>
    Create `src/routes/draft/[id]/+page.server.js` exporting async `load({ params, locals })`:
    - `const code = parseRoomCode(params.id)` using `import { parseRoomCode } from '$lib/join-parse.js'`.
    - `const room = await getRoomByPublicCode(db, code)`; if `!room`, `error(404, { message: 'Room not found' })`.
    - Return data: `{ room: { public_code: room.public_code, phase: room.phase, host_user_id: room.host_user_id }, userId: locals.user?.id ?? null }` (do not expose internal uuid if UI only needs code — include `id` if realtime needs it; **must** pass `public_code` for topic subscription).
    Plan 02-06 will add expiry side effects inside `getRoomByPublicCode` or load — for this plan, no expiry yet.
  </action>
  <acceptance_criteria>
    - `src/routes/draft/[id]/+page.server.js` exists and exports `load`.
    - `rg "getRoomByPublicCode" src/routes/draft/[id]/+page.server.js` matches.
    - `rg "error\\(404" src/routes/draft/[id]/+page.server.js` matches.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run check</automated>
  </verify>
  <done>Lobby route receives server-validated room payload or 404.</done>
</task>

</tasks>

<verification>
`npm run check` passes; manual smoke: create while signed in redirects to draft URL; join with pasted URL strips to code segment.
</verification>

<success_criteria>
ROOM-01 and ROOM-02 satisfied at HTTP layer: create + load/join path work with short codes and D-04 parsing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-lobby/02-02-SUMMARY.md`
</output>
