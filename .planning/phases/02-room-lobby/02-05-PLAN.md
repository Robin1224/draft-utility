---
phase: 02-room-lobby
plan: 05
type: execute
wave: 5
depends_on: [02-02, 02-03, 02-04, 02-06]
files_modified:
  - src/lib/components/atoms/Phases.svelte
  - src/lib/components/molecules/LobbyHostBar.svelte
  - src/lib/components/molecules/TeamColumn.svelte
  - src/lib/components/molecules/SpectatorsPanel.svelte
  - src/routes/draft/[id]/+page.server.js
  - src/routes/draft/[id]/+page.svelte
  - .env.example
autonomous: true
requirements: [ROOM-03, ROOM-06, ROOM-07]

must_haves:
  truths:
    - "Lobby shows Team A | Team B, collapsible spectators, Phases strip with future steps disabled per D-06"
    - "Host sees grouped controls: kick, move, start draft, cancel; Start disabled until both captains (ROOM-06) with visible reason per UI-SPEC"
    - "Copy link writes full absolute URL (D-03) using server-provided origin + /draft/<code>"
    - "Guests see rosters but no join-team buttons; sign-in path available"
  artifacts:
    - path: "src/routes/draft/[id]/+page.svelte"
      provides: "Lobby UI wired to $live/room"
      contains: "$live/room"
    - path: "src/lib/components/molecules/LobbyHostBar.svelte"
      provides: "D-08 host control grouping"
      contains: "Start draft|startDraft"
  key_links:
    - from: "+page.svelte"
      to: "lobby(publicCode)"
      via: "$live/room store subscription"
      pattern: "lobby\\("
    - from: "Copy link button"
      to: "navigator.clipboard.writeText"
      via: "click handler"
      pattern: "clipboard"
---

<objective>
Build the **lobby UI** for `/draft/[id]` per **02-UI-SPEC.md** and CONTEXT **D-05–D-08**: two team columns, collapsible spectators, phased strip, host bar, **copy full URL** (ROOM-07, D-03), guest read-only vs signed-in join (ROOM-03), and **Start draft** disabled state tied to snapshot captains (ROOM-06).

Purpose: Human-visible delivery of Phase 2 requirements; connects HTTP load data to `svelte-realtime` stores and RPCs.
Output: new molecules, updated `Phases.svelte`, `+page.svelte` + minimal `+page.server.js` augmentation for `appOrigin`.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-room-lobby/02-UI-SPEC.md
@.planning/phases/02-room-lobby/02-CONTEXT.md
@src/lib/components/molecules/Header.svelte

<interfaces>
`+page.server.js` load already returns `room` and `userId`. Add **`appOrigin`**: `const url = new URL(event.request.url); return { ..., appOrigin: url.origin }` (D-03 primary). Optional env fallback: if `PUBLIC_APP_URL` exists in `.env.example`, document it; prefer `url.origin` for dev correctness.

Client stubs: `import { lobby, joinTeam, kickMember, movePlayer, startDraft, cancelRoom } from '$live/room'` (export names must match `src/live/room.js`).

Snapshot shape from `loadLobbySnapshot`: `teams.A`, `teams.B`, `spectators`, `phase`, `hostUserId`, `publicCode`.

Phases mapping: `lobby` → highlight step 1; `drafting` → step 2; `review` → step 3. When `data.room.phase === 'lobby'`, steps 2–3 get **disabled styling** per D-06 (`opacity-50`, `pointer-events-none`, `aria-disabled="true"`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phases.svelte — active step + disabled future steps</name>
  <read_first>src/lib/components/atoms/Phases.svelte, .planning/phases/02-room-lobby/02-UI-SPEC.md</read_first>
  <files>src/lib/components/atoms/Phases.svelte</files>
  <action>
    Refactor props to accept **`roomPhase`** string: `'lobby' | 'drafting' | 'review'` (default `'lobby'`). Map to highlighted index 1–3. For segments **after** the active phase index, apply classes: `opacity-50 pointer-events-none select-none` and `aria-disabled="true"` on each disabled `<li>`. Active segment keeps current `underline` / `text-text-secondary` treatment per existing design. Export nothing else; keep `uppercase` layout.
  </action>
  <acceptance_criteria>
    - `Phases.svelte` contains `roomPhase` in `$props()` or destructuring.
    - `rg "aria-disabled" src/lib/components/atoms/Phases.svelte` matches.
    - `rg "pointer-events-none" src/lib/components/atoms/Phases.svelte` matches.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run check</automated>
  </verify>
  <done>D-06 visual contract implemented in Phases atom.</done>
</task>

<task type="auto">
  <name>Task 2: TeamColumn + SpectatorsPanel molecules</name>
  <read_first>src/lib/components/molecules/Header.svelte, .planning/phases/02-room-lobby/02-UI-SPEC.md</read_first>
  <files>src/lib/components/molecules/TeamColumn.svelte, src/lib/components/molecules/SpectatorsPanel.svelte</files>
  <action>
    1. **`TeamColumn.svelte`:** props: `label` ('Team A' | 'Team B'), `members` (array from snapshot), `teamKey` ('A'|'B'), `isGuest` boolean, `canJoin` boolean (signed-in, not on a team, room phase lobby), `onJoin` callback, `full` boolean from snapshot (member count >= 3). Render captain badge text **Captain**, host badge **Host** per member flags. Empty slots up to 3 total rows showing **Open**. Join button: **Join Team A/B** amber style when `canJoin && !full && !isGuest`; when `isGuest`, show line with link `resolve('/login')` + `?redirect=` current path (use `page.url.pathname + page.url.search` from `$app/state` or pass prop).
    2. **`SpectatorsPanel.svelte`:** collapsible via `<button aria-expanded>` + panel; header **Spectators (N)**; default **collapsed** on `max-sm` breakpoint optional — pick **collapsed by default on all** for simplicity unless UI-SPEC desktop-open rule; document choice in component comment. Chevron + keyboard support.
  </action>
  <acceptance_criteria>
    - Files `TeamColumn.svelte` and `SpectatorsPanel.svelte` exist under `src/lib/components/molecules/`.
    - `rg "Captain|Host" src/lib/components/molecules/TeamColumn.svelte` matches.
    - `rg "aria-expanded" src/lib/components/molecules/SpectatorsPanel.svelte` matches.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run check</automated>
  </verify>
  <done>D-05 roster columns and D-07 spectator collapse exist as molecules.</done>
</task>

<task type="auto">
  <name>Task 3: LobbyHostBar + draft page wire-up + copy link + load appOrigin</name>
  <read_first>src/routes/draft/[id]/+page.svelte, src/routes/draft/[id]/+page.server.js, src/lib/components/atoms/Phases.svelte, src/live/room.js</read_first>
  <files>src/lib/components/molecules/LobbyHostBar.svelte, src/routes/draft/[id]/+page.svelte, src/routes/draft/[id]/+page.server.js, .env.example</files>
  <action>
    1. **`LobbyHostBar.svelte`:** props: `isHost`, `snapshot` (latest lobby object), `publicCode`, `onKick({ userId?, guestId? })`, `onMove(userId, toTeam)`, `onStartDraft()`, `onCancelRoom()`. Render **only if `isHost`**. Group buttons in a flex row (D-08). **Start draft** primary amber, `disabled` when snapshot lacks captain on A or B or `snapshot.phase !== 'lobby'`; show adjacent `text-sm text-text-tertiary`: `Both teams need a captain` when disabled (02-UI-SPEC). Kick: for each roster row (pass members from parent) show **Kick** for non-host targets. Move: minimal UX — e.g. select player + target team A/B (executor discretion) using snapshot `userId` only (signed-in players).
    2. **`+page.server.js`:** add `appOrigin: new URL(event.request.url).origin` to returned data; add optional comment for `PUBLIC_APP_URL` in `.env.example` as override **if** you implement fallback in UI — otherwise only document `appOrigin` from request.
    3. **`+page.svelte`:** `let { data } = $props()`; `const lobbyStore = lobby(data.room.public_code)` from `$live/room`; handle `$lobbyStore === undefined` loading, `.error` display, else render. `Header` + `Phases roomPhase={$lobbyStore.phase}`. Instantiate `TeamColumn` x2, `SpectatorsPanel`, `LobbyHostBar`. **Copy link** button (visible to all): `const fullUrl = `${data.appOrigin}${resolve('/draft/' + data.room.public_code)}`` — use `resolve` for base path; `navigator.clipboard.writeText(fullUrl)`; show **Copied** inline `text-sm` success for ~2s on success; `aria-label="Copy room link"` on button.
    4. Wire `joinTeam`, `kickMember`, `movePlayer`, `startDraft`, `cancelRoom` with `async` handlers and basic error display (`text-red-600` one line).
  </action>
  <acceptance_criteria>
    - `rg "lobby\\(" src/routes/draft/[id]/+page.svelte` matches.
    - `rg "clipboard\\.writeText|navigator\\.clipboard" src/routes/draft/[id]/+page.svelte` matches.
    - `rg "appOrigin" src/routes/draft/[id]/+page.server.js` matches.
    - `LobbyHostBar.svelte` contains `Start draft` or `Start Draft` (case per design).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run check</automated>
  </verify>
  <done>ROOM-07 copy URL, ROOM-03 guest UX, ROOM-06 start gating visible; lobby subscribes to realtime snapshot.</done>
</task>

</tasks>

<verification>
`npm run build` succeeds; manual: open `/draft/<code>`, copy button pastes full URL with correct origin; guest sees no join; host sees disabled start until two captains.
</verification>

<success_criteria>
Lobby matches UI-SPEC structure; copy link is full absolute URL; host controls grouped; realtime roster visible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-lobby/02-05-SUMMARY.md`
</output>
