---
phase: 02-room-lobby
plan: 06
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - src/lib/server/room-lifecycle.js
  - src/lib/server/room-lifecycle.spec.js
  - src/lib/server/rooms.js
autonomous: true
requirements: [ROOM-08]

must_haves:
  truths:
    - "Ended or cancelled rooms are not joinable via getRoomByPublicCode (returns null)"
    - "Lobby rooms abandoned for 24h+ are lazily marked ended so they do not persist indefinitely"
    - "Pure time/phase rules are unit-tested without a browser"
  artifacts:
    - path: "src/lib/server/room-lifecycle.js"
      provides: "LOBBY_ABANDON_TTL_MS, shouldCloseRoomForRead(row, now), applyLazyLobbyExpiry(tx, row, now)"
      contains: "LOBBY_ABANDON"
    - path: "src/lib/server/rooms.js"
      provides: "getRoomByPublicCode integrates expiry before return"
      contains: "getRoomByPublicCode"
  key_links:
    - from: "getRoomByPublicCode"
      to: "room-lifecycle.js"
      via: "lazy abandon + ended filter"
      pattern: "applyLazy|ended"
---

<objective>
Implement **ROOM-08** with explicit policy: (1) rows with `phase === 'ended'` or non-null `ended_at` are **not returned** to new HTTP loads or usable as active lobby; (2) **abandoned lobby** rooms older than **24 hours** from `created_at` are lazily transitioned to `ended` with `ended_at = now()` inside the same DB read path; (3) optional row deletion is **out of scope** for this plan — soft end is sufficient if reads return null.

Purpose: Prevent orphaned lobbies from staying joinable forever (roadmap success criterion 6).
Output: `room-lifecycle.js`, tests, integrated `getRoomByPublicCode`.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (ROOM-08)
@.planning/phases/02-room-lobby/02-RESEARCH.md (Pitfall 5)

**Policy constants (locked for this plan):**
- `LOBBY_ABANDON_TTL_MS = 24 * 60 * 60 * 1000` (24 hours since `created_at` for `phase === 'lobby'`).
- After draft completes in later phases, setting `ended_at` / `phase` will use same read filter — Phase 3 must set `phase` to `review` then `ended`; this plan only guarantees lobby abandon + ended rows invisible to `getRoomByPublicCode`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: room-lifecycle.js pure helpers + spec</name>
  <read_first>src/lib/server/db/schema.js, .planning/phases/02-room-lobby/02-RESEARCH.md</read_first>
  <files>src/lib/server/room-lifecycle.js, src/lib/server/room-lifecycle.spec.js</files>
  <behavior>
    - `shouldHideRoomFromPublic(row)` true when `phase === 'ended'` OR `ended_at != null`.
    - `shouldAbandonLobby(row, now)` true when `phase === 'lobby'` and `now - created_at >= LOBBY_ABANDON_TTL_MS`.
  </behavior>
  <action>
    Create `src/lib/server/room-lifecycle.js` exporting:
    - `export const LOBBY_ABANDON_TTL_MS = 86400000`
    - `export function shouldHideRoomFromPublic(row, now)` — boolean
    - `export function shouldAbandonLobby(row, now)` — boolean
    Use `row.created_at` / `row.ended_at` / `row.phase` as Date or number (normalize in helper if Drizzle returns Date).

    Create `src/lib/server/room-lifecycle.spec.js` with table-driven tests: ended room hidden; fresh lobby not abandoned; lobby at 25h abandoned.
  </action>
  <acceptance_criteria>
    - `src/lib/server/room-lifecycle.js` exports `LOBBY_ABANDON_TTL_MS`.
    - `rg "shouldHideRoomFromPublic" src/lib/server/room-lifecycle.js` matches.
    - `npm run test:unit -- --run src/lib/server/room-lifecycle.spec.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run test:unit -- --run src/lib/server/room-lifecycle.spec.js</automated>
  </verify>
  <done>Time-based rules are tested in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into getRoomByPublicCode</name>
  <read_first>src/lib/server/rooms.js, src/lib/server/room-lifecycle.js</read_first>
  <files>src/lib/server/rooms.js</files>
  <action>
    At the start of `getRoomByPublicCode` implementation (after `parseRoomCode`):
    1. Fetch row by `public_code`.
    2. If no row, return null.
    3. If `shouldHideRoomFromPublic(row, now)` return **null** immediately.
    4. If `shouldAbandonLobby(row, now)`, run **single transaction**: `UPDATE room SET phase = 'ended', ended_at = NOW(), updated_at = NOW() WHERE id = row.id AND phase = 'lobby'`; then return **null** (treat as gone for joiners).
    5. Otherwise return row as today.

    Ensure `loadLobbySnapshot` / live stream loaders that use `getRoomByPublicCode` inherit behavior (no duplicate logic).
  </action>
  <acceptance_criteria>
    - `rg "shouldHideRoomFromPublic|shouldAbandonLobby" src/lib/server/rooms.js` matches.
    - `rg "room-lifecycle" src/lib/server/rooms.js` matches.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && npm run check</automated>
  </verify>
  <done>Ended and stale lobby rooms no longer resolve for public code lookup.</done>
</task>

<task type="auto">
  <name>Task 3: Document cancel + draft end hook for Phase 3</name>
  <read_first>src/lib/server/rooms.js, .planning/ROADMAP.md</read_first>
  <files>src/lib/server/rooms.js</files>
  <action>
    Add a short **JSDoc block** on `getRoomByPublicCode` or `room-lifecycle.js` module describing: **host `cancelRoom`** (02-04) sets `ended`; **future draft completion** (Phase 3) must set `phase` and `ended_at` so ROOM-08 join closure applies. No new runtime behavior required if cancel already sets `ended_at`.
  </action>
  <acceptance_criteria>
    - `rg "Phase 3|draft completion|cancelRoom" src/lib/server/room-lifecycle.js src/lib/server/rooms.js` matches in at least one file.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/robinvanderheijden/Documents/GitHub/draft-utility && rg "Phase 3|ended_at" src/lib/server/rooms.js src/lib/server/room-lifecycle.js</automated>
  </verify>
  <done>Future phases have an explicit contract comment for room closure.</done>
</task>

</tasks>

<verification>
`npm run test:unit -- --run --project server` passes.
</verification>

<success_criteria>
ROOM-08 satisfied for v1: ended/cancelled/abandoned lobby rooms stop resolving via `getRoomByPublicCode`; helpers are unit-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/02-room-lobby/02-06-SUMMARY.md`
</output>
