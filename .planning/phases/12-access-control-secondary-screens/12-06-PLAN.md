---
phase: 12-access-control-secondary-screens
plan: 06
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - src/lib/components/molecules/CyGuestGate.svelte
  - src/lib/components/molecules/CyGuestGate.svelte.spec.js
  - src/lib/components/molecules/LobbyHostBar.svelte
  - src/lib/components/molecules/LobbyHostBar.svelte.spec.js
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03]
user_setup: []

must_haves:
  truths:
    - "A blocked visitor sees the amber-flagged 403 card with the four-line readout, a Discord sign-in link, and a retry-as-guest button"
    - "The retry button reports its in-flight state and reports back when the room is still private, so it never looks dead"
    - "The Host Console's first section is an open_spectating switch reflecting the snapshot and calling back with the requested boolean"
    - "The switch is host-only, announces as a switch with a checked state, and is disabled once the draft has started"
  artifacts:
    - path: "src/lib/components/molecules/CyGuestGate.svelte"
      provides: "ACC-03 Guest Gate as a presentational component"
      min_lines: 45
    - path: "src/lib/components/molecules/CyGuestGate.svelte.spec.js"
      provides: "browser DOM spec for the gate including retry feedback states"
      min_lines: 60
    - path: "src/lib/components/molecules/LobbyHostBar.svelte"
      provides: "the open_spectating switch inside the Host Console modal"
      contains: "open_spectating"
  key_links:
    - from: "src/lib/components/molecules/CyGuestGate.svelte"
      to: "the login route with a redirect back to the draft"
      via: "the loginHref prop, built with resolve() by the page"
      pattern: "loginHref"
    - from: "src/lib/components/molecules/LobbyHostBar.svelte"
      to: "the page's setRoomVisibility caller"
      via: "onSetVisibility(next)"
      pattern: "onSetVisibility"
---

<objective>
Build the Guest Gate card (ACC-03, D-02/D-04) as a presentational component with its retry feedback states, and add the `open_spectating` switch (ACC-01/ACC-02, D-10) as the first section of the shipped Host Console modal.

Purpose: these are the two human-facing surfaces of access control — the one a blocked guest sees and the one the host uses to change it. Both are extracted or hosted in components that render in the browser vitest project, so ACC-01..03 get real DOM coverage.
Output: `CyGuestGate.svelte` plus spec, and an extension of `LobbyHostBar.svelte` plus its spec.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md
@.planning/phases/12-access-control-secondary-screens/12-PATTERNS.md
@.planning/phases/12-access-control-secondary-screens/12-RESEARCH.md
@.planning/phases/11-terminal-modals/11-CONTEXT.md
@.planning/phases/12-access-control-secondary-screens/12-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Prop contracts. Plan 08 wires these exact shapes. Do not change them. -->

CyGuestGate.svelte:
  { code: string,
    loginHref: string,
    onRetry: () => void | Promise<void>,
    checking?: boolean,
    stillGated?: boolean }
  The page builds `loginHref` with resolve() and owns the invalidateAll() mechanism.

LobbyHostBar.svelte — props gain:
  onSetVisibility: (next: boolean) => void
  and the LobbySnap typedef gains an OPTIONAL `isPublic?: boolean`
  (optional is mandatory: the existing spec fixtures at LobbyHostBar.svelte.spec.js:23-55
   build snapshots with only `phase` and `teams`, and a required field would make them type errors).
  Read it as `snapshot.isPublic === true`, never truthiness.

<!-- Consumed from Plan 02 (already shipped in src/app.css) -->
.cy-gate .cy-gate-card .cy-gate-eyebrow .cy-gate-readout .cy-gate-readout .cy-403
.cy-gate-actions .cy-gate-foot .cy-sr-only
.cy-hc-switch (+ :hover:not(:disabled), :focus-visible, :disabled) .cy-hc-switch-seg
.cy-hc-switch-seg.is-on .cy-hc-switch-sep .cy-hc-switch-state .cy-hc-switch-state.is-public
plus shipped: .cy-btn .cy-btn-ghost .cy-btn-discord .cy-foot .cy-hc-section .cy-hc-row .cy-field-label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CyGuestGate.svelte + browser spec (ACC-03)</name>
  <files>src/lib/components/molecules/CyGuestGate.svelte, src/lib/components/molecules/CyGuestGate.svelte.spec.js</files>
  <read_first>
    - src/lib/components/molecules/PauseOverlay.svelte — the whole file (49 lines): the shipped `.cy-*` card analog, one-line JSDoc `$props()`, `<pre>` with real newlines, zero `<style>` block, no `$live/*` import, `&gt;` for a literal leading `>`
    - src/lib/components/molecules/LoginCard.svelte lines 28-53 — the Discord CTA to reuse by COMPOSITION not markup: `class="cy-btn cy-btn-discord"`, the inline `aria-hidden="true"` SVG with its verbatim `d` path, the `{loading ? 'CONNECTING…' : 'CONTINUE_DISCORD()'}` label swap plus `disabled` + `aria-busy` triple (the exact precedent for the gate's `CHECKING…` state), and prettier's line-break-before-`>` shape for a long-`d` SVG. Also note the visible `role="alert"` at line 53.
    - src/routes/draft/[id]/+page.svelte line 259 — the EXISTING raw `<a href="/login?redirect=/draft/{code}">`. This is one of the pre-existing `svelte/no-navigation-without-resolve` eslint errors; do NOT copy it verbatim. The gate receives an already-resolved `loginHref` prop instead.
    - src/routes/login/+page.server.js line 8 — confirms the login route already honours `?redirect`
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § Component Contracts 2 (anatomy tree + aspect table), § Copywriting Contract → Guest Gate (every exact literal including the retry states), § Accessibility Contract, § State Matrix (the six Guest Gate rows)
    - .planning/phases/12-access-control-secondary-screens/12-RESEARCH.md § Design Source Verification → Guest Gate (the four readout lines and the explainer/foot copy) and § Architecture Patterns 4 and 5 (why the gate branch must read no stream value)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx lines 675-700 — `CYGuestGate`, the authoritative source
    - src/lib/components/molecules/LobbyHostBar.svelte.spec.js — the browser-spec analog: header imports including `import '../../../app.css'`, the `makeProps(overrides)` factory of `vi.fn()`s, `code: 'K7-MIRA'`, `expect.element(page.getByRole(...))`, `screen.rerender({...})` for prop-driven states, `document.querySelectorAll` for class-level assertions
    - src/app.css as written by Plan 02 (the `.cy-gate*` block, `.cy-403`, `.cy-sr-only`)
  </read_first>
  <action>
    Create `src/lib/components/molecules/CyGuestGate.svelte` and its browser spec.

    **Component.** Props: `/** @type {{ code: string, loginHref: string, onRetry: () => void | Promise<void>, checking?: boolean, stillGated?: boolean }} */ let { code, loginHref, onRetry, checking = false, stillGated = false } = $props();`

    Markup, per the UI-SPEC anatomy tree, in this order:
    - `.cy-gate` wrapper containing `<section class="cy-gate-card">` labelled by its own `<h2>` via `aria-labelledby` (give the h2 an id).
    - FIRST child: `<p class="cy-sr-only" role="alert">` reading `Access to room {code} is restricted. Sign in with Discord, or retry as guest.` The gate can arrive mid-session via D-11 live ejection, so a silent branch swap would be an AT defect.
    - `.cy-gate-eyebrow` reading `// access: RESTRICTED`.
    - `<h2>` reading `$ ROOM.ACCESS()`.
    - `<pre class="cy-gate-readout">` with exactly four lines:
      1. `> GET /draft/{code}`
      2. `< 403 SIGN_IN_REQUIRED` wrapped in `<span class="cy-403">`
      3. `> identity = <guest:anon>`
      4. `> hint: host has not opened this room to spectators`
      Use the interleaved template-literal form — one explicit `{`...`}` expression per text run with the `<span>` between them — so the newlines are immune to formatter reflow and to Svelte's leading-newline handling inside `<pre>`. Line 3 contains literal `<` and `>` characters: text-interpolate them so Svelte escapes them. NEVER `{@html}` — it would be a needless XSS surface next to displayName-adjacent data.
    - `<p>` explainer: `// sign in to join this draft as a player or captain — or wait for the host to start it, then spectate.`
    - `.cy-gate-actions` (column) containing, in order:
      - `<a class="cy-btn cy-btn-discord" href={loginHref}>` with the inline `aria-hidden="true"` Discord `<svg>` (copy the verbatim `d` path from `LoginCard.svelte`) and the label `SIGN_IN_DISCORD()`. An anchor, NOT a form: the login route already owns the `?/signin` OAuth action and already honours `?redirect`, and a cross-route form action was not verified. `.cy-btn-discord` already ships `width: 100%; justify-content: center; padding: 14px`, so the stacked full-width treatment needs no new CSS.
      - `<button type="button" class="cy-btn cy-btn-ghost">` calling `onRetry`, labelled `RETRY_AS_GUEST()` normally and `CHECKING…` while `checking`, with `disabled={checking}` and `aria-busy={checking}`. All three signals are mandatory: there is no global `.cy-btn:disabled` rule in `app.css` (a pre-existing gap, out of scope), so the label swap is what actually conveys the busy state.
    - When `stillGated`: `<p class="cy-foot" role="status">// still private — the host has not opened this room</p>`. A still-private room re-renders IDENTICALLY, so this feedback is mandatory rather than decorative — without it the button appears dead.
    - `<p class="cy-gate-foot">// you can still watch once the draft goes live</p>` as the last child.

    Structural constraint from RESEARCH Pattern 4/5 that this component enforces by construction: the gate reads NOTHING derived from the realtime stream — no `snapshot`, no `loading`, no `loadError`. That is what lets Svelte's `createSubscriber` tear down the WebSocket subscription and what makes `RETRY_AS_GUEST()` able to succeed at all. So: no `$live/*` import, and every value arrives as a prop.

    No `<style>` block. `role="status"` (polite) for the still-gated line; `role="alert"` (assertive) only for the arrival sentence.

    Run `svelte-autofixer` (Svelte MCP) on the file and resolve every issue and suggestion before finishing. Repeat until clean.

    **Spec** (`CyGuestGate.svelte.spec.js`, browser `client` project). Import `../../../app.css`. Use `code: 'K7-MIRA'`, a `loginHref` fixture such as `/login?redirect=%2Fdraft%2FK7-MIRA`, and a `vi.fn()` `onRetry`. Cover:
    - Eyebrow `// access: RESTRICTED` and heading `$ ROOM.ACCESS()` are present, and the section's accessible name resolves to the heading.
    - All four readout lines are present, with the code interpolated into `> GET /draft/K7-MIRA`.
    - The `403 SIGN_IN_REQUIRED` text sits inside an element with class `cy-403`.
    - The `> identity = <guest:anon>` line renders the literal `<guest:anon>` as TEXT — assert on `textContent`, and assert the readout element contains no child element created from that line (i.e. the angle brackets were escaped, not parsed as markup).
    - The explainer line and the `.cy-gate-foot` note are present with their exact copy.
    - `SIGN_IN_DISCORD()` is an `<a>` whose `href` equals the `loginHref` prop, carries `cy-btn-discord`, and contains an `svg` with `aria-hidden="true"`.
    - `RETRY_AS_GUEST()` click calls `onRetry` exactly once.
    - With `checking: true` (via `rerender`): the button label reads `CHECKING…`, it is disabled, and it carries `aria-busy="true"`; clicking it does not increase the `onRetry` call count.
    - With `stillGated: true`: a `[role="status"]` reading `// still private — the host has not opened this room` exists; with `stillGated: false` it does not.
    - Exactly one `[role="alert"]` exists and its text names the room code.
    Every `it` needs at least one assertion (`requireAssertions: true`). No `it.todo`.
  </action>
  <acceptance_criteria>
    - `test -f src/lib/components/molecules/CyGuestGate.svelte` and `grep -c "<style" src/lib/components/molecules/CyGuestGate.svelte` returns 0
    - `grep -c '\$live' src/lib/components/molecules/CyGuestGate.svelte` returns 0
    - `grep -c "{@html" src/lib/components/molecules/CyGuestGate.svelte` returns 0
    - `grep -c "invalidateAll\|goto(" src/lib/components/molecules/CyGuestGate.svelte` returns 0 (the page owns the mechanism; the component only calls `onRetry`)
    - `grep -F '// access: RESTRICTED' src/lib/components/molecules/CyGuestGate.svelte` matches
    - `grep -F '403 SIGN_IN_REQUIRED' src/lib/components/molecules/CyGuestGate.svelte` matches, together with `cy-403`
    - `grep -F 'host has not opened this room to spectators' src/lib/components/molecules/CyGuestGate.svelte` matches
    - `grep -F 'CHECKING…' src/lib/components/molecules/CyGuestGate.svelte` matches (U+2026)
    - `grep -F 'aria-busy' src/lib/components/molecules/CyGuestGate.svelte` matches
    - `grep -F 'you can still watch once the draft goes live' src/lib/components/molecules/CyGuestGate.svelte` matches
    - `npx vitest run src/lib/components/molecules/CyGuestGate.svelte.spec.js` passes with at least 10 tests and 0 todo
    - The spec proves the literal `<guest:anon>` renders as escaped text
    - `svelte-autofixer` reports zero issues and zero suggestions for `CyGuestGate.svelte`
    - `npx eslint src/lib/components/molecules/CyGuestGate.svelte` reports no `no-navigation-without-resolve` error (the href arrives pre-resolved as a prop)
    - `npm test` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/components/molecules/CyGuestGate.svelte.spec.js && npm test</automated>
  </verify>
  <done>`CyGuestGate.svelte` renders the amber-flagged 403 card with all four readout lines and the red `.cy-403` hook, a Discord anchor driven by a pre-resolved `loginHref`, a retry button with working `CHECKING…` and still-gated feedback, one screen-reader alert, zero stream reads, and at least 10 green browser assertions.</done>
</task>

<task type="auto">
  <name>Task 2: the open_spectating switch in the Host Console (ACC-01/ACC-02, D-10)</name>
  <files>src/lib/components/molecules/LobbyHostBar.svelte, src/lib/components/molecules/LobbyHostBar.svelte.spec.js</files>
  <read_first>
    - src/lib/components/molecules/LobbyHostBar.svelte — the whole file (193 lines). Specifically: the props typedef block at 5-34 (the JSDoc-inline-object `$props()` idiom, `LobbyMember` / `LobbySnap` / `ScriptTurn` typedefs, the `$bindable` pair); the `{#if isHost}` guard at 81 wrapping the whole `<section class="cy-host-panel">`; the lobby-phase launcher gate at 85; the `CyModal` invocation at 113 and the `.cy-modal-head` at 114-117 (the insertion point is immediately AFTER this head); the `move_player` `.cy-hc-section` at 119-145 (the frame to copy) and its `disabled={!moveTargetValid || snapshot.phase !== 'lobby'}` expression at 140 (the draft-started-lock precedent); the `EXEC` button metrics.
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § Component Contracts 4 — this contains the COMPLETE recommended markup block for the switch plus its full aspect table (placement, source of truth, callback, in-flight, draft-started lock, accent discipline, host-only, rejected alternatives). Treat it as the authority: PATTERNS § No Analog Found records that no toggle or switch exists anywhere in this codebase or the design handoff.
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § Copywriting Contract → Open-spectating control (field label, segment labels, state readout, both hints, accessible name, failure copy) and § Accessibility Contract rows "Switch" and "Label casing (not a defect)"
    - .planning/phases/12-access-control-secondary-screens/12-RESEARCH.md § Open Questions 2 (why no extra draft-started lock is needed beyond the existing launcher gate)
    - .planning/phases/12-access-control-secondary-screens/12-PATTERNS.md § 11 (the `move_player` section and props typedef quoted verbatim, plus the mandatory-optional rule for `isPublic`)
    - src/lib/components/molecules/LobbyHostBar.svelte.spec.js — the whole file (246 lines): the `makeProps` factory at 10-71 and the snapshot fixtures at 23-55 (which carry ONLY `phase` and `teams`), the `openConsole()` helper, the `move_player` EXEC test at 145-158, the class-level assertion idiom at 215-220
    - src/app.css as written by Plan 02 (the nine `.cy-hc-switch*` rules) and the shipped `.cy-hc-section` / `.cy-hc-row` / `.cy-field-label` rules
  </read_first>
  <action>
    Add the `open_spectating` switch to `src/lib/components/molecules/LobbyHostBar.svelte`, then extend its spec.

    **Props.** Add `onSetVisibility: (next: boolean) => void` to the `$props()` JSDoc type block and to the destructure, joining the existing `onKick` / `onMove` / `onStartDraft` / `onCancelRoom` callback run. Add `isPublic?: boolean` to the `LobbySnap` typedef — it MUST be optional, because the existing spec fixtures at `LobbyHostBar.svelte.spec.js:23-55` build snapshots with only `phase` and `teams` and a required property would turn them into type errors.

    Derive the display state as `snapshot.isPublic === true`, never truthiness — an absent field must render as private rather than break the strict-comparison discipline. Add a local `pending` `$state` boolean for the in-flight state.

    **Markup.** Insert a new `.cy-hc-section` as the FIRST section of the Host Console modal body, immediately after `.cy-modal-head` (line 114-117) and ABOVE the `move_player` section. Room-level scope reads top-down (room, then roster), and a fixed-height row above the variable-length kick list keeps the roster tools from shifting. Transcribe the markup from UI-SPEC § Component Contracts 4, which specifies:
    - `<span class="cy-field-label" id="cy-hc-visibility-label">open_spectating</span>` — snake_case, matching the shipped `turn_timer` / `move_player` / `kick` labels.
    - A `.cy-hc-row` containing the switch button and the state readout.
    - `<button type="button" class="cy-hc-switch" role="switch" aria-checked={isPublic} aria-labelledby="cy-hc-visibility-label" disabled={snapshot.phase !== 'lobby' || pending} aria-busy={pending} onclick={...}>`. `aria-checked` is MANDATORY — Svelte's compiler enforces it for `role="switch"` (`a11y_role_has_required_aria_props`). `aria-labelledby`, not `aria-label`, so the accessible name IS the visible `open_spectating` text (WCAG 2.5.3). Note `.cy-field-label` applies `text-transform: uppercase`, so it RENDERS as `OPEN_SPECTATING` while `aria-labelledby` resolves the lowercase DOM text — Label-in-Name matching is case-insensitive and the shipped `move_player` / `kick` labels already behave this way. Do NOT "fix" this by hardcoding an uppercase `aria-label`.
    - Three inner spans: `<span class="cy-hc-switch-seg" class:is-on={!isPublic}>OFF</span>`, `<span class="cy-hc-switch-sep" aria-hidden="true">|</span>`, `<span class="cy-hc-switch-seg" class:is-on={isPublic}>ON</span>`.
    - `<span class="cy-hc-switch-state" class:is-public={isPublic} aria-hidden="true">room = {isPublic ? 'PUBLIC' : 'PRIVATE'}</span>` — `aria-hidden` because it is redundant with `aria-checked`.
    - `<p class="cy-foot">` carrying `// anyone with the link can spectate the lobby` when public and `// guests are blocked until you start the draft` when private.

    **Click handler.** Set `pending = true`, call `onSetVisibility(!isPublic)`, and clear `pending` when the call settles. The switch has NO optimistic local state — `snapshot.isPublic` is the only source of truth (D-13), because the RPC's broadcast is what flips the visual, so the switch can never show a state the server does not hold. While `pending`, the label does NOT change (the broadcast supplies the new state); `disabled` + `aria-busy` prevent a double-fire.

    **Accent discipline.** The active segment uses the NEUTRAL inversion for both states (`background: var(--cy-text); color: var(--cy-bg)` from `.cy-chat-tabs button.is-active`) — already in the CSS from Plan 02. Lime appears only on the `room = PUBLIC` readout. Private is never red: "private" is the safe default (ACC-01), not an error.

    **Host-only.** The whole section sits inside the existing `{#if isHost}` guard at line 81, so it inherits host gating for free. `{#if isHost}` is UX; the server-side `host_user_id` comparison in `setRoomVisibility` (Plan 03) is the real boundary.

    Do NOT add a `<style>` block. Do NOT change `CyModal`'s invocation, the `move_player` / `kick` sections, `startDisabled`, `removableMembers`, or any other frozen Phase 11 behaviour.

    Run `svelte-autofixer` (Svelte MCP) on the file and resolve every issue and suggestion before finishing. Repeat until clean.

    **Spec extension.** Add `onSetVisibility: vi.fn()` to the `makeProps` factory. Do NOT add `isPublic` to the shared snapshot fixtures — leaving them without it proves the absent-field-renders-private path. Cover:
    - With a snapshot lacking `isPublic`: the switch exists inside the console, `aria-checked` is `"false"`, the `.cy-hc-switch-state` reads `room = PRIVATE` and lacks `is-public`, and the private hint copy is shown.
    - With `snapshot.isPublic: true`: `aria-checked` is `"true"`, the state readout reads `room = PUBLIC` and carries `is-public`, the `ON` segment carries `is-on` while `OFF` does not, and the public hint copy is shown.
    - Clicking the switch when private calls `onSetVisibility` exactly once with `true`; clicking when public calls it exactly once with `false`.
    - The switch is queryable via `page.getByRole('switch', { name: 'open_spectating' })` — proving the `aria-labelledby` wiring resolves.
    - The `|` separator carries `aria-hidden="true"`.
    - With `snapshot.phase: 'drafting'`: the switch is disabled (the draft-started lock, matching the `EXEC` precedent).
    - With `isHost: false`: no `role="switch"` element exists anywhere (host-only).
    - The switch section appears BEFORE the `move_player` section in DOM order — assert with `compareDocumentPosition` or by index within the modal body's children.
    - Every pre-existing test in the file still passes unmodified.
    No `it.todo`. Every `it` needs at least one assertion.
  </action>
  <acceptance_criteria>
    - `grep -F 'open_spectating' src/lib/components/molecules/LobbyHostBar.svelte` matches
    - `grep -F 'role="switch"' src/lib/components/molecules/LobbyHostBar.svelte` matches, together with `aria-checked` and `aria-labelledby="cy-hc-visibility-label"`
    - `grep -F 'onSetVisibility' src/lib/components/molecules/LobbyHostBar.svelte` matches at least 3 times (typedef, destructure, call)
    - `grep -F 'isPublic?: boolean' src/lib/components/molecules/LobbyHostBar.svelte` matches (OPTIONAL in the LobbySnap typedef)
    - `grep -F 'snapshot.isPublic === true' src/lib/components/molecules/LobbyHostBar.svelte` matches (strict comparison, not truthiness)
    - The switch element uses `aria-labelledby`, not `aria-label` — inspect the `role="switch"` element and confirm no `aria-label` attribute is present on it
    - `grep -c "<style" src/lib/components/molecules/LobbyHostBar.svelte` returns 0
    - `grep -F 'guests are blocked until you start the draft' src/lib/components/molecules/LobbyHostBar.svelte` and `grep -F 'anyone with the link can spectate the lobby'` both match
    - In `src/lib/components/molecules/LobbyHostBar.svelte`, the `open_spectating` line number is LOWER than the `move_player` line number (verify with `grep -n "open_spectating\|move_player"`)
    - `grep -c "cy-hc-switch" src/lib/components/molecules/LobbyHostBar.svelte` returns at least 6
    - `npx vitest run src/lib/components/molecules/LobbyHostBar.svelte.spec.js` passes with at least 8 new tests, 0 todo, and every pre-existing test still green
    - The spec contains a `getByRole('switch', { name: 'open_spectating' })` query
    - `svelte-autofixer` reports zero issues and zero suggestions for `LobbyHostBar.svelte`
    - `npm test` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/components/molecules/LobbyHostBar.svelte.spec.js && npm test</automated>
  </verify>
  <done>The Host Console modal opens with `open_spectating` as its first section: a `role="switch"` two-segment terminal control named by the visible label, reading `snapshot.isPublic === true`, calling `onSetVisibility(!isPublic)`, disabled outside the lobby phase and while pending, neutral for both states with lime only on the `room = PUBLIC` readout, host-only, and proven by at least 8 green browser assertions with every Phase 11 test still passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → rendered DOM | `code` and `loginHref` are interpolated into a `<pre>` block and an `href` |
| host UI → the visibility RPC | The switch is the only UI path to `setRoomVisibility`, but not the only path — the RPC is directly callable over the socket |
| component → navigation | The gate's Discord anchor leaves the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Tampering (XSS) | the four-line gate readout, which contains a literal `<guest:anon>` | mitigate | Plain `{expr}` text interpolation, which auto-escapes. `{@html}` is forbidden and grep-asserted at zero, and the spec asserts the angle brackets render as text rather than being parsed as markup |
| T-12-29 | Elevation of privilege | `{#if isHost}` around the switch | accept (documented) | This is UX only. The enforcement point is the server-side `roomRow.host_user_id !== ctx.user.id` comparison in `setRoomVisibility` (Plan 03, T-12-03). Recorded so no reviewer treats the Svelte guard as the boundary |
| T-12-30 | Tampering (state desync) | switch showing a visibility the server does not hold | mitigate | No optimistic local state. `snapshot.isPublic === true` is the sole source of truth; only the RPC's broadcast flips the visual, and `pending` blocks a double-fire while a call is in flight |
| T-12-23 | Tampering | absent `isPublic` interpreted as public | mitigate | Strict `=== true`, so a snapshot lacking the field renders private (fail-closed). The spec deliberately keeps the shared fixtures without the field to exercise this path |
| T-12-31 | Information disclosure (a11y) | live arrival of the gate after a D-11 ejection | mitigate | `p.cy-sr-only[role="alert"]` announces the restriction; `role="status"` (polite) is used for the retry feedback so the two do not compete |
| T-12-06b | Information disclosure | the gate branch keeping a WebSocket subscription open | mitigate | The component takes only plain props and imports no `$live/*`, so the gate branch reads nothing stream-derived. That structural property is what lets `createSubscriber` tear the subscription down (the page-side half is Plan 08, T-12-06) |
| T-12-28 | Tampering (open redirect / lint regression) | the Discord anchor | mitigate | `href` arrives as a pre-resolved `loginHref` prop built with `resolve()` by the page; the component constructs no URL. Verified with a targeted eslint run against `svelte/no-navigation-without-resolve` |
| T-12-32 | Denial of service (double-submit) | rapid switch clicks | mitigate | `disabled={... || pending}` plus `aria-busy={pending}` prevents a second in-flight RPC |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (`12-RESEARCH.md` § Package Legitimacy Audit: not applicable, verified against `package.json`). No install task exists in any Phase 12 plan |
</threat_model>

<verification>
- `npm test` green (both projects). Do NOT gate on `npm run lint` or `npm run check` — both red at baseline. Never run `npm run format`.
- Run `svelte-autofixer` (Svelte MCP) on both `.svelte` files until zero issues and zero suggestions. Use `list-sections` + `get-documentation` for any `role="switch"`, `<pre>` whitespace, or runes question.
- Do NOT use `playground-link` for code written into the project.
- Every pre-existing `LobbyHostBar.svelte.spec.js` test must pass unmodified — Phase 11's MOD-02 behaviour is frozen.
- Optionally compare `npx eslint src` against the 20-error baseline; never as a pass/fail gate.
- Intermediate-state note: `onSetVisibility` has no caller until Plan 08 wires it. Give it a required prop type anyway; the spec supplies a `vi.fn()`.
</verification>

<success_criteria>
- `CyGuestGate.svelte` renders the ACC-03 card verbatim per UI-SPEC § Copywriting, with working `CHECKING…` and still-gated feedback, and reads no stream-derived value.
- The Host Console's first section is a `role="switch"` `open_spectating` control reflecting `snapshot.isPublic === true`, calling `onSetVisibility(next)`, disabled outside the lobby, neutral for both states.
- Both files are `svelte-autofixer`-clean and add no new eslint errors.
- Suite green with every Phase 11 test intact.
</success_criteria>

<output>
Create `.planning/phases/12-access-control-secondary-screens/12-06-SUMMARY.md` when done. Record: the final `CyGuestGate` prop signature, the switch's final markup shape, any `svelte-autofixer` change it forced (especially around `role="switch"`), and the new client-project test count.
</output>
