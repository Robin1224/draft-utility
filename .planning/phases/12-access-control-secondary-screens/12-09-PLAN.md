---
phase: 12-access-control-secondary-screens
plan: 09
type: execute
wave: 5
depends_on: [12-02, 12-08]
files_modified:
  - src/phase12-access-screens.spec.js
  - .planning/phases/12-access-control-secondary-screens/12-VALIDATION.md
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03, ACC-04, SCR-01, SCR-02]
user_setup: []

must_haves:
  truths:
    - "The load-bearing structural properties of the page and layout are locked by source contracts, so a future refactor cannot silently reopen the subscription leak or reorder the branches"
    - "The whole suite is green with no regression against the 202-test baseline, and the eslint src error count has not grown past 20"
    - "Every new and modified Svelte file has been through svelte-autofixer"
    - "Every row of the phase's Per-Task Verification Map has been run and marked green, while the status legend and the Approval note keep their literal `⬜ pending` text"
  artifacts:
    - path: "src/phase12-access-screens.spec.js"
      provides: "CSS contract (Plan 02) plus the page/layout/DraftBoard source contracts"
      min_lines: 110
    - path: ".planning/phases/12-access-control-secondary-screens/12-VALIDATION.md"
      provides: "Per-Task Verification Map with every row observed green"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "src/phase12-access-screens.spec.js"
      to: "src/routes/draft/[id]/+page.svelte"
      via: "regex source contracts on branch order and the gate derive"
      pattern: "page\\.svelte"
    - from: "src/phase12-access-screens.spec.js"
      to: "src/routes/+layout.svelte"
      via: "source contract on the gated subscription read"
      pattern: "page\\.data\\.gated"
---

<objective>
Lock the structural invariants that no browser spec can reach — branch order, the gate branch's zero stream reads, the layout subscription gate, the removed drafting-condition `'cancelled'`, and the deleted DraftBoard branch — then run the phase-level regression sweep and mark every row of `12-VALIDATION.md`'s verification map green.

Purpose: the composition root cannot be rendered in the browser project because it imports `$live/*` directly (`src/phase11-fixes.spec.js:4-6` documents this), so the properties that make access control actually hold are only assertable as source contracts. Without them, a well-meaning refactor reopens the refcount leak or flashes ESTABLISHING_LINK over a dead room, and nothing goes red.
Output: an extended `src/phase12-access-screens.spec.js` and a fully green `12-VALIDATION.md` map.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-access-control-secondary-screens/12-VALIDATION.md
@.planning/phases/12-access-control-secondary-screens/12-RESEARCH.md
@.planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md
@.planning/phases/12-access-control-secondary-screens/12-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source contracts for the page, layout and DraftBoard</name>
  <files>src/phase12-access-screens.spec.js</files>
  <read_first>
    - src/phase12-access-screens.spec.js — as created by Plan 02 (the CSS contract half). Reuse its `read()` helper; add new describes rather than restructuring what is there.
    - src/phase11-fixes.spec.js — the whole file (26 lines). This is the exact technique: `const pageSrc = read('src/routes/draft/[id]/+page.svelte')` then `expect(pageSrc).toMatch(/regex/)` and `expect(pageSrc).toContain('literal')`. Its header comment states WHY the draft page cannot be rendered. Note it uses a `/** @param {string} p */`-annotated `read` helper, which is the preferred form (phase10-screens.spec.js's un-annotated version is one of the 10 svelte-check baseline errors).
    - src/routes/draft/[id]/+page.svelte — as shipped by Plan 08. Read the whole file so the contract regexes match the actual text, not the plan's paraphrase.
    - src/routes/+layout.svelte — as shipped by Plan 08 Task 3 (25 lines).
    - src/lib/components/molecules/DraftBoard.svelte — as shipped by Plan 08 Task 2.
    - .planning/phases/12-access-control-secondary-screens/12-RESEARCH.md § Validation Architecture → "Phase Requirements → Test Map" (the four rows whose Test Type is "source contract") and § Architecture Patterns 4 and 5
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § Component Contracts 5 (the branch list and its bullets)
    - vite.config.js line 19 — `expect: { requireAssertions: true }`
  </read_first>
  <action>
    Extend `src/phase12-access-screens.spec.js` with new describes carrying the source contracts. Read each source file once via the existing `read()` helper. Do not modify or reorder the CSS-contract describes from Plan 02.

    **Guard against self-invalidating greps.** Several of these contracts assert that a string is ABSENT. Because comments count, an assertion like "the file no longer contains `cancelled`" can be defeated or falsely broken by a comment mentioning the word. For every absence assertion, strip comment lines before counting — filter out lines whose trimmed form starts with `//`, `/*`, `*` or `<!--` — and assert against the stripped text. Do not write a bare `not.toContain` on a token that also appears in prose.

    Contracts on `src/routes/draft/[id]/+page.svelte` (SCR-01, SCR-02, ACC-03, ACC-04):
    - Branch order: assert `{#if gated}` appears, and that its index in the source is LOWER than the index of the cancelled branch, which is lower than the index of `showConnecting`, which is lower than the index of the `loadError` branch. Assert this with explicit `indexOf` comparisons rather than one giant regex, so a failure message names which pair is out of order.
    - Gate-first / no stream reads: assert the substring between `{#if gated}` and the next `{:else if` contains `CyGuestGate` and contains NONE of `snapshot`, `streamVal`, `loading`, `loadError`. This is the property that lets `createSubscriber` tear down the subscription.
    - The three-source gate derive: assert the source contains `data.gated`, `loadError.code === 'FORBIDDEN'`, and `snapshot.isPublic === false`; and assert (comment-stripped) that `!snapshot.isPublic` does NOT appear.
    - `showConnecting` excludes `loadError`: assert the source contains a `showConnecting` derive whose expression includes `!loadError`. Rendering Connecting for `loadError` would fight the frozen PauseOverlay grace flow (D-06).
    - D-09: assert (comment-stripped) that `// loading room…` is gone.
    - D-15: assert `snapshot.phase === 'cancelled'` appears exactly once in the comment-stripped source, and that `phase === 'drafting' || snapshot.phase === 'cancelled'` does not appear.
    - D-16: assert the `CyCancelled` invocation passes `players` derived from `teams.A.length` plus `teams.B.length` and `spectators` from `spectators.length`.
    - D-18/D-19: assert `snapshot.cancelReason ?? 'grace'` appears (the safe default).
    - D-05/D-08: assert `CONNECT_FLOOR_MS` and `CONNECT_TIMEOUT_MS` are declared, and that the floor value is within the D-05 authorized 600–900ms range (parse the number and assert the bound, rather than hardcoding 900, so a later tuning inside the range does not go red).
    - D-20 client half: assert `if (gated) return;` appears, and that its index is LOWER than the index of `const store = activeChatStream(code);`.
    - D-11 latched ejection promotion (the subscription-release mechanism, 12-08-T1(e)): assert `ejectionPromoted` appears; assert the index of `ejectionPromoted = true` is LOWER than the index of the `invalidateAll()` call that follows it (an unlatched or late-latched call is an infinite invalidation loop); and assert the source slice from `if (ejectionPromoted) return;` up to that `invalidateAll()` contains `data.gated`, `isGuest`, `snapshot` and `snapshot.isPublic === false`, i.e. the call site is reachable only from the snapshot-sourced ejection branch and never from the already-gated first-paint path.
    - ACC-02: assert `setRoomVisibility(code` appears and that `onSetVisibility=` is passed to `LobbyHostBar`.
    - D-04: assert `invalidateAll()` appears, and that `resolve('/login')` appears (no raw login href is introduced by this phase).

    Contracts on `src/routes/+layout.svelte` (ACC-03, D-19):
    - Assert `!page.data.gated` appears exactly once.
    - Assert `fromStore(lobby(` appears exactly ONCE in the file — a second subscriber would re-open the refcount leak.
    - Assert the same source line contains both `code` and `page.data.gated`, i.e. the gate is on the subscription read itself and not a separate unused derive.
    - Assert `src/routes/draft/[id]/+layout.svelte` does NOT exist on disk (use `existsSync`), because creating one would add a second subscriber to the same refcounted store.

    Contracts on `src/lib/components/molecules/DraftBoard.svelte` (SCR-02, Pitfall 10):
    - Assert `cy-draft-cancelled` and `cancelledTeam` are both absent from the comment-stripped source.
    - Assert `snapshot.phase !== 'cancelled'` IS still present — that is the frozen PauseOverlay grace clause, and deleting it would break D-06.

    Every `it` needs at least one assertion. No `it.todo`. Prefer several small `it`s with descriptive names over one omnibus test, so a failure identifies the violated invariant. Tabs, single quotes, printWidth 100, JSDoc on any new helper. Never run `npm run format`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/phase12-access-screens.spec.js` passes with at least 21 tests total (the Plan 02 CSS contracts plus at least 14 new source contracts) and 0 todo
    - `grep -c "it.todo" src/phase12-access-screens.spec.js` returns 0
    - The spec reads all three of `src/routes/draft/[id]/+page.svelte`, `src/routes/+layout.svelte` and `src/lib/components/molecules/DraftBoard.svelte`
    - The spec contains an `existsSync` assertion that `src/routes/draft/[id]/+layout.svelte` is absent
    - The spec asserts `fromStore(lobby(` occurs exactly once in `src/routes/+layout.svelte`
    - The spec strips comment lines before every absence assertion — verify no bare `not.toContain` is applied to unfiltered source for `// loading room…`, `!snapshot.isPublic`, `cancelledTeam` or `cy-draft-cancelled`
    - Deliberate failure check: temporarily reorder two branches in a scratch copy (do NOT commit it) and confirm the branch-order test fails with a message naming the offending pair; then restore
    - `npm test` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/phase12-access-screens.spec.js && npm test</automated>
  </verify>
  <done>`src/phase12-access-screens.spec.js` locks branch order, the gate branch's zero stream reads, the three-source derive with its strict comparison, `showConnecting`'s exclusion of `loadError`, the removed placeholder and drafting-condition `'cancelled'`, the D-20 statement order, the latched ejection promotion with its guard ordering, the single gated layout subscription, the absent route-level layout, and the DraftBoard deletions — all with comment-stripped absence checks and a verified-failing branch-order test.</done>
</task>

<task type="auto">
  <name>Task 2: Phase regression sweep and verification-map close-out</name>
  <files>.planning/phases/12-access-control-secondary-screens/12-VALIDATION.md</files>
  <read_first>
    - .planning/phases/12-access-control-secondary-screens/12-VALIDATION.md — the whole file. The `## Per-Task Verification Map` is ALREADY POPULATED by the planner with real `12-0N-T{n}` task IDs, and the frontmatter already reads `status: ready` / `nyquist_compliant: true` / `wave_0_complete: true`. Your job is to RUN every command in the map and flip each row's `Status` from `⬜ pending` to `✅ green`. Do NOT restructure the table, do NOT change the frontmatter values, and do NOT touch `## Manual-Only Verifications`.
    - Every `12-0N-SUMMARY.md` present in the phase directory — for the recorded test counts and any deviation from the planned command
    - .planning/phases/12-access-control-secondary-screens/12-CONTEXT.md `<deferred>` — the note that ROADMAP success criterion 5's "130 tests" figure is stale and must be read as "the suite stays green"
  </read_first>
  <action>
    (a) **Run every Automated Command in the verification map**, in map order, and record the result of each. These are the exact invocations already written into the table, including the quoted bracketed path for `page.server.spec.js`. Any row whose command does not pass is a phase blocker: report it rather than editing the row to something weaker.

    (b) **Regression sweep.** Run and record:
    - `npm test` — must be green across both projects with at least 202 passing. Report the exact final counts: files, passed, skipped, todo, and the per-project split. The "130 existing unit tests" in ROADMAP criterion 5 and REQUIREMENTS.md line 6 is the stale v1.0 figure; 202 is the measured baseline. Plan 02 T1 removed exactly one test and every other task added tests, so the final total must exceed 202.
    - `npx eslint src` — record the error count and compare against the 20-error baseline. This is a REPORT, never a pass/fail gate. If it exceeds 20, identify each new error and fix it; the likely causes are a missing `resolve()` on a new anchor or an unused import. If it is 19 because `DraftBoard.svelte`'s `goto` import was removed, note the improvement.
    - Do NOT run `npm run lint` or `npm run check` as gates — both are red at baseline (19 prettier failures across `src/`, 10 svelte-check errors in 4 files). Do NOT run `npm run format` under any circumstances: `rooms.js`, `room.js`, `app.css` and `rooms.spec.js` all fail `prettier --check` today and reformatting them would bury the entire phase diff.
    - `svelte-autofixer` (Svelte MCP) on every `.svelte` file created or modified this phase: `CyConnecting.svelte`, `CyGuestGate.svelte`, `CyCancelled.svelte`, `LobbyHostBar.svelte`, `DraftBoard.svelte`, `src/routes/draft/[id]/+page.svelte`, `src/routes/+layout.svelte`. Confirm each returns zero issues and zero suggestions; if any still reports something, fix it and re-run until clean. CLAUDE.md mandates this and UI-SPEC records it as an executor obligation.
    - Snapshot-shape check: confirm the published lobby snapshot gained ONLY `isPublic` and `cancelReason` and no existing key changed name, type or meaning. `grep` the return object in `src/lib/server/rooms.js` `loadLobbySnapshot` against the six original keys (`publicCode`, `roomId`, `phase`, `hostUserId`, `teams`, `spectators`) and confirm all six are still present and unrenamed. This is the milestone's frozen-shape constraint.
    - `git status` — confirm no stray artifacts, particularly no drizzle scratch files and no committed `build/` churn. Note that `src/live/$types.d.ts` is expected to have been regenerated by the svelte-realtime Vite plugin once `setRoomVisibility` landed; that diff is legitimate.

    (c) **Close out the map.** Flip each row's `Status` cell from `⬜ pending` to `✅ green` for every command you observed passing. Scope this to the `## Per-Task Verification Map` table ONLY. Two other occurrences of the literal `⬜ pending` live outside that table and MUST survive verbatim: the status legend line (`*Status: ⬜ pending · ✅ green · ...*`, ~line 90) and the `**Approval:**` note (~line 143) that states rows stay pending until each task runs. Do not edit either. Every check below is therefore written against the table-row form `| ⬜ pending |`, never the bare phrase. Update the `Baseline` and `Expected end state` rows in `## Test Infrastructure` only if the observed final counts differ from what is recorded there. Append one line under `**Approval:**` recording the observed final `npm test` counts and the `npx eslint src` error count, dated.

    Leave everything else in the file as the planner wrote it: the frontmatter values, the table structure and task IDs, the sampling-continuity note, the `## Wave 0 Requirements` list (tick its boxes for items that shipped), the `## Validation Sign-Off` checklist, and all four `## Manual-Only Verifications` rows — those are correctly deferred to `/gsd:verify-work` human UAT, consistent with Phases 9-11.

    Do NOT edit `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md` checkboxes here — requirement and phase completion bookkeeping belongs to `/gsd:verify-work` and milestone completion.
  </action>
  <acceptance_criteria>
    - `npm test` is green with at least 202 passing tests; the exact per-project counts are recorded in the summary
    - `npx eslint src` reports at most 20 errors, and the count is recorded in the summary
    - `grep -c "| ⬜ pending |" .planning/phases/12-access-control-secondary-screens/12-VALIDATION.md` returns 0 (every table row observed and flipped). Do NOT gate on a bare `grep -c "⬜ pending"` — the status legend and the `**Approval:**` note both retain that literal by design and must not be edited, so a bare count can never reach 0
    - `grep -c "✅ green" .planning/phases/12-access-control-secondary-screens/12-VALIDATION.md` returns at least 30 (every `Status` cell plus the pre-existing `File Exists` marks)
    - The frontmatter still reads `status: ready`, `nyquist_compliant: true`, `wave_0_complete: true` — unchanged
    - `grep -c "12-XX-TN" .planning/phases/12-access-control-secondary-screens/12-VALIDATION.md` returns 0 (it already did; confirm the planner's table was not reverted to a stub)
    - Every one of ACC-01, ACC-02, ACC-03, ACC-04, SCR-01 and SCR-02 still appears in at least one map row
    - The `## Manual-Only Verifications` section still has its four original rows
    - `loadLobbySnapshot`'s return object still contains all six original keys, unrenamed
    - `git status` shows no untracked build or drizzle scratch artifacts
    - `svelte-autofixer` returns zero issues and zero suggestions for all seven touched `.svelte` files
  </acceptance_criteria>
  <verify>
    <automated>npm test && node -e "const v=require('fs').readFileSync('.planning/phases/12-access-control-secondary-screens/12-VALIDATION.md','utf8');const need=['nyquist_compliant: true','status: ready','wave_0_complete: true'];const miss=need.filter(s=>!v.includes(s));if(miss.length){console.error('missing',miss);process.exit(1)}if(v.includes('12-XX-TN')){console.error('stub task ids reappeared');process.exit(1)}if(/\|\s*⬜ pending\s*\|/.test(v)){console.error('map rows still pending');process.exit(1)}for(const r of ['ACC-01','ACC-02','ACC-03','ACC-04','SCR-01','SCR-02'])if(!v.includes(r)){console.error('requirement missing from map:',r);process.exit(1)}const s=require('fs').readFileSync('src/lib/server/rooms.js','utf8');for(const k of ['publicCode','roomId','hostUserId','spectators'])if(!s.includes(k)){console.error('frozen snapshot key missing:',k);process.exit(1)}console.log('phase close-out OK')"</automated>
  </verify>
  <done>Every Automated Command in the verification map has been run and its row flipped to `✅ green`, `npm test` is green above the 202-test baseline, `npx eslint src` is at or below 20 errors, all seven touched `.svelte` files are `svelte-autofixer`-clean, the frozen snapshot keys are intact, and the map's structure, frontmatter and manual-only rows are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source text → structural invariant | The properties that keep the WebSocket subscription closed for a gated client are not expressible as runtime assertions in this harness; the source contract is the only enforcement against regression |
| test assertion → its own subject | A grep-based contract that counts comment lines can be silently defeated by prose, making the guard itself the weakest link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-36 | Information disclosure (regression) | a future refactor reintroducing a stream read into the gate branch, or a second `fromStore(lobby(...))` reader | mitigate | Source contracts assert the gate branch contains none of `snapshot` / `streamVal` / `loading` / `loadError`, that `fromStore(lobby(` occurs exactly once in the root layout, and that no route-level draft layout exists on disk. These are the only automatable guards for a file that cannot be rendered in the browser project |
| T-12-38 | Repudiation (regression) | a future change reordering the branches so Connecting swallows the grace flow or flashes over a dead room | mitigate | Explicit `indexOf` ordering assertions per adjacent pair, with a deliberately induced failure verified during the task so the test is known to actually fail when violated |
| T-12-39 | Tampering (self-invalidating test) | absence assertions defeated or falsely broken by comment prose | mitigate | Comment lines are stripped before every absence count; bare `not.toContain` on an unfiltered file is forbidden by an acceptance criterion |
| T-12-40 | Tampering (masked regression) | using a red-at-baseline command as a gate, or running a repo-wide formatter | mitigate | `npm test` is the only pass/fail gate. `npm run lint` and `npm run check` are report-only against their known baselines, and `npm run format` is explicitly forbidden because it would rewrite four files this phase edits and bury the diff |
| T-12-41 | Tampering (frozen-shape violation) | the additive snapshot fields turning into a breaking change | mitigate | The close-out asserts all six original `loadLobbySnapshot` keys are still present and unrenamed, so `isPublic` and `cancelReason` are provably additive — the milestone's frozen-shape constraint |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (`12-RESEARCH.md` § Package Legitimacy Audit: not applicable, verified against `package.json`). No install task exists in any Phase 12 plan, so the slopcheck gate is vacuous |
</threat_model>

<verification>
- `npm test` is the only green gate — baseline 202 passed / 1 skipped / 34 todo across 24 files (server 150, client 52). Plan 02 removes one test (the Phase 10 scope-guard `it`) and every other plan adds tests, so the final count must exceed 202.
- `npm run lint` and `npm run check` are red at baseline and must never be used as gates. `npm run format` must never be run.
- `npx eslint src` is a report compared against 20 errors.
- Manual-only verifications stay deferred to `/gsd:verify-work`: two-browser live ejection on flip-to-private, reduced-motion appearance of the dots and meter, the real-socket feel of the 900ms floor, and the accepted 404-on-refresh after cancellation.
</verification>

<success_criteria>
- Branch order, the gate branch's zero stream reads, the strict `isPublic === false` comparison, `showConnecting`'s exclusion of `loadError`, the D-20 statement order, the single gated layout subscription, the absent route-level layout, and the DraftBoard deletions are all locked by passing source contracts.
- Every absence assertion is comment-stripped, and the branch-order test is verified to fail when violated.
- The suite is green above the 202-test baseline; eslint `src` is at or below 20 errors; all seven touched `.svelte` files are `svelte-autofixer`-clean.
- Every row of `12-VALIDATION.md`'s Per-Task Verification Map is observed green, with the frozen snapshot keys proven intact.
</success_criteria>

<output>
Create `.planning/phases/12-access-control-secondary-screens/12-09-SUMMARY.md` when done. Record: the final `npm test` counts per project, the `npx eslint src` error count versus the 20-error baseline, the number of source contracts added, the outcome of the deliberate branch-order failure check, any map row whose command needed correcting, and any `svelte-autofixer` fix that was still outstanding when this plan ran.
</output>
