---
phase: 12-access-control-secondary-screens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.css
  - src/phase10-screens.spec.js
  - src/phase12-access-screens.spec.js
autonomous: true
requirements: [SCR-01, SCR-02, ACC-01, ACC-02, ACC-03]
user_setup: []

must_haves:
  truths:
    - "The three Phase 12 card stylesheets exist under .cy-app with the verbatim cyber.css values"
    - "Both new keyframes (cy-dots, cy-fill) exist and are suppressed under prefers-reduced-motion, with the dots pinned to a readable static value"
    - "A visually-hidden utility class exists so the gate and cancelled screens can carry a screen-reader alert"
    - "The open_spectating switch has a full style vocabulary derived from the shipped Phase 11 micro-controls"
    - "The suite is green in the same commit as the CSS append — no red window from the Phase 10 scope guard"
  artifacts:
    - path: "src/app.css"
      provides: ".cy-loading*, .cy-gate*, .cy-cancel*, cy-dots, cy-fill, A1-A7 additions"
      contains: ".cy-loading {"
    - path: "src/phase12-access-screens.spec.js"
      provides: "node CSS contract spec for the Phase 12 port"
      min_lines: 60
    - path: "src/phase10-screens.spec.js"
      provides: "scope guard reconciled — Phase 12 exclusions removed, DS-04 assertion intact"
      contains: "border-radius"
  key_links:
    - from: "src/app.css"
      to: "@media (prefers-reduced-motion: reduce)"
      via: "fourth per-phase suppression block"
      pattern: "cy-dots::after \\{ animation: none"
    - from: "src/phase12-access-screens.spec.js"
      to: "src/app.css"
      via: "readFileSync CSS contract"
      pattern: "src/app\\.css"
---

<objective>
Port the Phase 12 Cyber CSS verbatim into `src/app.css` (the `.cy-loading*` / `.cy-gate*` / `.cy-cancel*` blocks plus the `cy-dots` and `cy-fill` keyframes), add the authorized additions A1–A7 from `12-UI-SPEC.md`, reconcile the Phase 10 scope guard in the same task, and stand up the node CSS-contract spec.

Purpose: every Wave 2 component plan renders `.cy-*` classes and every browser spec imports `src/app.css` for visibility assertions. The stylesheet gets exactly one owner plan so there are no cascade conflicts — the Phase 10 Plan 01 precedent.
Output: appended `src/app.css` sections, an edited `src/phase10-screens.spec.js`, and a new `src/phase12-access-screens.spec.js`.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md
@.planning/phases/12-access-control-secondary-screens/12-RESEARCH.md
@.planning/phases/12-access-control-secondary-screens/12-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Class names this plan publishes. Wave 2 component plans consume these and must not invent others. -->

Ported verbatim (cyber.css 469-518):
.cy-loading / .cy-loading-card / .cy-loading-eyebrow / .cy-loading-card h2 / .cy-loading-card h2 .cy-dots::after
.cy-loading-log / .cy-loading-log b / .cy-loading-log .cy-wait / .cy-loading-meter / .cy-loading-bar
.cy-loading-bar b / .cy-loading-pct ; @keyframes cy-dots ; @keyframes cy-fill
.cy-gate / .cy-gate-card / .cy-gate-eyebrow / .cy-gate-card h2 / .cy-gate-card p / .cy-gate-readout
.cy-gate-readout .cy-403 / .cy-gate-actions / .cy-gate-foot
.cy-cancel / .cy-cancel-card / .cy-cancel-eyebrow / .cy-cancel-card h2 / .cy-cancel-log
.cy-cancel-log .cy-sig / .cy-cancel-log .cy-ok / .cy-cancel-actions

Designed additions (UI-SPEC A1-A7 — this is the CLOSED list, nothing else is in contract):
A1 reduced-motion block ; A2 .cy-loading-log .cy-err ; A3 .cy-loading-card.is-timeout h2
A4 .cy-loading-actions ; A5 flex-wrap: wrap added to .cy-cancel-actions ; A7 .cy-sr-only
A6 .cy-hc-switch (+ :hover:not(:disabled), :focus-visible, :disabled) / .cy-hc-switch-seg /
   .cy-hc-switch-seg.is-on / .cy-hc-switch-sep / .cy-hc-switch-state / .cy-hc-switch-state.is-public

Already shipped — do NOT re-port: .cy-btn .cy-btn-sm .cy-btn-primary .cy-btn-ghost .cy-btn-discord
.cy-btn-danger .cy-foot .cy-chat-cursor cy-blink .cy-grow .cy-field .cy-field-label .cy-input
.cy-hc-section .cy-hc-row .cy-hc-hint
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the verbatim Phase 12 CSS + A1-A5/A7 and reconcile the Phase 10 scope guard</name>
  <files>src/app.css, src/phase10-screens.spec.js</files>
  <read_first>
    - src/app.css — read from line 690 to the end (841 lines total). Note that the Phase 10 `.cy-pause*` block (~698-708) and the Phase 11 `.cy-hc-*` block (~808-819) are written ONE COMPACT RULE PER LINE, unlike Phase 8's multi-line block (~291-370). Note the three existing `@media (prefers-reduced-motion: reduce)` blocks at ~373, ~712, ~838, and the `/* ============ PHASE N — TITLE ============ */` section banners at ~385 and ~717.
    - .planning/phases/12-access-control-secondary-screens/12-RESEARCH.md § Design Source Verification — contains the COMPLETE verbatim CSS to transcribe (all three blocks plus both keyframes), already extracted from cyber.css 469-518 in the compact one-rule-per-line style. Transcribe from there; do not re-derive.
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § CSS Port & Justified Additions (the A1-A7 table with verbatim rule text) and § Verbatim-Port Checklist (the fidelity guard list)
    - design_handoff_pickban_cyber/prototype/variants/cyber.css lines 465-520 — the authoritative source, for a final byte comparison
    - src/phase10-screens.spec.js — the whole file (93 lines). The scope guard is at 83-93: the `it` to delete spans 84-88, the DS-04 `it` to keep spans 90-92.
    - vite.config.js line 31 — `expect: { requireAssertions: true }`, so an assertion-free `it` FAILS
  </read_first>
  <action>
    Two edits that MUST land in the same commit, because appending `.cy-loading` turns `src/phase10-screens.spec.js:85` red.

    (a) Append to `src/app.css`, after the current final line (841), a new section opened by the banner `/* ============ PHASE 12 — ACCESS & SECONDARY SCREENS ============ */`, containing:

    The three verbatim blocks and both keyframes, transcribed byte-for-byte from `12-RESEARCH.md` § Design Source Verification. Load-bearing literals that must survive transcription exactly: `min-height: 78vh` on all three wrappers; outer padding `40px` / `32px` / `40px`; card `max-width` `560px` / `500px` / `520px`; `.cy-loading-card` `box-shadow: inset 0 0 80px rgba(196,75,255,0.05)`; `.cy-gate-card` `border-left: 3px solid var(--cy-amber)` plus `box-shadow: 0 0 60px rgba(255,170,0,0.08)` plus flex-column `gap: 14px`; `.cy-cancel-card` `border: 1px solid var(--cy-red)` plus `box-shadow: 0 0 60px rgba(255,34,85,0.16)`; `.cy-cancel-eyebrow` `text-shadow: 0 0 12px #ff225580` (a literal, NOT a token); h2 sizes 22 / 22 / 24 px all with `letter-spacing: 0.04em`; `.cy-loading-log` `min-height: 8.4em` and `line-height: 1.6`; both `b` rules carrying `font-weight: 400` (`<b>` is a colour hook, never a weight change); `.cy-loading-bar` `flex: 1; font-size: 16px; letter-spacing: 1px; color: var(--cy-line-2); overflow: hidden; white-space: nowrap`; `@keyframes cy-dots` as four `content` steps at 0/25/50/75% with `1.4s steps(4) infinite`; `@keyframes cy-fill` from `clip-path: inset(0 100% 0 0)` to `inset(0 0 0 0)` with `2.4s linear infinite`; `white-space: pre-wrap` on `.cy-loading-log`, `.cy-gate-readout` and `.cy-cancel-log`.

    Addition A2: `.cy-loading-log .cy-err` sets `color: var(--cy-red)`.
    Addition A3: `.cy-loading-card.is-timeout h2` sets `color: var(--cy-red)` and `text-shadow: 0 0 12px #ff225580`.
    Addition A4: `.cy-loading-actions` sets `display: flex; gap: 8px; flex-wrap: wrap`.
    Addition A5: add `flex-wrap: wrap` to the ported `.cy-cancel-actions` rule — the prototype was authored at 1400x900 and at 320px two buttons plus the status span overflow the 520px card.
    Addition A7: `.cy-sr-only` sets `position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip-path: inset(50%); white-space: nowrap; border: 0` — use `clip-path`, never the legacy `clip`.
    Addition A1: a FOURTH `@media (prefers-reduced-motion: reduce)` block — a new per-phase block with a rationale comment; do NOT edit the three existing ones. It contains exactly three selectors: `.cy-loading-card h2 .cy-dots::after` set to `animation: none` and `content: "..."`; `.cy-loading-bar b` set to `animation: none`; and `.cy-chat-cursor` set to `animation: none`. The pinned `content: "..."` is mandatory — `animation: none` alone reverts `.cy-dots::after` to the base rule's `content: ""` and the heading would read `ESTABLISHING_LINK` with nothing after it. `.cy-loading-bar b` needs no static value: an unset `clip-path` renders the full lime bar, an honest static indeterminate meter. The `.cy-chat-cursor` line closes a real Phase 8 D-10 gap (app.css 373/712/838 suppress `.cy-brand-cur` but never `.cy-chat-cursor` at 668) and causes no visual regression because `cy-blink` animates opacity only.

    Style rules: match the compact one-rule-per-line formatting and two-space indent of the Phase 10/11 ported blocks. Zero `border-radius` anywhere (DS-04). Never run `npm run format` — `src/app.css` already fails `prettier --check` at baseline and reformatting would bury the phase diff.

    Do NOT add anything outside the A1-A7 list; it is closed by contract. Do NOT re-port any class from the "already shipped" list in the `<interfaces>` block. A6 (the switch vocabulary) belongs to Task 2, not this one.

    (b) Reconcile `src/phase10-screens.spec.js`. Delete the entire `it('does not leak Phase 12 selectors into the port')` block (lines 84-88), not just its three `expect(css).not.toContain(...)` assertions — `requireAssertions: true` makes an emptied `it` fail. Retitle the enclosing describe from `Phase 10 — scope guards (no Phase 12, no radius)` to `Phase 10 — scope guards (no radius)`. Keep the `it('DS-04: still contains zero border-radius declarations')` block and its `expect(css).not.toMatch(/border-radius/)` assertion BYTE-IDENTICAL. Net test count −1. This is exactly the operation Phase 11 Plan 01 performed on its own four exclusions, in the same task as its CSS append.
  </action>
  <acceptance_criteria>
    - `grep -c "border-radius" src/app.css` returns 0 (DS-04 stays green)
    - `grep -c "PHASE 12 — ACCESS & SECONDARY SCREENS" src/app.css` returns 1
    - All of these anchors match in `src/app.css`: `.cy-loading {`, `.cy-loading-card {`, `.cy-loading-log {`, `.cy-loading-meter {`, `.cy-loading-bar {`, `.cy-loading-pct {`, `.cy-gate {`, `.cy-gate-card {`, `.cy-gate-readout {`, `.cy-gate-actions {`, `.cy-gate-foot {`, `.cy-cancel {`, `.cy-cancel-card {`, `.cy-cancel-log {`, `.cy-cancel-actions {`, `.cy-sr-only {`, `.cy-loading-actions {`
    - `grep -c "@keyframes cy-dots" src/app.css` returns 1; `grep -c "@keyframes cy-fill" src/app.css` returns 1
    - `grep -c "prefers-reduced-motion" src/app.css` returns 4
    - `grep -F 'text-shadow: 0 0 12px #ff225580' src/app.css` matches at least twice (`.cy-cancel-eyebrow` and A3)
    - `grep -F 'min-height: 8.4em' src/app.css` matches; `grep -F 'clip-path: inset(50%)' src/app.css` matches
    - `grep -c "cy-hc-switch" src/app.css` returns 0 (A6 belongs to Task 2)
    - `grep -c "does not leak Phase 12 selectors" src/phase10-screens.spec.js` returns 0
    - `grep -F "expect(css).not.toMatch(/border-radius/)" src/phase10-screens.spec.js` still matches
    - `npx vitest run src/phase10-screens.spec.js` passes
    - `npm test` passes with at least 201 passing tests (baseline 202 minus the one deleted scope-guard `it`)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/phase10-screens.spec.js src/phase8-foundation.spec.js && npm test</automated>
  </verify>
  <done>`src/app.css` carries the verbatim Phase 12 port plus A1-A5 and A7, zero `border-radius`, and a fourth reduced-motion block; `src/phase10-screens.spec.js` no longer excludes the Phase 12 prefixes and its DS-04 assertion is unchanged; the suite is green in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Append the A6 open_spectating switch style vocabulary</name>
  <files>src/app.css</files>
  <read_first>
    - src/app.css — the Phase 11 `.cy-hc-*` region (~808-819) these classes join, plus these exact shipped rules to derive metrics from: `.cy-field-label` (~765), `.cy-hc-section` (~808), `.cy-hc-row` (~809), `.cy-chat-tabs button.is-active` (~661), `.cy-script-rm` (~799), `.cy-script-move:disabled` (~804), `.cy-input:focus` (~367-370)
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § CSS Port & Justified Additions row A6, § Spacing Scale including the inline correction note about `4px 8px` vs `4px 10px`, § Color (the closed lime and red reserved lists), and § Component Contracts 4 (the switch markup this CSS must style)
    - .planning/phases/12-access-control-secondary-screens/12-PATTERNS.md § 19 (quoted source values for each shipped rule above) and § No Analog Found (there is no toggle or switch anywhere in the codebase or the design handoff — UI-SPEC is the authority)
  </read_first>
  <action>
    Append the A6 switch vocabulary to `src/app.css` inside the Phase 12 section created in Task 1, as a commented sub-block that names the Phase 11 `.cy-hc-*` family it extends.

    Every metric is copied from a named shipped rule — do not invent values:

    `.cy-hc-switch` (the button): `display: inline-flex; align-items: center; background: var(--cy-bg); border: 1px solid var(--cy-line); font: inherit; font-size: 11px; cursor: pointer; padding: 0; color: var(--cy-text-3)` — the metrics band of `.cy-script-rm` at app.css:799.
    `.cy-hc-switch:hover:not(:disabled)`: neutral hover only, `border-color: var(--cy-line-2)`. NO red — red is reserved to the six elements in UI-SPEC § Color, and "private" is the safe default, never an error state.
    `.cy-hc-switch:focus-visible`: `outline: 1px solid var(--cy-lime); outline-offset: 1px`, copied from `.cy-input:focus` at app.css:367-370.
    `.cy-hc-switch:disabled`: `opacity: 0.4; cursor: default`, byte-for-byte from `.cy-script-move:disabled` at app.css:804.
    `.cy-hc-switch-seg`: `padding: 4px 8px` (byte-identical to `.cy-script-rm`; NOT `4px 10px`, which is off the 4-grid and was corrected during checker review) and `color: var(--cy-text-3)`.
    `.cy-hc-switch-seg.is-on`: the active inversion, byte-identical to `.cy-chat-tabs button.is-active` at app.css:661 — `background: var(--cy-text); color: var(--cy-bg)`. Deliberately neutral for BOTH states; lime is not used here.
    `.cy-hc-switch-sep` (the `|` separator): `color: var(--cy-line-2)` and no padding beyond the segments.
    `.cy-hc-switch-state` (the `room = PRIVATE` readout): `font-size: 11px; color: var(--cy-text-3)`.
    `.cy-hc-switch-state.is-public`: `color: var(--cy-lime); text-shadow: var(--cy-lime-glow)`. This is the ONLY lime in the switch and it carries one meaning: this room is open.

    Zero `border-radius`. No font size outside the phase's 11 / 12 / 13 / 16 / 22 / 24 set. Compact one-rule-per-line formatting. Never run `npm run format`.
  </action>
  <acceptance_criteria>
    - All nine selectors match in `src/app.css`: `.cy-hc-switch {`, `.cy-hc-switch:hover:not(:disabled)`, `.cy-hc-switch:focus-visible`, `.cy-hc-switch:disabled`, `.cy-hc-switch-seg {`, `.cy-hc-switch-seg.is-on`, `.cy-hc-switch-sep`, `.cy-hc-switch-state {`, `.cy-hc-switch-state.is-public`
    - `grep -c "border-radius" src/app.css` still returns 0
    - `grep -F 'padding: 4px 8px' src/app.css` matches; `grep -c "4px 10px" src/app.css` returns 0
    - The `.cy-hc-switch-seg.is-on` declaration contains both `background: var(--cy-text)` and `color: var(--cy-bg)`
    - Within the A6 sub-block only: `--cy-lime` appears exactly twice (the `:focus-visible` outline and `.cy-hc-switch-state.is-public`) and `--cy-red` appears zero times
    - `npx vitest run src/phase10-screens.spec.js` passes and `npm test` passes
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/app.css','utf8');const need=['.cy-hc-switch {','.cy-hc-switch:hover:not(:disabled)','.cy-hc-switch:focus-visible','.cy-hc-switch:disabled','.cy-hc-switch-seg {','.cy-hc-switch-seg.is-on','.cy-hc-switch-sep','.cy-hc-switch-state {','.cy-hc-switch-state.is-public','padding: 4px 8px'];const miss=need.filter(s=>!c.includes(s));if(miss.length){console.error('missing',miss);process.exit(1)}if(c.includes('border-radius')){console.error('border-radius present');process.exit(1)}if(c.includes('4px 10px')){console.error('stale 4px 10px');process.exit(1)}console.log('A6 switch CSS OK')" && npm test</automated>
  </verify>
  <done>The nine `.cy-hc-switch*` selectors exist in the Phase 12 section of `src/app.css` with metrics copied from the named Phase 11 rules, lime confined to focus and the public readout, and zero `border-radius`.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Phase 12 CSS contract spec</name>
  <files>src/phase12-access-screens.spec.js</files>
  <read_first>
    - src/phase10-screens.spec.js — the `read()` helper (lines 1-6) and the `toContain('.cy-x {')` anchor idiom (note the trailing space-brace, which pins the selector to a rule opening rather than an arbitrary substring)
    - src/phase8-foundation.spec.js lines 1-45 — literal-value assertions and the `split(...).length - 1` counting trick
    - src/phase11-fixes.spec.js — the whole file (26 lines). Prefer its `/** @param {string} p */`-annotated `read` helper over phase10-screens.spec.js's un-annotated one, which is one of the 10 svelte-check baseline errors.
    - src/app.css as written by Tasks 1 and 2
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § Verbatim-Port Checklist and § Motion Contract (the reduced-motion verification note: `page.emulateMedia` is unavailable in this vitest-browser provider, so reduced motion is asserted through this CSS contract)
    - vite.config.js lines 29-52 — project routing is by filename only; `phase12-access-screens.spec.js` (no `.svelte.` segment) routes to the node `server` project
  </read_first>
  <action>
    Create `src/phase12-access-screens.spec.js` as a node-project spec asserting the CSS contract for this phase. Read `src/app.css` once via a `readFileSync`-backed `read()` helper, following the `phase10-screens.spec.js` precedent.

    Organise as describes covering:

    (1) Verbatim port anchors — assert the presence of every rule-opening anchor listed in this plan's `<interfaces>` "Ported verbatim" block, using the trailing `" {"` form.

    (2) Verbatim values (fidelity guard, from UI-SPEC § Verbatim-Port Checklist) — assert the literal strings `min-height: 78vh`, `max-width: 560px`, `max-width: 500px`, `max-width: 520px`, `inset 0 0 80px rgba(196,75,255,0.05)`, `border-left: 3px solid var(--cy-amber)`, `0 0 60px rgba(255,170,0,0.08)`, `0 0 60px rgba(255,34,85,0.16)`, `text-shadow: 0 0 12px #ff225580`, `letter-spacing: 0.04em`, `min-height: 8.4em`, `letter-spacing: 1px`, and `white-space: pre-wrap`.

    (3) Keyframes — assert `@keyframes cy-dots` and `@keyframes cy-fill` each appear exactly once (use the counting trick), and that the `cy-fill` block contains both `inset(0 100% 0 0)` and `inset(0 0 0 0)`, and the `cy-dots` block contains `steps(4)`.

    (4) Reduced motion (D-07, Phase 8 D-10) — assert `src/app.css` contains exactly 4 `@media (prefers-reduced-motion: reduce)` blocks, and that the Phase 12 one contains all three A1 selectors and the pinned `content: "..."` for the dots.

    (5) Colour-hook classes — assert `.cy-403`, `.cy-sig`, `.cy-ok`, `.cy-wait`, `.cy-err` each appear, and that `.cy-sig` and `.cy-403` and `.cy-err` resolve to `var(--cy-red)` while `.cy-ok` resolves to `var(--cy-lime)`.

    (6) A6 switch vocabulary — assert the nine switch selectors, `padding: 4px 8px`, the neutral inversion pair on `.cy-hc-switch-seg.is-on`, and that `4px 10px` does NOT appear.

    (7) A7 and DS-04 — assert `.cy-sr-only {` with `clip-path: inset(50%)`, and that the whole file still contains zero `border-radius` (a Phase-12-local restatement of the DS-04 guard; the canonical one stays in `phase10-screens.spec.js`).

    Every `it` must carry at least one assertion (`requireAssertions: true`). Do NOT add `+page.svelte` or `+layout.svelte` source contracts here — Plan 09 extends this file with those once the page wiring exists; adding them now would make the suite red for four waves.

    Style: tabs, single quotes, printWidth 100, JSDoc annotation on the `read` helper. Never run `npm run format`.
  </action>
  <acceptance_criteria>
    - `test -f src/phase12-access-screens.spec.js` succeeds and the file is at least 60 lines
    - `npx vitest run src/phase12-access-screens.spec.js` passes with at least 7 tests, 0 failures, 0 todo
    - `grep -c "it.todo" src/phase12-access-screens.spec.js` returns 0
    - `grep -c "page.svelte\|+layout.svelte" src/phase12-access-screens.spec.js` returns 0 (source contracts are Plan 09's)
    - The spec asserts `prefers-reduced-motion` block count equals 4
    - The spec asserts `4px 10px` is absent from `src/app.css`
    - `npm test` passes with at least 208 passing tests (201 after the Task 1 deletion, plus at least 7 new)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/phase12-access-screens.spec.js && npm test</automated>
  </verify>
  <done>`src/phase12-access-screens.spec.js` exists in the node project and green-asserts the full Phase 12 CSS contract: verbatim anchors, verbatim literals, both keyframes, the four reduced-motion blocks, the colour hooks, the A6 switch vocabulary, `.cy-sr-only`, and zero `border-radius`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design source → shipped stylesheet | Values transcribed by hand from `cyber.css`; a transcription error is a fidelity defect, not a security one |
| CSS → assistive technology | `.cy-sr-only` is the only carrier of the plain-language 403 / cancellation sentences; a broken rule silently removes information from AT users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | Information disclosure (a11y) | `.cy-sr-only` visually-hidden rule | mitigate | Asserted by the CSS contract spec including `clip-path: inset(50%)`; a `display: none` variant would drop the text from the a11y tree entirely and is explicitly excluded |
| T-12-18 | Denial of service (motion-induced) | `cy-dots` and `cy-fill` infinite keyframes | mitigate | A1 adds a fourth `@media (prefers-reduced-motion: reduce)` block suppressing both plus `.cy-chat-cursor`, asserted by a block-count-equals-4 test |
| T-12-19 | Tampering (visual spoofing) | red used for the closed/private state | mitigate | UI-SPEC closes the red list to six elements; the A6 switch is neutral for both states and the spec asserts `--cy-red` is absent from the switch sub-block, so "private" can never be mistaken for "error" |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (`12-RESEARCH.md` § Package Legitimacy Audit: not applicable, verified against `package.json`). No install task exists in any Phase 12 plan |

No trust boundary is crossed by this plan — it ships stylesheet text and node-level file assertions only. No runtime input, no authorization decision, no network or DB access.
</threat_model>

<verification>
- `npm test` green. Do NOT gate on `npm run lint` or `npm run check` — both are red at baseline (19 prettier + 20 eslint `src/` failures; 10 svelte-check errors in 4 files).
- Never run `npm run format`.
- `grep -c "border-radius" src/app.css` is 0 after every task.
- `git diff src/phase10-screens.spec.js` shows only the deleted `it` and the retitled describe — the DS-04 assertion line must be untouched.
</verification>

<success_criteria>
- The three Phase 12 card blocks and both keyframes exist in `src/app.css` under `.cy-app` with the verbatim `cyber.css` values.
- A1-A7 are all present and nothing outside that closed list was added.
- The Phase 10 scope guard no longer excludes `cy-loading` / `cy-gate` / `cy-cancel`, and its DS-04 assertion is byte-identical.
- `src/phase12-access-screens.spec.js` green-asserts the whole CSS contract, including the reduced-motion block that cannot be tested in the browser project.
- Suite green in every commit — no red window.
</success_criteria>

<output>
Create `.planning/phases/12-access-control-secondary-screens/12-02-SUMMARY.md` when done. Record: the app.css line count before and after, the new test count, and any `cyber.css` value where transcription required a judgement call.
</output>
