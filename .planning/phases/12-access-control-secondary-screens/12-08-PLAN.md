---
phase: 12-access-control-secondary-screens
plan: 08
type: execute
wave: 4
depends_on: [12-03, 12-04, 12-05, 12-06, 12-07]
files_modified:
  - src/routes/draft/[id]/+page.svelte
  - src/lib/components/molecules/DraftBoard.svelte
  - src/routes/+layout.svelte
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03, ACC-04, SCR-01, SCR-02]
user_setup: []

must_haves:
  truths:
    - "A blocked guest sees the Guest Gate on first paint, on socket refusal, and on live ejection — all through one branch"
    - "RETRY_AS_GUEST re-runs the load without a reload and drops the guest into the room once the host opens it"
    - "Cold-loading a draft room shows the Connecting screen for a readable minimum window, then the lobby or draft"
    - "A dead socket surfaces a timeout state instead of parking on ESTABLISHING_LINK forever"
    - "Everyone including the host sees the Room Cancelled screen, and the draft board no longer competes for that phase"
    - "The host's open_spectating switch reaches setRoomVisibility and failures surface in the existing actionError line"
    - "A gated guest holds no WebSocket subscription and receives no chat for the room"
    - "A guest ejected mid-lobby stops receiving roster broadcasts, not just stops seeing them — the subscription is released, not merely hidden"
  artifacts:
    - path: "src/routes/draft/[id]/+page.svelte"
      provides: "the five-branch chain, the gate derive, the connect latches, and the visibility handler"
      contains: "showConnecting"
    - path: "src/routes/+layout.svelte"
      provides: "the D-19 subscription gate on the shared refcounted lobby store"
      contains: "page.data.gated"
    - path: "src/lib/components/molecules/DraftBoard.svelte"
      provides: "the dead cancelled sub-branch removed"
      contains: "cy-draft"
  key_links:
    - from: "src/routes/draft/[id]/+page.svelte"
      to: "CyGuestGate / CyConnecting / CyCancelled"
      via: "the ordered {#if} chain"
      pattern: "\\{#if gated\\}"
    - from: "src/routes/+layout.svelte"
      to: "fromStore(lobby(code))"
      via: "conditional read gated on page.data.gated"
      pattern: "!page\\.data\\.gated"
    - from: "src/routes/draft/[id]/+page.svelte"
      to: "page.data.gated"
      via: "latched one-shot invalidateAll() on snapshot-sourced ejection"
      pattern: "ejectionPromoted = true"
    - from: "src/routes/draft/[id]/+page.svelte"
      to: "setRoomVisibility live RPC"
      via: "onSetVisibility prop on LobbyHostBar"
      pattern: "setRoomVisibility\\(code"
---

<objective>
Wire everything together in the composition root: the three-source `gated` derive, the D-05 floor and D-08 timeout latches, the `handleSetVisibility` RPC caller, the D-20 client chat gate, the restructured five-branch `{#if}` chain (D-09/D-15), the deletion of `DraftBoard`'s now-dead cancelled sub-branch, and the D-19 subscription gate in the root layout.

Purpose: this is the plan where all six requirements become user-visible. It is also where two library contracts bite: the shared lobby store is refcount-cached with no `refetch`, so a gated client must not subscribe at all; and branch ORDER determines whether the Connecting screen fights the frozen PauseOverlay grace flow.
Output: a restructured `src/routes/draft/[id]/+page.svelte`, three deletions in `src/lib/components/molecules/DraftBoard.svelte`, and a one-condition change in `src/routes/+layout.svelte`.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-access-control-secondary-screens/12-CONTEXT.md
@.planning/phases/12-access-control-secondary-screens/12-RESEARCH.md
@.planning/phases/12-access-control-secondary-screens/12-PATTERNS.md
@.planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md
@.planning/phases/12-access-control-secondary-screens/12-05-SUMMARY.md
@.planning/phases/12-access-control-secondary-screens/12-06-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Component prop contracts landed by Plans 05 and 06. Wire these EXACT shapes. -->
CyGuestGate  { code, loginHref, onRetry, checking?, stillGated? }
CyConnecting { code, timedOut?, onRetry }
CyCancelled  { code, reason?, players, spectators }
LobbyHostBar { ...existing, onSetVisibility: (next: boolean) => void }

<!-- Server contracts landed by Plans 01, 03, 04, 07 -->
page load data: { room: { public_code, phase, host_user_id? }, userId, gated: boolean, appOrigin, actions?, teams? }
lobby snapshot: { publicCode, roomId, phase, hostUserId, teams: {A,B}, spectators, isPublic, cancelReason? }
live RPC: setRoomVisibility(publicCode, isPublic) -> snapshot
stream refusal surfaces as streamVal = { error: { code: 'FORBIDDEN', message: 'Room is private' } }

<!-- Constants this plan introduces -->
CONNECT_FLOOR_MS = 900     (D-05; measured — full type takes 2594ms, so 900ms lands mid-line-2)
CONNECT_TIMEOUT_MS = 10_000 (D-08)

<!-- BRANCH ORDER IS LOAD-BEARING (RESEARCH Pattern 4, UI-SPEC Component Contract 5) -->
1. {#if gated}                                 -> CyGuestGate   (reads NO stream value)
2. {:else if snapshot && snapshot.phase === 'cancelled'}  -> CyCancelled   (this exact form; NOT the `snapshot?.phase` optional-chain shorthand — 12-08-T2's grep-count-of-1 and 12-09-T1's "appears exactly once" contract both pin this literal)
3. {:else if showConnecting}                   -> CyConnecting
4. {:else if loadError}                        -> existing error copy (NOT the gate, NOT connecting)
5. {:else if snapshot}                         -> drafting | review | lobby  ('cancelled' removed from the drafting condition)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The page script — gate derive, connect latches, visibility handler, chat gate</name>
  <files>src/routes/draft/[id]/+page.svelte</files>
  <read_first>
    - src/routes/draft/[id]/+page.svelte — read the ENTIRE file (399 lines). Critical regions: the imports at 1-20 (including `resolve` from `$app/paths` at line 2 and the `$live/*` imports); the four stream derives at 39-50 (`snapshot`, `loadError`, `loading = streamVal === undefined`, `isGuest`, `isHost`); `draftPath` at 63; `copyTimer` at 36-37 and `copyLink()` at 73-86; `errMsg` at 66-71 (its `'Something went wrong'` fallback is why the RPC throws authored messages); `handleKick` at 106-113 (the five-line RPC-caller shape every handler uses); the chat `$effect` at 180-192.
    - src/phase11-fixes.spec.js lines 17-19 — a source-regex assertion over the chat `$effect`: `/const store = activeChatStream\(code\);[\s\S]*?chatStreamVal = undefined;[\s\S]*?store\.subscribe/`. An early `return` placed ABOVE `const store = ...` keeps this green; reordering those three statements BREAKS it.
    - .planning/phases/12-access-control-secondary-screens/12-RESEARCH.md § Architecture Patterns 4 (the three-source gate derive, verbatim) and 5 (do not subscribe while gated), § Code Examples E4 (the latch + `showConnecting` derive, verbatim), § Resolved Investigation 5 (`invalidateAll()` semantics and its limits) and 6 (why `streamVal === undefined` is cold-load-only, and the measured floor table), § Common Pitfalls 2, 3, 7, 11
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § Component Contracts 1 (the "Retry semantics" row — CyConnecting's retry is a FULL RELOAD, not invalidateAll) and 2 (the gate's retry IS invalidateAll), § Component Contracts 5, § State Matrix
    - .planning/phases/12-access-control-secondary-screens/12-PATTERNS.md § 9 (every construct quoted from this same file: derives, the PauseOverlay timer-effect analog, the copy pattern, the handler shape, the chat effect with its regex warning, the child-prop pattern)
    - src/lib/components/molecules/PauseOverlay.svelte lines 7-14 — the in-repo timer `$effect` with teardown, the analog for the mount latches
  </read_first>
  <action>
    Edit only the `<script>` section of `src/routes/draft/[id]/+page.svelte`. The template is Task 2's job.

    (a) **Imports.** Add `CyGuestGate`, `CyConnecting` and `CyCancelled` from `$lib/components/molecules/`, `invalidateAll` from `$app/navigation`, and `setRoomVisibility` from the existing `$live/room` import. Keep the existing `resolve` import.

    (b) **The three-source `gated` derive (RESEARCH Pattern 4).** Add, using `$derived.by` with early returns in this exact order:
    - `if (data.gated) return true;` — D-02 SSR first paint.
    - `if (loadError && loadError.code === 'FORBIDDEN') return true;` — D-01 socket refusal. This third source is what makes the `hooks.ws.js:52-70` identity split degrade gracefully: a signed-in user can legitimately arrive at the socket as a guest, pass SSR, and be refused by the stream; without this clause they would see a broken lobby with an error line instead of a gate with two useful actions.
    - `if (!isGuest || !snapshot) return false;`
    - `return snapshot.phase === 'lobby' && snapshot.isPublic === false;` — D-11 live ejection, which is the ONLY way an ejected guest learns they were ejected (the SSR load does not re-run; nothing server-side can trigger a client navigation).
    Use `snapshot.isPublic === false`, NEVER `!snapshot.isPublic`. A snapshot published before this deploy, or any hand-built fixture, lacks the field, and `undefined` is falsy — the loose form would gate every user out including the host.
    Order matters for a second reason: because Svelte 5 deriveds are lazy, `data.gated === true` short-circuits on the first line without ever reading `loadError`, which is part of what keeps the gated path from subscribing.

    (c) **The D-05/D-08 latches (RESEARCH E4).** Add module-level constants `CONNECT_FLOOR_MS = 900` and `CONNECT_TIMEOUT_MS = 10_000`, two `$state` booleans `floorElapsed` and `connectTimedOut`, and a single MOUNT-ONLY `$effect` that sets two `setTimeout`s and returns a teardown clearing both. The effect must contain NO reactive reads, so it runs exactly once — deriving the floor from snapshot arrival instead would re-trigger on every later phase transition. Follow the `PauseOverlay.svelte:7-14` teardown shape.

    Then `const showConnecting = $derived(!gated && !loadError && (loading || !floorElapsed));`

    `loadError` is deliberately EXCLUDED. `loading` is `streamVal === undefined`, and `store.set(undefined)` happens only in the library's `cleanup()` when the refcount hits zero — reconnects keep the last value and terminal closes arrive as `{ error: DISCONNECTED | CONNECTION_CLOSED | TIMEOUT }`. So rendering Connecting for `loadError` would show `ESTABLISHING_LINK` during a mid-draft disconnect and fight the frozen PauseOverlay grace flow that D-06 forbids touching. Do not widen this derive.

    900ms is the measured choice at the top of D-05's authorized 600–900ms range: the full type takes 2594ms, so 900ms lands mid-line-2 with the `▮` still trailing — legible and clearly in progress. Under `prefers-reduced-motion` the log is complete at first paint and the floor is the only thing keeping the screen readable, which is D-05's stated rationale.

    (d) **`handleSetVisibility`.** Add a handler following the `handleKick` five-line shape byte-for-byte: clear `actionError`, `try { await setRoomVisibility(code, next); } catch (e) { actionError = errMsg(e); }`. Positional args, matching the RPC signature from Plan 03. No optimistic local state — the broadcast is what flips the switch.

    (e) **`handleRetryAsGuest` + gate feedback state + the latched ejection promotion.** Two pieces, both built on `invalidateAll()`.

    First, `handleRetryAsGuest`: add `retryChecking` and `retryStillGated` as `$state` booleans and a handler that sets `retryChecking = true`, `await invalidateAll()`, then sets `retryStillGated` to whether the page is still gated after the load re-ran, and finally clears `retryChecking` in all cases. `invalidateAll()` is the correct primitive (D-04): it re-runs server `load` functions and does NOT remount components, so the Phase 8 boot animations stay put. `invalidate('key')` would be a no-op here because this load registers no dependency via `fetch` or `depends()`. `invalidateAll()` is browser-only and throws on the server, so it must only ever be called from an `onclick` or from inside a browser-only `$effect`.

    Second, **the latched ejection promotion — this is what actually closes the D-11 subscription leak.** On the live-ejection path the SSR load does not re-run, so `data.gated` stays `false` and the `gated` derive can only detect the ejection by reading `snapshot`. Reading `snapshot` is reading the stream, so the shared `lobby` store keeps a tracking reader and `createSubscriber` never tears the subscription down: an ejected guest would keep receiving roster broadcasts for a room they are locked out of until they happened to click `RETRY_AS_GUEST()`. D-11 says "'Private' means private", so this residual is not acceptable — close it.

    Add a `$state` latch `ejectionPromoted = false` and an `$effect` that fires ONE `invalidateAll()` when the snapshot-sourced gate condition first becomes true:
    - Guard order inside the effect, exactly: `if (ejectionPromoted) return;` then `if (data.gated) return;` then `if (!isGuest || !snapshot) return;` then `if (!(snapshot.phase === 'lobby' && snapshot.isPublic === false)) return;` then `ejectionPromoted = true;` and only then `invalidateAll();`.
    - **The latch is mandatory and must be set BEFORE the call.** `invalidateAll()` re-runs `load`, which replaces `data`, which is a reactive read inside this effect — an unlatched call is an infinite invalidation loop that will pin a CPU core and hammer the server. Any executor or later refactor that removes `ejectionPromoted`, moves the assignment after the call, or widens the guards is introducing that loop. Do not do it.
    - Fire ONLY for the snapshot-sourced path. The `if (data.gated) return;` guard is what enforces that: when the guest was already gated on first paint there is nothing to promote, `page.data.gated` is already `true`, and re-invalidating would be pure churn.
    - What the promotion buys: once `load` re-runs, `+page.server.js` (Plan 04) computes `gated: true` for this guest, so `page.data.gated` flips to `true`. From that point the `gated` derive short-circuits on its FIRST line without reading `snapshot` or `loadError`, the root layout's `!page.data.gated` condition (Task 3) stops reading `fromStore(lobby(code))` too, and with no tracking reaction left `createSubscriber` releases the underlying topic subscription a microtask later. The gate keeps rendering throughout — `gated` is `true` before the invalidation and `true` after it, so there is no flash back into the lobby.
    - This is a SEPARATE, deliberately REACTIVE `$effect` — not the mount-only one from (c). (c) must stay read-free so it runs once; this one must read `data`, `isGuest` and `snapshot` so it can observe the flip. Keep them as two distinct effects.
    - The effect is itself a tracking reader while it is armed, and that is fine: Svelte 5 re-collects an effect's dependencies on every run, so once `ejectionPromoted` is `true` the first guard returns before `snapshot` is ever read and the effect drops its dependency on the store along with everything else. It does not keep the subscription alive after it has fired.
    - Scope fence: this lives in `src/routes/draft/[id]/+page.svelte`, which this plan already owns. Do NOT add a seventh realtime touch point, do NOT put this in the layout, and do NOT create a route-level layout.

    (f) **`signInHref`.** Add a `$derived` login href: `resolve('/login') + '?redirect=' + encodeURIComponent(draftPath)`. `resolve()` is mandatory (`svelte/no-navigation-without-resolve`); do NOT reuse the raw `<a href="/login?redirect=/draft/{code}">` at line 259, which is one of the 20 pre-existing eslint errors.

    (g) **`handleReloadForTimeout`.** Add a handler that calls `location.reload()`, for `CyConnecting`'s `onRetry`. Per UI-SPEC Component Contract 1, the Connecting retry is a FULL RELOAD, not `invalidateAll()`: a hung stream store exposes no `refetch` and `invalidateAll()` re-runs only HTTP loads, so a new client and a new socket are the only reliable reset for a connect that never resolved. This deliberately differs from the gate's retry.

    (h) **The D-20 client chat gate.** Add `if (gated) return;` as the VERY FIRST statement inside the chat `$effect` at 180-192 — above `const store = activeChatStream(code);` — so the store is never constructed and no subscription is made. Placement is doubly constrained: `phase11-fixes.spec.js:17-19` asserts a regex requiring `const store = activeChatStream(code);` … `chatStreamVal = undefined;` … `store.subscribe` in that source order. An early return above the first statement keeps the regex green; reordering those three statements breaks it. This is the client/UX half of D-20; the authoritative guard is the server-side one in `src/live/chat.js` (Plan 07).

    Do NOT change: `snapshot`, `loadError`, `loading`, `isGuest`, `isHost`, `errMsg`, `copyLink`, `copyTimer`, `draftPath`, `chatRole`, `activeTab`, `draftScript`, `timerSeconds`, or any existing handler. Every existing behaviour in this file is frozen except the additions above.

    Tabs, single quotes, printWidth 100. Never run `npm run format`. Run `svelte-autofixer` (Svelte MCP) on the file and resolve every issue and suggestion before finishing.
  </action>
  <acceptance_criteria>
    - `grep -F "snapshot.isPublic === false" "src/routes/draft/[id]/+page.svelte"` matches and `grep -c "!snapshot.isPublic" "src/routes/draft/[id]/+page.svelte"` returns 0
    - `grep -F "loadError.code === 'FORBIDDEN'" "src/routes/draft/[id]/+page.svelte"` matches
    - `grep -F "CONNECT_FLOOR_MS = 900" "src/routes/draft/[id]/+page.svelte"` and `grep -F "CONNECT_TIMEOUT_MS = 10_000"` both match
    - `grep -F "const showConnecting = $derived(!gated && !loadError && (loading || !floorElapsed))" "src/routes/draft/[id]/+page.svelte"` matches (or the same expression with equivalent whitespace — `loadError` MUST be negated in it)
    - `grep -F "setRoomVisibility(code" "src/routes/draft/[id]/+page.svelte"` matches
    - `grep -F "invalidateAll()" "src/routes/draft/[id]/+page.svelte"` matches
    - `grep -F "ejectionPromoted" "src/routes/draft/[id]/+page.svelte"` matches at least three times (the `$state` declaration, the `if (ejectionPromoted) return;` guard, and the `ejectionPromoted = true;` assignment)
    - Latch precedes the call: with `grep -n "ejectionPromoted = true;\|invalidateAll()" "src/routes/draft/[id]/+page.svelte"`, the `ejectionPromoted = true;` line number is LOWER than the `invalidateAll()` line number that follows it inside the same effect
    - The promotion effect is reachable only from the snapshot-sourced branch: the source between `if (ejectionPromoted) return;` and the `invalidateAll()` that follows it contains all four guards — `if (data.gated) return;`, `!isGuest`, `!snapshot`, and `snapshot.isPublic === false`. Assert this as a source contract (12-09-T1 adds the committed version); a scratch `node -e` check is enough to confirm it during this task
    - `grep -F "location.reload()" "src/routes/draft/[id]/+page.svelte"` matches exactly once
    - `grep -F "resolve('/login')" "src/routes/draft/[id]/+page.svelte"` matches
    - `grep -F "if (gated) return;" "src/routes/draft/[id]/+page.svelte"` matches, and its line number is LOWER than the `const store = activeChatStream(code);` line number (verify with `grep -n "if (gated) return;\|const store = activeChatStream"`)
    - `npx vitest run src/phase11-fixes.spec.js` passes — the WR-04 chat-effect regex must still match
    - `npm test` passes
    - `svelte-autofixer` reports zero issues and zero suggestions
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/phase11-fixes.spec.js src/phase10-screens.spec.js src/phase12-access-screens.spec.js && npm test</automated>
  </verify>
  <done>The page script exposes a lazy three-source `gated` derive with a strict `=== false`, mount-only floor and timeout latches feeding a `showConnecting` that excludes `loadError`, a `handleSetVisibility` following the frozen handler shape, an `invalidateAll()`-based gate retry with checking/still-gated state, a latched one-shot `invalidateAll()` that promotes a snapshot-sourced ejection into `page.data.gated` so the shared lobby subscription is released, a reload-based connect retry, a `resolve()`-built login href, and a `gated` early return at the very top of the chat `$effect` with the WR-04 regex still green.</done>
</task>

<task type="auto">
  <name>Task 2: The five-branch template chain + DraftBoard cancelled-branch deletion</name>
  <files>src/routes/draft/[id]/+page.svelte, src/lib/components/molecules/DraftBoard.svelte</files>
  <read_first>
    - src/routes/draft/[id]/+page.svelte — the template from 249 to the end. Critical: the `actionError` line at 250-252; the current chain head at 254-266 (`{#if loading}` renders the `// loading room…` placeholder, `{:else if loadError}` renders the guest sign-in fallback with the raw href at 259, `{:else if snapshot}` then `{#if snapshot.phase === 'drafting' || snapshot.phase === 'cancelled'}` at 266); the DraftBoard + ChatPanel block at 266-278; the review branch at 279 onward; the `LobbyHostBar` call site at 334-344 (the child-prop pattern: shorthand for same-name props, `onX` callbacks by reference, `bind:` only for two-way).
    - src/lib/components/molecules/DraftBoard.svelte — read the whole file (159 lines). The three regions to remove: `cancelledTeamLabel(team)` at 76-78 (JSDoc at 75); the `cancelledTeam` `$derived` at 80-91 (its own comment already records that captain-scanning is unreliable); the `{#if snapshot.phase === 'cancelled'} ... {:else}` wrapper at 121-130, which collapses to just the `{:else}` body. ALSO note: the `PauseOverlay` render at 113-119 including its `&& snapshot.phase !== 'cancelled'` clause is the FROZEN grace flow — keep it exactly as is. Check whether the `goto` import at line 2 has any other use before removing it.
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § Component Contracts 5 (the branch list and its three bullets, including the explicit statement that ChatPanel is NOT rendered on the cancelled screen and that this is intended, not a regression) and § State Matrix
    - .planning/phases/12-access-control-secondary-screens/12-RESEARCH.md § Common Pitfalls 7 and 10, § Code Examples E4 (the template order block), § State of the Art (the `||` at line 266 was Phase 10's placeholder while the SIGKILL reskin was deferred)
    - .planning/phases/12-access-control-secondary-screens/12-PATTERNS.md § 9 (the chain quoted verbatim) and § 20 (the three DraftBoard regions quoted verbatim, plus the `goto`-import follow-through note)
    - src/lib/components/molecules/CyGuestGate.svelte, CyConnecting.svelte, CyCancelled.svelte as landed by Plans 05 and 06 — for their exact prop names
  </read_first>
  <action>
    (a) **Restructure the `{#if}` chain in `src/routes/draft/[id]/+page.svelte`** to exactly this order. Order is load-bearing; do not reorder it for readability.

    1. `{#if gated}` → render `CyGuestGate` with `{code}`, `loginHref={signInHref}`, `onRetry={handleRetryAsGuest}`, `checking={retryChecking}`, `stillGated={retryStillGated}`. This branch must render NOTHING derived from the stream — no `snapshot`, no `loading`, no `loadError`-derived value. That structural property is what lets Svelte's `createSubscriber` tear down the WebSocket subscription and what makes `RETRY_AS_GUEST()` able to succeed: the shared store is cached by `path + ':' + args` with a refcount and exposes no `refetch`, so once the guard throws, a store that is still being read holds `{ error: FORBIDDEN }` permanently.
    2. `{:else if snapshot && snapshot.phase === 'cancelled'}` → render `CyCancelled` with `{code}`, `reason={snapshot.cancelReason ?? 'grace'}`, `players={snapshot.teams.A.length + snapshot.teams.B.length}`, `spectators={snapshot.spectators.length}`. Placing this ABOVE the connecting branch is what stops a dead room flashing `ESTABLISHING_LINK` during the floor window.
    3. `{:else if showConnecting}` → render `CyConnecting` with `{code}`, `timedOut={connectTimedOut}`, `onRetry={handleReloadForTimeout}`. This REPLACES the `<p class="cy-foot">// loading room…</p>` placeholder (D-09); delete that placeholder.
    4. `{:else if loadError}` → keep the EXISTING error copy for non-FORBIDDEN codes. Do not delete this branch and do not turn it into the gate: `FORBIDDEN` is already absorbed by branch 1 via the `gated` derive, while reconnects and terminal closes (`DISCONNECTED` / `CONNECTION_CLOSED` / `TIMEOUT`) legitimately land here. Leave the existing guest/non-guest sub-branch as it is, including the raw href at line 259 — do not "fix" that pre-existing eslint error in this task, since it is unrelated to the phase diff.
    5. `{:else if snapshot}` → the existing drafting / review / lobby sub-chain, with ONE change: the inner condition becomes `snapshot.phase === 'drafting'` only. Remove `|| snapshot.phase === 'cancelled'`. That `||` was Phase 10's placeholder while the SIGKILL reskin was deferred.

    Consequence to accept deliberately, stated so no reviewer reads it as a regression: `ChatPanel` currently renders for `phase === 'drafting' || phase === 'cancelled'` (lines 265-278). Splitting `cancelled` into branch 2 removes the chat sidebar from a dead room. This is prototype-faithful (`CYCancelled` has no chat) and intended. Do not re-add it.

    (b) **Pass `onSetVisibility={handleSetVisibility}` to `LobbyHostBar`** at its call site (334-344), following the existing child-prop pattern. Leave every other prop, including the two `bind:` props, exactly as they are.

    (c) **Delete `DraftBoard`'s cancelled sub-branch** in the SAME task, per RESEARCH Pitfall 10 — two competing cancelled UIs would otherwise double-render, or the SIGKILL card would render inside the draft grid. Remove all three regions: `cancelledTeamLabel` (**76-78**, with its JSDoc at 75), the `cancelledTeam` derive (**80-91** — the closing `);` is on 91), and the `{#if snapshot.phase === 'cancelled'} ... {:else}` wrapper (**121-130**, `{:else}` on 130), collapsing the template to the former `{:else}` body. Offsets measured 2026-09-04; delete by matching the identifiers, not by line number alone. Grep for any remaining use of `goto` in the file; if the cancelled branch was its only consumer, remove the now-unused import at line 2 as well — that also removes one `svelte/no-navigation-without-resolve` error, a net improvement against the 20-error baseline. KEEP the `PauseOverlay` render at **113-119** including its `&& snapshot.phase !== 'cancelled'` clause: that is the frozen disconnect/grace flow D-06 forbids touching.

    Tabs, single quotes, printWidth 100. Never run `npm run format`. Run `svelte-autofixer` (Svelte MCP) on both `.svelte` files and resolve every issue and suggestion before finishing.
  </action>
  <acceptance_criteria>
    - `grep -F "{#if gated}" "src/routes/draft/[id]/+page.svelte"` matches, and it is the FIRST `{#if` in the template region (verify with `grep -n "{#if" "src/routes/draft/[id]/+page.svelte"` and compare against the `<main>` line number)
    - `grep -c "// loading room…" "src/routes/draft/[id]/+page.svelte"` returns 0
    - `grep -c "snapshot.phase === 'cancelled'" "src/routes/draft/[id]/+page.svelte"` returns 1 (branch 2 only)
    - `grep -c "phase === 'drafting' || snapshot.phase === 'cancelled'" "src/routes/draft/[id]/+page.svelte"` returns 0
    - In `src/routes/draft/[id]/+page.svelte`, the `CyCancelled` line number is LOWER than the `CyConnecting` line number, and the `CyGuestGate` line number is lower than both
    - `grep -F "onSetVisibility={handleSetVisibility}" "src/routes/draft/[id]/+page.svelte"` matches
    - `grep -F "snapshot.cancelReason ?? 'grace'" "src/routes/draft/[id]/+page.svelte"` matches
    - The `{:else if loadError}` branch still exists in the chain
    - `grep -c "cy-draft-cancelled" src/lib/components/molecules/DraftBoard.svelte` returns 0
    - `grep -c "cancelledTeam" src/lib/components/molecules/DraftBoard.svelte` returns 0
    - `grep -F "snapshot.phase !== 'cancelled'" src/lib/components/molecules/DraftBoard.svelte` still matches (the frozen PauseOverlay clause)
    - `grep -c "goto" src/lib/components/molecules/DraftBoard.svelte` returns 0 if the cancelled branch was its only consumer
    - `npx vitest run src/phase10-screens.spec.js src/phase11-fixes.spec.js` passes
    - `npm test` passes
    - `svelte-autofixer` reports zero issues and zero suggestions for both files
    - `npx eslint src` reports no MORE than 20 errors (the baseline; ideally 19 after the `goto` removal)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/phase10-screens.spec.js src/phase11-fixes.spec.js src/phase12-access-screens.spec.js && npm test</automated>
  </verify>
  <done>The template renders gate → cancelled → connecting → loadError → snapshot in that order with the placeholder gone and `'cancelled'` removed from the drafting condition; `LobbyHostBar` receives `onSetVisibility`; and `DraftBoard`'s cancelled branch, its two derives and its now-unused `goto` import are gone while the PauseOverlay grace clause is intact.</done>
</task>

<task type="auto">
  <name>Task 3: The root layout subscription gate (D-19)</name>
  <files>src/routes/+layout.svelte</files>
  <read_first>
    - src/routes/+layout.svelte — read the WHOLE file (25 lines). The `page` import from `$app/state` is ALREADY there at line 5; `code` is derived from `page.params.id` at ~14; the shared subscription is the `snap` derive at line 17: `const snap = $derived.by(() => (code ? fromStore(lobby(code)).current : null));`; the `phase` fallback at 18-20 is what makes the Shell Contract's "gate shows 01_LOBBY" true — leave it alone.
    - .planning/phases/12-access-control-secondary-screens/12-CONTEXT.md D-19 including its inline "Path correction (2026-09-04, pattern-mapper)" note
    - .planning/phases/12-access-control-secondary-screens/12-PATTERNS.md § Upstream Ref Corrections (first row) and § 10 — `src/routes/draft/[id]/+layout.svelte` DOES NOT EXIST; `ls src/routes/draft/[id]/` returns only `+page.server.js`, `+page.svelte`, `page.server.spec.js`
    - .planning/phases/12-access-control-secondary-screens/12-RESEARCH.md § Architecture Patterns 5, § Resolved Investigation 4 (the refcount analysis and the `createSubscriber` teardown semantics) and 5 (why `invalidateAll()` alone cannot lift the gate), § Common Pitfalls 2
    - .planning/phases/12-access-control-secondary-screens/12-UI-SPEC.md § Shell / Phase Tracker Contract (zero changes to `CyShell.svelte` or the tracker logic — do NOT add a `04_CANCELLED` step)
  </read_first>
  <action>
    Make the root layout's lobby subscription conditional on the SSR gate flag.

    Change the `snap` derive at `src/routes/+layout.svelte:17` so the `fromStore(lobby(code))` read happens only when `code` is present AND `page.data.gated` is falsy — i.e. extend the existing `code ?` ternary condition with `&& !page.data.gated`. `page` from `$app/state` is already imported at line 5, so this is one condition and no new import.

    Why this single line matters: svelte-realtime caches stream stores by `path + ':' + args` with a refcount (`client.js:398-460`), and this layout subscribes to the very SAME `lobby(code)` store as the page. Leaving the layout subscribed while the page is gated does two things, both bad. First, an ejected guest keeps receiving every roster broadcast for a room they are locked out of, because `access`/`filter` in this library is subscribe-time-only and there is no server-initiated unsubscribe. Second, once the D-01 guard throws, the shared store holds `{ error: FORBIDDEN }` PERMANENTLY: it exposes no `refetch` and `invalidateAll()` re-runs only HTTP loads, so `RETRY_AS_GUEST()` would be dead on arrival. With both the page's gate branch and this read gated, no tracking reaction reads the store, `createSubscriber` tears the underlying subscription down a microtask later, and a successful retry becomes a clean first-subscribe — which also legitimately shows the Connecting screen during that first hydration.

    Do NOT create `src/routes/draft/[id]/+layout.svelte`. That file does not exist, and creating one to host this gate would add a SECOND subscriber to the same refcounted store and re-open the exact leak D-19 exists to close. Gate the existing root layout; create nothing.

    Do NOT touch the `phase` fallback at 18-20 — it is what makes the gate and Connecting screens show `01_LOBBY` with no `CyShell` change. Do NOT touch `CyShell.svelte` or the tracker logic, and do NOT add a `04_CANCELLED` step: `TRACKER.indexOf('cancelled')` returns `-1`, which Phase 8 D-07 already handles as "no active step, no crash".

    Note `page.data.gated` is `undefined` on Home and Login, so `!page.data.gated` is true there — but `code` is `null` on those routes and the existing `code ?` short-circuit already handles it. No regression.

    Run `svelte-autofixer` (Svelte MCP) on the file and resolve every issue and suggestion before finishing.
  </action>
  <acceptance_criteria>
    - `grep -F "!page.data.gated" src/routes/+layout.svelte` matches exactly once
    - `grep -F "fromStore(lobby(code))" src/routes/+layout.svelte` matches exactly once (still ONE subscriber in this file)
    - `test ! -f "src/routes/draft/[id]/+layout.svelte"` succeeds (no route-level layout was created)
    - `git diff --stat src/routes/+layout.svelte` shows 1 changed line
    - `git diff src/lib/components/chrome/CyShell.svelte` is empty
    - `grep -c "04_CANCELLED" src/lib/components/chrome/CyShell.svelte src/routes/+layout.svelte` returns 0 for both
    - `npx vitest run src/lib/components/chrome/CyShell.svelte.spec.js src/phase8-foundation.spec.js` passes
    - `npm test` passes
    - `svelte-autofixer` reports zero issues and zero suggestions
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/components/chrome/CyShell.svelte.spec.js src/phase8-foundation.spec.js src/phase10-screens.spec.js && npm test</automated>
  </verify>
  <done>`src/routes/+layout.svelte:17` reads the shared lobby store only when a room code exists and `page.data.gated` is falsy, in a one-line change, with no route-level layout created and `CyShell` untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-published snapshot → rendered branch | The `gated` derive and every screen's props come from `data` (SSR) and `streamVal` (WebSocket); neither is user-authored but both are attacker-influenceable in the sense that a hostile client controls which it subscribes to |
| client → WebSocket subscription lifetime | Whether the browser holds a topic subscription is decided entirely by which Svelte reactions read the refcounted store |
| client → live RPC | `handleSetVisibility` and the chat effect both initiate socket calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Information disclosure | ejected or gated guest retaining a WebSocket subscription through the shared refcounted store | mitigate | Two distinct paths, mitigated differently. **First paint** (`data.gated === true`): the `gated` derive short-circuits on its first line without reading `snapshot` or `loadError`, branch 1 renders `CyGuestGate` which reads nothing stream-derived, and `src/routes/+layout.svelte:17` gates its `fromStore(lobby(code))` read on `page.data.gated` — no tracking reaction reads the store, so `createSubscriber` never subscribes. **Live ejection** (D-11, `page.data.gated` still `false` because SSR does not re-run): the snapshot IS the only ejection signal, so the derive must read the store to detect it and the first-paint short-circuit cannot apply. T1(e)'s latched one-shot `invalidateAll()` closes this: it re-runs `load`, `page.data.gated` becomes `true`, both readers then short-circuit on source 1, and `createSubscriber` tears the subscription down. The latch (`ejectionPromoted`, set before the call) is what keeps that from being an infinite invalidation loop. Needed because svelte-realtime 0.4.6 has no server-initiated unsubscribe |
| T-12-36 | Information disclosure | gate branch accidentally reading a stream-derived value | mitigate | Enforced structurally by what branch 1 renders, and by `gated` being a lazy `$derived.by` whose first line short-circuits on `data.gated` without touching `loadError`. Asserted as a source contract in Plan 09 |
| T-12-08b | Information disclosure | gated guest still subscribing to `chatAll` | mitigate | `if (gated) return;` as the first statement of the chat `$effect`, above `activeChatStream(code)`, so the store is never constructed. This is the UX half; the authoritative guard is server-side in `src/live/chat.js` (Plan 07, T-12-08) — a client-only fix would not stop a direct WS client |
| T-12-12 | Spoofing / availability | SSR and WebSocket disagreeing about identity (`hooks.ws.js:52-70` falls back to `role: 'guest'` on any `getSession` error) | mitigate | `loadError?.code === 'FORBIDDEN'` is the third source of the `gated` derive, so the mismatch degrades to the gate with `SIGN_IN_DISCORD()` and `RETRY_AS_GUEST()` both useful, instead of a lobby shell with a dead error line |
| T-12-23 | Tampering | `!snapshot.isPublic` gating every user out | mitigate | Strict `snapshot.isPublic === false`, grep-asserted, so a pre-deploy snapshot or a fixture lacking the field cannot lock out the host and everyone else |
| T-12-25 | Spoofing (misattribution) | cancellation reason | mitigate | `reason={snapshot.cancelReason ?? 'grace'}` — an absent field defaults to grace wording and never accuses the host |
| T-12-11 | Tampering (XSS) | snapshot values reaching the three new screens | mitigate | All values pass as props into components that use plain Svelte interpolation; no `{@html}` anywhere in the phase |
| T-12-28 | Tampering (open redirect / lint regression) | the gate's login href | mitigate | Built as `resolve('/login') + '?redirect=' + encodeURIComponent(draftPath)`, where `draftPath` is itself `resolve('/draft/[id]', { id: code })`. No user-controlled URL is constructed, and the eslint error count is asserted not to grow |
| T-12-37 | Denial of service (UX) | a dead socket parking the user on `ESTABLISHING_LINK` forever | mitigate | `CONNECT_TIMEOUT_MS = 10_000` latch surfaces the designed timeout state with a working full-reload retry |
| T-12-38 | Repudiation (broken frozen flow) | Connecting screen swallowing the disconnect/grace flow | mitigate | `showConnecting` excludes `loadError`, and the cancelled branch sits above it. Reconnects and terminal closes arrive as `{error: ...}` and continue to reach the PauseOverlay path, which D-06 forbids disturbing |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages (`12-RESEARCH.md` § Package Legitimacy Audit: not applicable, verified against `package.json`). No install task exists in any Phase 12 plan |
</threat_model>

<verification>
- `npm test` green (both projects). Do NOT gate on `npm run lint` or `npm run check` — both red at baseline. Never run `npm run format`.
- Run `svelte-autofixer` (Svelte MCP) on `+page.svelte`, `DraftBoard.svelte` and `+layout.svelte` until zero issues and zero suggestions. Use `list-sections` + `get-documentation` for any `invalidateAll()`, `$effect` teardown, or lazy-derived question.
- Do NOT use `playground-link` for code written into the project.
- `src/phase11-fixes.spec.js` must stay green — its WR-04 regex constrains the chat `$effect`'s statement order.
- `npx eslint src` must report no more than 20 errors (the baseline); the `goto` removal in `DraftBoard.svelte` should bring it to 19.
- Manual-only checks are deferred to `/gsd:verify-work` per `12-VALIDATION.md`: two-browser live ejection, reduced-motion appearance, the real-socket feel of the 900ms floor, and the accepted 404-on-refresh after cancellation.
</verification>

<success_criteria>
- The gate renders from all three sources (SSR, socket refusal, live ejection) through one branch that reads no stream value.
- `RETRY_AS_GUEST()` re-runs the load without a reload, reports its in-flight and still-gated states, and succeeds cleanly once the host opens the room.
- Cold load shows Connecting for at least 900ms and never shows it for `loadError`; a 10s dead socket shows the timeout state.
- Everyone including the host sees `CyCancelled` with real counts and the correct reason wording; `DraftBoard` no longer competes for that phase.
- The host switch reaches `setRoomVisibility` and failures surface via the existing `actionError` line.
- A gated guest holds no lobby subscription and no chat subscription.
- Suite green; eslint no worse than the 20-error baseline.
</success_criteria>

<output>
Create `.planning/phases/12-access-control-secondary-screens/12-08-SUMMARY.md` when done. Record: the final branch order as shipped, the chosen floor and timeout constants, whether the `goto` import was removable from `DraftBoard.svelte`, the resulting `npx eslint src` error count, and any `svelte-autofixer` change it forced.
</output>
