---
phase: 09-signature-effects-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - src/lib/components/effects/CyLogo.svelte
  - src/lib/components/effects/CyLogo.svelte.spec.js
  - src/app.css
autonomous: true
requirements: [FX-04]
must_haves:
  truths:
    - "The shaded-ASCII DRAFT wordmark renders as 9 cy-logo-line spans with role=img and aria-label DRAFT"
    - "When rendered is true, the line-by-line reveal fires (is-rendered class) animating transform+brightness only, never opacity"
    - "The base .cy-logo is always visible; under prefers-reduced-motion the reveal/glitch are suppressed leaving the static wordmark"
  artifacts:
    - path: "src/lib/components/effects/CyLogo.svelte"
      provides: "Wordmark component: .cy-logo-wrap (role=img, aria-label=DRAFT, is-rendered when rendered prop), <pre class=cy-logo> of 9 verbatim CY_LOGO_LINES spans, .cy-logo-glitch ghost overlay (aria-hidden)"
      contains: "aria-label=\"DRAFT\""
      min_lines: 30
    - path: "src/lib/components/effects/CyLogo.svelte.spec.js"
      provides: "Browser tests: 9 spans + base visible, rendered→is-rendered triggers reveal, reveal never animates opacity, reduced-motion suppresses reveal/glitch leaving static wordmark"
    - path: "src/app.css"
      provides: ".cy-logo* rules + cy-logo-in + cy-glitch keyframes + 9 nth-child delays + reduced-motion suppression, all verbatim from cyber.css 131-178"
      contains: "@keyframes cy-logo-in"
  key_links:
    - from: "Phase 10 Home hero"
      to: "src/lib/components/effects/CyLogo.svelte"
      via: "<CyLogo rendered={bootLog.done} /> — reveal fires when boot log finishes"
      pattern: "rendered"
    - from: "src/lib/components/effects/CyLogo.svelte"
      to: "src/app.css .cy-logo-wrap.is-rendered"
      via: "is-rendered class toggled by the rendered prop drives the CSS reveal"
      pattern: "is-rendered"
---

<objective>
Build the shaded-ASCII `DRAFT` wordmark as a Svelte 5 component (`CyLogo.svelte`) rendering the 9-line `CY_LOGO_LINES` block-art with a line-by-line slide/glow reveal driven by a `rendered` prop, and add the `.cy-logo*` CSS (+ keyframes + reduced-motion block) to `src/app.css`.

This is a VERBATIM PORT of `cyber.jsx` §`CY_LOGO_LINES` + the `CYHome` reveal wiring, and `cyber.css` lines 131–178. The 9 block-art strings (every glyph + leading space), the `cy-logo-in` keyframe, the 9 nth-child delays, and the reduced-motion block are load-bearing.

Per D-01: the wordmark STAYS `DRAFT` (port CY_LOGO_LINES verbatim; aria-label="DRAFT"). No new ASCII-art authoring.

This plan is Wave 2 because it appends to `src/app.css` (a file also written by Plan 09-02). Depending on 09-02 serializes the app.css writes to avoid a conflict.

Purpose: Establishes FX-04. Phase 9 has NO runtime consumer (D-03) — the component is validated by browser tests and composed by Phase 10 Home as `<CyLogo rendered={bootLog.done} />`.
Output: `CyLogo.svelte`, its browser spec, and the `.cy-logo*` CSS.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-signature-effects-infrastructure/09-UI-SPEC.md
@.planning/phases/09-signature-effects-infrastructure/09-CONTEXT.md
@design_handoff_pickban_cyber/prototype/variants/cyber.jsx
@design_handoff_pickban_cyber/prototype/variants/cyber.css
@src/app.css

<interfaces>
<!-- Component contract. -->
CyLogo.svelte props:
  rendered: boolean = false   // when true, the wrap gets `is-rendered` to fire the reveal

Structure (mirrors cyber.jsx CYHome lines 242–251):
  <div class="cy-logo-wrap" class:is-rendered={rendered} role="img" aria-label="DRAFT">
    <pre class="cy-logo">{#each CY_LOGO_LINES as line}<span class="cy-logo-line">{line + "\n"}</span>{/each}</pre>
    <div class="cy-logo-glitch" aria-hidden="true"><pre>{CY_LOGO_LINES.join("\n")}</pre></div>
  </div>

The base `.cy-logo` is ALWAYS visible. The reveal animates transform + brightness ONLY, never opacity (fidelity guardrail — the logo can never get stuck hidden). The `rendered` prop is the only input; Phase 10 wires it to `bootLog.done`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .cy-logo* + keyframes CSS to app.css (verbatim, before component so the spec can assert the cascade)</name>
  <read_first>
    - design_handoff_pickban_cyber/prototype/variants/cyber.css lines 131–178 (the EXACT source: .cy-logo-wrap, .cy-logo, .cy-logo-line, the 9 nth-child delays, @keyframes cy-logo-in, .cy-logo-glitch, @keyframes cy-glitch, reduced-motion block)
    - .planning/phases/09-signature-effects-infrastructure/09-UI-SPEC.md "Effect 3 — Shaded-ASCII DRAFT Wordmark" (reveal animation table, motion-safety) + Verbatim-Port Checklist "Wordmark" block
    - src/app.css (the file being appended to; note an existing `@media (prefers-reduced-motion: reduce)` block already sits at the bottom — ADD a SECOND scoped block for the logo, or extend, but do NOT duplicate the existing .cy-brand-cur rule)
  </read_first>
  <files>src/app.css</files>
  <action>
Append the `.cy-logo*` block to `src/app.css` VERBATIM from cyber.css 131–178, under the `.cy-app` structural section (Phase 8 hybrid CSS org). Add exactly:

```css
/* ANSI wordmark — always visible (base state); the reveal animates a slide +
   glow flash only, never opacity, so the logo can never get stuck hidden. */
.cy-logo-wrap {
	position: relative;
}
.cy-logo {
	margin: 0;
	font-size: 13px;
	line-height: 1.05;
	font-weight: 700;
	white-space: pre;
	overflow-x: auto;
	overflow-y: hidden;
	color: var(--cy-lime);
	text-shadow:
		1px 0 var(--cy-violet),
		-1px 0 rgba(40, 200, 255, 0.55),
		0 0 12px rgba(179, 255, 61, 0.4);
}
.cy-logo-line {
	display: block;
}
.cy-logo-wrap.is-rendered .cy-logo-line {
	animation: cy-logo-in 0.5s cubic-bezier(0.2, 0.7, 0.3, 1) backwards;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(1) {
	animation-delay: 0.02s;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(2) {
	animation-delay: 0.08s;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(3) {
	animation-delay: 0.14s;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(4) {
	animation-delay: 0.2s;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(5) {
	animation-delay: 0.26s;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(6) {
	animation-delay: 0.32s;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(7) {
	animation-delay: 0.38s;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(8) {
	animation-delay: 0.44s;
}
.cy-logo-wrap.is-rendered .cy-logo-line:nth-child(9) {
	animation-delay: 0.5s;
}
@keyframes cy-logo-in {
	from {
		transform: translateX(-12px);
		filter: brightness(2.6);
	}
	55% {
		filter: brightness(1.5);
	}
	to {
		transform: translateX(0);
		filter: brightness(1);
	}
}
/* occasional glitch ghost overlay once rendered */
.cy-logo-glitch {
	position: absolute;
	inset: 0;
	pointer-events: none;
	color: var(--cy-violet);
	opacity: 0;
	mix-blend-mode: screen;
}
.cy-logo-glitch pre {
	margin: 0;
	font-size: 13px;
	line-height: 1.05;
	font-weight: 700;
	white-space: pre;
}
.cy-logo-wrap.is-rendered .cy-logo-glitch {
	animation: cy-glitch 6.5s steps(1) infinite;
	animation-delay: 1.2s;
}
@keyframes cy-glitch {
	0%,
	96%,
	100% {
		opacity: 0;
		transform: translate(0, 0);
	}
	96.5% {
		opacity: 0.7;
		transform: translate(3px, -1px);
	}
	97.5% {
		opacity: 0;
	}
	98% {
		opacity: 0.5;
		transform: translate(-2px, 1px);
	}
	99% {
		opacity: 0;
	}
}
```

Then add the wordmark reduced-motion suppression VERBATIM (cyber.css 175–178). The existing reduced-motion `@media` block in app.css only handles `.cy-brand-cur`; add the logo rules to a reduced-motion block (either extend the existing block or add a new one — do NOT remove or alter the existing `.cy-brand-cur { animation: none; }` rule):
```css
@media (prefers-reduced-motion: reduce) {
	.cy-logo-wrap.is-rendered .cy-logo-line {
		animation: none;
	}
	.cy-logo-glitch {
		animation: none !important;
	}
}
```

Critical fidelity guardrail: the reveal animates `transform` + `filter: brightness()` ONLY — there is NO opacity in `@keyframes cy-logo-in`. Do NOT add opacity. The base `.cy-logo` carries no opacity:0 — it is always visible. DS-04: no border-radius introduced.

Run prettier formatting consistent with the file (tabs, as the existing app.css uses). Do NOT alter existing rules.
  </action>
  <verify>
    <automated>npx vitest run --project server src/phase8-foundation.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - src/app.css contains `@keyframes cy-logo-in`
    - The `cy-logo-in` keyframe contains `transform: translateX(-12px)` and `filter: brightness(2.6)` and `filter: brightness(1)` and does NOT contain `opacity` inside the cy-logo-in keyframe body
    - src/app.css contains all 9 `:nth-child` delays: 0.02s, 0.08s, 0.14s, 0.2s, 0.26s, 0.32s, 0.38s, 0.44s, 0.5s
    - src/app.css contains `.cy-logo` with `color: var(--cy-lime)` and the verbatim `text-shadow` (1px 0 var(--cy-violet), -1px 0 rgba(40, 200, 255, 0.55), 0 0 12px rgba(179, 255, 61, 0.4))
    - src/app.css contains `@keyframes cy-glitch` and `.cy-logo-wrap.is-rendered .cy-logo-glitch` with `animation-delay: 1.2s` and `6.5s steps(1) infinite`
    - src/app.css reduced-motion block contains `.cy-logo-wrap.is-rendered .cy-logo-line { animation: none; }` and `.cy-logo-glitch { animation: none !important; }`
    - The existing `.cy-brand-cur { animation: none; }` reduced-motion rule still present (no regression)
    - `grep -c 'border-radius' src/app.css` is 0
    - `npx vitest run --project server src/phase8-foundation.spec.js` exits 0
  </acceptance_criteria>
  <done>All .cy-logo* CSS, both keyframes, the 9 nth-child delays, and the reduced-motion suppression are in app.css verbatim. The reveal animates transform+brightness only (never opacity). Phase 8 grep assertions still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create CyLogo.svelte + browser tests (D-03)</name>
  <read_first>
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx lines 10–20 (CY_LOGO_LINES — copy all 9 strings VERBATIM incl. leading spaces) and lines 242–251 (CYHome reveal markup: cy-logo-wrap + is-rendered + role/aria-label, the per-line spans, the glitch ghost)
    - .planning/phases/09-signature-effects-infrastructure/09-UI-SPEC.md "Effect 3" component contract + "Block-art content (literal — CY_LOGO_LINES, copy VERBATIM, 9 lines)"
    - src/lib/components/chrome/CyShell.svelte.spec.js (browser test harness conventions)
    - src/lib/components/atoms/Phases.svelte (Svelte 5 runes #each + class: directive conventions)
    - src/app.css (the .cy-logo* cascade from Task 1, so the spec can assert is-rendered behavior)
  </read_first>
  <files>src/lib/components/effects/CyLogo.svelte, src/lib/components/effects/CyLogo.svelte.spec.js</files>
  <action>
Create `src/lib/components/effects/CyLogo.svelte`.

Define `CY_LOGO_LINES` VERBATIM (copy all 9 strings exactly from cyber.jsx 11–19 — preserve EVERY glyph and leading/trailing space; these ARE the art):
```js
const CY_LOGO_LINES = [
  " ▓█████▄  ██▀███   ▄▄▄        █████▒▄▄▄█████▓",
  " ▒██▀ ██▌▓██ ▒ ██▒▒████▄    ▓██   ▒▓  ██▒ ▓▒",
  " ░██   █▌▓██ ░▄█ ▒▒██  ▀█▄  ▒████ ░▒ ▓██░ ▒░",
  " ░▓█▄   ▌▒██▀▀█▄  ░██▄▄▄▄██ ░▓█▒  ░░ ▓██▓ ░ ",
  " ░▒████▓ ░██▓ ▒██▒ ▓█   ▓██▒░▒█░     ▒██▒ ░ ",
  "  ░▒▒▓  ▒ ░ ▒▓ ░▒▓░ ▒▒   ▓▒█░ ▒ ░     ▒ ░░   ",
  "  ░ ▒  ▒   ░▒ ░ ▒░  ▒   ▒▒ ░ ░         ░    ",
  "  ░ ░  ░   ░░   ░   ░   ▒    ░ ░     ░      ",
  "    ░       ░           ░  ░               ",
];
```

Props:
```js
let { rendered = false } = $props();
```

Markup (mirror cyber.jsx CYHome 242–251; each span's text is `line + "\n"`; the glitch overlay is the joined art, aria-hidden):
```svelte
<div class="cy-logo-wrap" class:is-rendered={rendered} role="img" aria-label="DRAFT">
	<pre class="cy-logo">{#each CY_LOGO_LINES as line}<span class="cy-logo-line">{line + '\n'}</span>{/each}</pre>
	<div class="cy-logo-glitch" aria-hidden="true"><pre>{CY_LOGO_LINES.join('\n')}</pre></div>
</div>
```
No `<style>` block — all `.cy-logo*` CSS lives in app.css (Task 1). Do NOT animate opacity anywhere. Run the Svelte MCP `svelte-autofixer` on the component and resolve all issues/suggestions (note: `role="img"` with `aria-label` on a div is intentional and correct for decorative-as-text ASCII).

Create `src/lib/components/effects/CyLogo.svelte.spec.js` using the vitest-browser-svelte harness (D-03 wordmark assertions):
1. `it('renders 9 cy-logo-line spans inside role=img aria-label DRAFT, base always visible')`:
   - `render(CyLogo)`. Assert `document.querySelectorAll('.cy-logo-line').length === 9`. Assert the wrap has `role="img"` and `aria-label="DRAFT"` (e.g. `page.getByRole('img', { name: 'DRAFT' })`). Assert `.cy-logo` exists and its computed `opacity` is not 0 (base always visible — `getComputedStyle(el).opacity !== '0'`).
2. `it('rendered prop toggles is-rendered to fire the reveal')`:
   - `render(CyLogo, { rendered: false })` → assert `.cy-logo-wrap.is-rendered` is null. Re-render / use a rerender helper with `rendered: true` → assert `document.querySelector('.cy-logo-wrap.is-rendered')` is not null. (If the harness lacks rerender, render two instances or use `render(CyLogo, { rendered: true })` in a second test asserting `.is-rendered` present.)
3. `it('reveal never animates opacity — base wordmark stays visible')`:
   - With `rendered: true`, assert the `.cy-logo` element's computed opacity is `'1'` (never animates to 0). This guards the fidelity rule that the reveal uses transform+brightness only.
4. `it('reduced-motion suppresses reveal/glitch leaving the static wordmark')`:
   - Emulate reduced motion (`await page.emulateMedia?.({ reducedMotion: 'reduce' })` if available with the Playwright provider; this is a CSS-media concern so emulateMedia is the right tool — the suppression is pure CSS). `render(CyLogo, { rendered: true })`. Assert the 9 spans are still present and `.cy-logo` opacity is `'1'` (static wordmark visible). Assert the reveal animation is suppressed: `getComputedStyle(document.querySelector('.cy-logo-line')).animationName === 'none'` (the reduced-motion block sets `animation: none`). If `page.emulateMedia` is unavailable in this provider, assert the CSS contract instead by reading app.css for the reduced-motion `.cy-logo-line { animation: none }` rule — but PREFER emulateMedia for a true behavioral assertion.

Each `it` MUST have ≥1 assertion. Run the client project for this spec, then the node project for the foundation spec.
  </action>
  <verify>
    <automated>npx vitest run --project client src/lib/components/effects/CyLogo.svelte.spec.js && npx vitest run --project server src/phase8-foundation.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/components/effects/CyLogo.svelte exists
    - File contains `CY_LOGO_LINES` with exactly 9 entries (the first line contains `▓█████▄` and the last is the `░` tail line)
    - File contains `aria-label="DRAFT"` and `role="img"`
    - File contains `class:is-rendered={rendered}`
    - File contains `class="cy-logo-line"` rendered via `{#each CY_LOGO_LINES`
    - File contains the `.cy-logo-glitch` overlay with `aria-hidden="true"` wrapping `CY_LOGO_LINES.join('\n')`
    - File contains NO `<style>` block and NO `opacity` animation
    - File src/lib/components/effects/CyLogo.svelte.spec.js exists
    - Spec asserts `.cy-logo-line` count is 9
    - Spec asserts `role=img` + name `DRAFT` (getByRole img)
    - Spec asserts the rendered prop toggles `.is-rendered`
    - Spec asserts `.cy-logo` computed opacity is `'1'` (base always visible / reveal never animates opacity)
    - Spec has a reduced-motion test asserting the static wordmark remains and the reveal animation is suppressed
    - svelte-autofixer reports no remaining issues
    - `npx vitest run --project client src/lib/components/effects/CyLogo.svelte.spec.js` exits 0
    - `npx vitest run --project server src/phase8-foundation.spec.js` exits 0
  </acceptance_criteria>
  <done>CyLogo renders the 9 verbatim block-art lines as cy-logo-line spans inside a role=img/aria-label=DRAFT wrap with the glitch ghost; the rendered prop drives is-rendered; the base wordmark is always visible (no opacity animation); reduced motion leaves the static wordmark with reveal/glitch suppressed. Browser tests pass; Phase 8 spec still green.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project client src/lib/components/effects/CyLogo.svelte.spec.js` exits 0.
- `npx vitest run --project server src/phase8-foundation.spec.js` exits 0 (app.css edit safe).
- app.css contains the verbatim cy-logo-in + cy-glitch keyframes, 9 nth-child delays, .cy-logo text-shadow, and the reduced-motion suppression block.
- CyLogo.svelte ports the 9 CY_LOGO_LINES verbatim with role=img/aria-label="DRAFT", is-rendered driven by the rendered prop, and no opacity animation.
- No realtime/snapshot import in CyLogo.svelte.
</verification>

<success_criteria>
FX-04: The shaded-ASCII DRAFT wordmark renders with its line-by-line reveal (9 cy-logo-line spans, is-rendered triggers cy-logo-in stagger animating transform+brightness only). Base .cy-logo always visible; reduced motion leaves the fully-formed static wordmark with reveal/glitch suppressed (D-01).
</success_criteria>

<output>
After completion, create `.planning/phases/09-signature-effects-infrastructure/09-03-SUMMARY.md`.
</output>
