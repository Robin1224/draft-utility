---
phase: 09-signature-effects-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/effects/cyTypedLog.svelte.js
  - src/lib/components/effects/cyTypedLog.svelte.spec.js
  - src/app.css
autonomous: true
requirements: [FX-03]
must_haves:
  truths:
    - "A terminal log types out character-by-character via a reusable rune-based utility"
    - "The typed log instantly shows the full text (done=true) when prefers-reduced-motion is set — no typing"
    - "The pending timer is cleared on teardown (no leaked setTimeout)"
    - "The .cy-boot CSS reserves layout space so lines do not jump as they type in"
  artifacts:
    - path: "src/lib/components/effects/cyTypedLog.svelte.js"
      provides: "createTypedLog(lines, opts) rune factory exposing reactive { text, done }; reduced-motion jump-to-full; clearTimeout teardown; SSR matchMedia guard. Exports CY_BOOT_LINES reference content."
      contains: "export function createTypedLog"
      min_lines: 40
    - path: "src/lib/components/effects/cyTypedLog.svelte.spec.js"
      provides: "Browser tests: char-by-char progression, done flip, reduced-motion immediate full text, clearTimeout teardown"
    - path: "src/app.css"
      provides: ".cy-boot rule (font-size 12px, line-height 1.55, color var(--cy-text-2), min-height 9.3em, margin-bottom 18px)"
      contains: ".cy-boot"
  key_links:
    - from: "consumer screen (Phase 10 Home / Phase 12 Connecting)"
      to: "src/lib/components/effects/cyTypedLog.svelte.js"
      via: "createTypedLog(lines, opts) → reads .text / .done reactively"
      pattern: "createTypedLog"
---

<objective>
Build the reusable typed terminal-log mechanism as a rune-based `.svelte.js` factory (`createTypedLog(lines, opts)`) that prints `string[]` character-by-character and exposes reactive `{ text, done }`, jumping straight to full text under `prefers-reduced-motion` (FX-03). Also add the `.cy-boot` CSS to `src/app.css`.

This is a VERBATIM PORT of `cyber.jsx` §`useTypedLog` (React hook → Svelte 5 rune factory). The timing constants are load-bearing.

Decision (planner's discretion per UI-SPEC): implement as a `.svelte.js` rune factory (NOT a component wrapper) so it is composable by both the Phase 10 Home boot log (default opts) and the Phase 12 connect log (`{ speed: 7, lineGap: 120 }`). Phase 9 has NO runtime consumer — the factory is validated by browser component tests (D-03) and consumed downstream.

Purpose: Establishes FX-03. Output: `cyTypedLog.svelte.js`, its browser spec, and the `.cy-boot` CSS.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-signature-effects-infrastructure/09-UI-SPEC.md
@.planning/phases/09-signature-effects-infrastructure/09-CONTEXT.md
@design_handoff_pickban_cyber/prototype/variants/cyber.jsx
@src/app.css

<interfaces>
<!-- The factory contract. Reactive state must be readable as live getters. -->
createTypedLog(lines: string[], opts?: { speed?: number, lineGap?: number })
  → returns an object exposing reactive getters:
       get text(): string   // full.slice(0, n)
       get done(): boolean  // n >= full.length
  Internally: full = lines.join("\n").
  Defaults: speed = 9 (ms/char), lineGap = 60 (ms after a newline).
  Initial delay before typing starts: 300 ms.
  Reduced motion: initialize n = full.length so text===full and done===true immediately, and DO NOT start the timer.

Because the timed state must be reactive AND torn down, the factory uses `$state` for `n` and an internal `$effect` (created with `$effect.root` so it can be called from a factory and disposed) OR returns a `start()`/`stop()` pair. Preferred shape: the factory creates an `$effect.root` that owns the setTimeout chain and is auto-cleaned; expose a `stop()` that disposes the root. The reduced-motion branch sets n to full.length and never schedules a timer. Mirror React's `() => clearTimeout(timer)` cleanup.
</interfaces>

<svelte5_note>
React `useState(reduced ? full.length : 0)` + `useEffect(..., [full, reduced, speed, lineGap])` returning `() => clearTimeout(timer)` maps to: a `$state` count, an `$effect` (inside `$effect.root`) that schedules the setTimeout chain and returns a teardown calling `clearTimeout`. Guard `typeof window !== "undefined" && window.matchMedia`. Use the Svelte MCP `list-sections` → `get-documentation` for "$effect", "$effect.root", and "$state" before implementing, and run `svelte-autofixer` if any `.svelte` markup is produced (none expected for a `.svelte.js` module, but verify the rune usage compiles).
</svelte5_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cyTypedLog.svelte.js — verbatim typed-log rune factory</name>
  <read_first>
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx lines 32–57 (§useTypedLog — exact source) and lines 22–30 (CY_BOOT_LINES content) and lines 645–656 (CY_CONNECT_LINES + the {speed:7,lineGap:120} override, for the reusability contract — do NOT build connect content here)
    - .planning/phases/09-signature-effects-infrastructure/09-UI-SPEC.md "Effect 2 — Typed Terminal Log" (timing table, stepping behavior, motion-safety obligations, boot-log content) + Verbatim-Port Checklist "Typed log" block
  </read_first>
  <files>src/lib/components/effects/cyTypedLog.svelte.js</files>
  <behavior>
    Encoded by the browser spec in Task 2 (write RED first, implement to GREEN):
    - Test 1: createTypedLog(["ab"]) → initially text is "" (or 1 char after first tick) and done is false; after enough time, text === "ab" and done === true.
    - Test 2: char-by-char progression — text length increases monotonically over time toward full.length.
    - Test 3: newline pause — a multi-line input applies lineGap after the newline char (verifiable by total elapsed time being longer than speed*chars when a newline is present; primary assertion is correctness of final text).
    - Test 4: reduced-motion → text === full and done === true IMMEDIATELY (synchronously after creation, no waiting), and NO timer scheduled.
    - Test 5: stop()/teardown clears the pending timer (spy clearTimeout is called on disposal).
  </behavior>
  <action>
Create `src/lib/components/effects/cyTypedLog.svelte.js`.

Export the boot-log reference content VERBATIM (copy from cyber.jsx lines 23–30 — final boot copy is a Phase 10 decision, this is the reference default):
```js
export const CY_BOOT_LINES = [
  "$ ./draftnet --init",
  "[ OK ] mounting entity catalog ......... 28 found",
  "[ OK ] websocket bridge ................ connected",
  "[ OK ] chat filter ..................... armed",
  "[ OK ] render protocol ................. ready",
  "> booting interface_",
];
```

Export the factory. Transcribe the timing + stepping VERBATIM from §useTypedLog:
```js
export function createTypedLog(lines, { speed = 9, lineGap = 60 } = {}) {
  const full = lines.join("\n");
  const reduced =
    typeof window !== "undefined" &&
    window.matchMedia &&
    window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  let n = $state(reduced ? full.length : 0);

  // Own the timer chain inside an $effect.root so the factory can be called
  // outside component init and disposed deterministically.
  const dispose = $effect.root(() => {
    $effect(() => {
      if (reduced) return;             // jump-to-full already set via initial n
      let i = 0;
      let timer;
      const step = () => {
        i += 1;
        n = i;
        if (i >= full.length) return;
        const justTypedNewline = full[i - 1] === "\n";
        timer = setTimeout(step, justTypedNewline ? lineGap : speed);
      };
      timer = setTimeout(step, 300);   // initial 300ms delay
      return () => clearTimeout(timer); // teardown — mirror React cleanup
    });
  });

  return {
    get text() { return full.slice(0, n); },
    get done() { return n >= full.length; },
    stop: dispose,
  };
}
```
Key verbatim values that MUST match: `speed = 9`, `lineGap = 60`, initial `setTimeout(step, 300)`, newline → `lineGap` else `speed`, `full = lines.join("\n")`, reduced → `n = full.length` (no timer). The `text` getter is `full.slice(0, n)`; `done` is `n >= full.length`.

Do NOT import or touch the realtime/snapshot layer. This module is presentational.

If the runtime requires the factory to be created during component init for `$state`/`$effect` to bind (some Svelte versions warn on `$effect.root` outside a tracking context), the spec in Task 2 drives the factory from inside a tiny test harness component — keep the public factory signature `createTypedLog(lines, opts)` regardless. Verify rune usage compiles (svelte-check / the test run will surface compile errors).
  </action>
  <verify>
    <automated>npx vitest run --project client src/lib/components/effects/cyTypedLog.svelte.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/components/effects/cyTypedLog.svelte.js exists
    - File contains `export function createTypedLog`
    - File contains `{ speed = 9, lineGap = 60 } = {}` (verbatim defaults)
    - File contains `lines.join("\n")`
    - File contains `setTimeout(step, 300)` (initial delay)
    - File contains `justTypedNewline ? lineGap : speed`
    - File contains `full[i - 1] === "\n"`
    - File contains `reduced ? full.length : 0`
    - File contains `clearTimeout` in a teardown/return
    - File contains `typeof window !== "undefined"` and `matchMedia("(prefers-reduced-motion: reduce)")`
    - File exports `CY_BOOT_LINES` with the 6 verbatim lines (grep `28 found` and `> booting interface_`)
    - File does NOT import from the realtime/snapshot layer (no `svelte-realtime`, no snapshot store import)
  </acceptance_criteria>
  <done>createTypedLog ports the useTypedLog timing/stepping verbatim, exposes reactive { text, done }, jumps to full text + done under reduced motion with no timer, and clears its timer on disposal. CY_BOOT_LINES exported verbatim. Task 2 spec passes.</done>
</task>

<task type="auto">
  <name>Task 2: Browser tests for cyTypedLog (D-03) + add .cy-boot CSS verbatim</name>
  <read_first>
    - src/lib/components/chrome/CyShell.svelte.spec.js (the vitest-browser-svelte harness conventions, fake timers via `vi.useFakeTimers()`, `vi` spying)
    - src/lib/components/effects/cyTypedLog.svelte.js (the factory under test, from Task 1)
    - .planning/phases/09-signature-effects-infrastructure/09-CONTEXT.md D-03 (typed-log assertions to encode)
    - design_handoff_pickban_cyber/prototype/variants/cyber.css lines 126–129 (.cy-boot verbatim source)
    - src/app.css (the file being appended to — see Phase 8 hybrid CSS org under .cy-app; add .cy-boot near the other structural .cy-* classes)
  </read_first>
  <files>src/lib/components/effects/cyTypedLog.svelte.spec.js, src/app.css</files>
  <action>
Create `src/lib/components/effects/cyTypedLog.svelte.spec.js` (D-03 typed-log verification — component tests only).

Use `vi.useFakeTimers()` to drive the setTimeout chain deterministically, and `vi.useRealTimers()` in `afterEach`. Because the factory uses `$state`/`$effect.root`, drive it from a minimal harness: either (a) instantiate `createTypedLog` inside a `.svelte` test harness component rendered via `vitest-browser-svelte`, or (b) call `createTypedLog` directly if the runtime permits `$effect.root` at module call time. Prefer the approach that the runtime accepts; if `$effect.root` must run inside an effect context, wrap it in a tiny inline harness component in the spec. Each `it` MUST have ≥1 assertion (`requireAssertions: true`).

Tests (encode the D-03 typed-log assertions):
1. `it('types out character-by-character then flips done')`:
   - `vi.useFakeTimers()`. Create `const log = createTypedLog(["ab"])`. Assert `log.done === false` initially. Advance timers (`vi.advanceTimersByTime(300)` then per-char `speed`=9ms steps) and assert `log.text` grows toward `"ab"` and finally `log.text === "ab"` and `log.done === true`.
2. `it('applies lineGap after a newline')`:
   - Create `createTypedLog(["a", "b"])` → full = "a\nb". Advance past initial 300ms + first char; assert that the gap before the char following the newline uses lineGap (60ms) not speed (9ms) — e.g. after typing through the newline, advancing only `speed` ms does NOT yet reveal "b", but advancing `lineGap` ms does. Final assertion: full text === "a\nb", done true.
3. `it('jumps straight to full text + done under reduced motion (no typing)')`:
   - Stub reduced motion BEFORE creating the factory: `vi.stubGlobal('matchMedia', (q) => ({ matches: /reduce/.test(q), media: q, addEventListener(){}, removeEventListener(){} }))` (or `page.emulateMedia({ reducedMotion: 'reduce' })` if the Playwright provider exposes it). Create `const log = createTypedLog(["hello","world"])`. Assert SYNCHRONOUSLY (no timer advance) that `log.text === "hello\nworld"` and `log.done === true`. `vi.unstubAllGlobals()` in afterEach.
4. `it('clears the pending timer on stop()/teardown')`:
   - `vi.useFakeTimers()`. `const clearSpy = vi.spyOn(globalThis, 'clearTimeout')`. Create the log, advance a little so a timer is pending, then call `log.stop()` (or unmount the harness). Assert `clearSpy` was called. Restore in afterEach.

Then ADD the `.cy-boot` CSS to `src/app.css` VERBATIM (from cyber.css 126–129), placed under the `.cy-app`-scoped structural section (near the existing `.cy-shader`/`.cy-scanlines` block, consistent with Phase 8 hybrid CSS org). Append exactly:
```css
.cy-boot {
	white-space: pre-wrap;
	font-size: 12px;
	line-height: 1.55;
	color: var(--cy-text-2);
	min-height: 9.3em;
	margin-bottom: 18px;
}
```
(The `min-height: 9.3em` reserves space so the layout does not jump as lines type in.) Do NOT duplicate or alter any existing rule. Do NOT introduce any border-radius (DS-04).

Run the client project for this spec. Also run the node project for the foundation spec to confirm the app.css edit did not break the Phase 8 string-grep assertions.
  </action>
  <verify>
    <automated>npx vitest run --project client src/lib/components/effects/cyTypedLog.svelte.spec.js && npx vitest run --project server src/phase8-foundation.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/components/effects/cyTypedLog.svelte.spec.js exists
    - Spec contains a char-by-char progression test that ends asserting `done === true` and the full text
    - Spec contains a reduced-motion test asserting full text + done SYNCHRONOUSLY (no timer advance)
    - Spec contains a teardown test spying on `clearTimeout`
    - Spec uses `vi.useFakeTimers()` (deterministic timing)
    - src/app.css contains `.cy-boot` with `min-height: 9.3em` and `margin-bottom: 18px` and `line-height: 1.55` and `color: var(--cy-text-2)`
    - src/app.css still contains all Phase 8 tokens (no regression): `grep -q -- '--cy-bg: #050409' src/app.css`
    - `grep -c 'border-radius' src/app.css` is 0 (DS-04 preserved)
    - `npx vitest run --project client src/lib/components/effects/cyTypedLog.svelte.spec.js` exits 0
    - `npx vitest run --project server src/phase8-foundation.spec.js` exits 0 (Phase 8 grep assertions still pass)
  </acceptance_criteria>
  <done>The typed-log factory is proven by browser tests (progression, lineGap, reduced-motion jump, clearTimeout teardown) and the verbatim .cy-boot CSS is in app.css with no Phase 8 regression.</done>
</task>

</tasks>

<verification>
- `npx vitest run --project client src/lib/components/effects/cyTypedLog.svelte.spec.js` exits 0.
- `npx vitest run --project server src/phase8-foundation.spec.js` exits 0 (app.css edit safe).
- cyTypedLog.svelte.js contains verbatim timing: speed 9, lineGap 60, initial 300ms, newline→lineGap, reduced→full.length, clearTimeout teardown.
- app.css contains the verbatim `.cy-boot` rule.
- No realtime/snapshot import in cyTypedLog.svelte.js.
</verification>

<success_criteria>
FX-03: A terminal log types out character-by-character (via createTypedLog) and instantly shows the full text (done=true) when prefers-reduced-motion is set. The mechanism is reusable across Home boot log (default opts) and the Phase 12 connect log (speed 7, lineGap 120). The .cy-boot CSS reserves layout space (min-height 9.3em) so lines do not jump as they type.
</success_criteria>

<output>
After completion, create `.planning/phases/09-signature-effects-infrastructure/09-02-SUMMARY.md`.
</output>
