---
phase: 10-core-screen-reskins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.css
  - src/phase10-screens.spec.js
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06]
must_haves:
  truths:
    - "Every per-screen Cyber CSS class the six screens reference exists in src/app.css under .cy-app"
    - "Ported values match cyber.css verbatim (colors, spacing, sizes are load-bearing for fidelity)"
    - "The already-shipped Phase 8/9 foundation (.cy-boot, .cy-logo*, .cy-shader*, cy-blink, tokens) is NOT duplicated"
    - "Phase 11/12 styles (loading/gate/cancel/modal/stepper/script/host-console — cyber.css 465+) are NOT ported"
  artifacts:
    - path: "src/app.css"
      provides: "All Phase-10 per-screen Cyber CSS (home/login/lobby/draft/chat/review/pause)"
      contains: ".cy-home"
    - path: "src/phase10-screens.spec.js"
      provides: "Node string-grep spec asserting the ported CSS exists verbatim"
      exports: []
  key_links:
    - from: "src/app.css"
      to: "cyber.css lines 88-463"
      via: "verbatim port into .cy-app scope"
      pattern: "\\.cy-home|\\.cy-login|\\.cy-lobby|\\.cy-draft-grid|\\.cy-review|\\.cy-pause"
---

<objective>
Port every per-screen Cyber CSS block the six Phase-10 screens depend on (Home, Login, Lobby, Drafting, Chat, Review, Pause) from `design_handoff_pickban_cyber/prototype/variants/cyber.css` into `src/app.css` under the existing `.cy-app` scope, verbatim. This is the shared stylesheet foundation that unblocks all screen reskins (Plans 02-07).

Purpose: Plans 02-07 restyle components by adding `.cy-*` class names; those classes must already exist in the global stylesheet. Centralizing the CSS port here (one owner of `src/app.css`) lets the screen plans run in parallel without stylesheet conflicts.
Output: `src/app.css` extended with the Phase-10 screen blocks; a node string-grep spec proving the port landed verbatim.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-screen-reskins/10-CONTEXT.md
@.planning/phases/10-core-screen-reskins/10-UI-SPEC.md

# Source of truth for the CSS values (port verbatim)
@design_handoff_pickban_cyber/prototype/variants/cyber.css

# Existing foundation — do NOT duplicate these blocks
@src/app.css

# Existing string-grep spec pattern to mirror (node project)
@src/phase8-foundation.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Home + Login + Lobby Cyber CSS into src/app.css</name>
  <read_first>
    - src/app.css (current foundation — APPEND only; find the end of the existing screen-agnostic blocks; do NOT touch the token/@font-face/.cy-shader*/.cy-boot/.cy-logo*/cy-blink/reduced-motion blocks)
    - design_handoff_pickban_cyber/prototype/variants/cyber.css (lines 62-302 — buttons/inputs, HOME, LOGIN, LOBBY)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (CSS Organization section — verbatim-fidelity contract)
  </read_first>
  <action>
    Append the following cyber.css blocks to src/app.css VERBATIM (copy the rules byte-for-byte; do NOT normalize spacing values, do NOT change hex/px values — they are the fidelity contract). All rules are already written against `.cy-*` selectors which live inside the `.cy-app` scope, so paste them as-is.

    Port these blocks from cyber.css:
    - Buttons/inputs (cyber.css lines 62-86): `.cy-btn`, `.cy-btn:hover`, `.cy-btn-block`, `.cy-btn-sm`, `.cy-btn-primary`, `.cy-btn-primary:hover`, `.cy-btn-lime`, `.cy-btn-violet`, `.cy-btn-danger`, `.cy-btn-danger:hover`, `.cy-btn-ghost`, `.cy-btn-discord`, `.cy-input`, `.cy-input:focus`. (Note: `.cy-btn-{accent}-block` referenced by the Lobby join CTA is achieved by composing `cy-btn cy-btn-lime cy-btn-block` per cyber.jsx — no separate rule needed.)
    - HOME (cyber.css lines 88-232): `.cy-home`, `.cy-hero`, `.cy-frame`, `.cy-frame-border`, `.cy-frame-meas`, `.cy-frame-edge`, `.cy-frame-top`, `.cy-frame-bottom`, `.cy-frame-side`, `.cy-frame-label`, `.cy-hero-tagline`, `.cy-prompt`, `.cy-slash`, `.cy-home-grid`, `.cy-card` (+ `button.cy-card`, `:hover`, `::before`, `::after`), `.cy-card-head`, `.cy-card-num`, `.cy-card-title`, `.cy-card-prompt`, `.cy-card-rule`, `.cy-card-desc`, `.cy-card-action`, `.cy-card-btn` (+ hover combinations), `.cy-input-wrap`, `.cy-input-prompt`, `.cy-input-bare` (+ `:focus`), `.cy-input-wrap:focus-within`.
      DO NOT re-add `.cy-boot`, `.cy-logo`, `.cy-logo-wrap`, `.cy-logo-line`, `.cy-logo-glitch`, `@keyframes cy-logo-in`, `@keyframes cy-glitch`, or their reduced-motion block — those are ALREADY in src/app.css from Phase 9. (cyber.css lines 126-178 — SKIP, already shipped. The `.cy-frame*` rules around them ARE new — port those.)
    - LOGIN (cyber.css lines 234-246): `.cy-login`, `.cy-login-card`, `.cy-login-eyebrow`, `.cy-login-card h2`, `.cy-login-card p`, `.cy-divider`, `.cy-foot`.
    - LOBBY (cyber.css lines 248-301): `.cy-lobby`, `.cy-banner` (+ eyebrow/h2/p), `.cy-banner-stats` (+ `> div`, `b`, `span`), `.cy-host-panel`, `.cy-host-head`, `.cy-host-row` (+ `.cy-input`), `.cy-grow`, `.cy-lobby-grid`, `.cy-team`, `.cy-team-lime`, `.cy-team-violet`, `.cy-team-head`, `.cy-bracket`, `.cy-team-status`, `.cy-slot-list`, `.cy-slot`, `.cy-slot-empty`, `.cy-slot-num`, `.cy-slot-name`, `.cy-tag`, `.cy-tag-cap`, `.cy-spec`, `.cy-spec-head`, `.cy-spec-list`, `.cy-spec-pill`.

    Add a comment header `/* ============ PHASE 10 — SCREEN RESKINS ============ */` before the first appended block so the new section is greppable.

    DO NOT introduce any `border-radius` (DS-04: zero radius — the phase8-foundation spec asserts src/app.css has no border-radius; preserve that by copying cyber.css which has none in these ranges).
  </action>
  <verify>
    <automated>npm run test -- --run src/phase8-foundation.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "border-radius" src/app.css` returns 0 (DS-04 preserved)
    - `grep "\.cy-home {" src/app.css` matches (HOME block ported)
    - `grep "\.cy-login-card {" src/app.css` matches
    - `grep "\.cy-lobby {" src/app.css` matches
    - `grep "\.cy-team-lime" src/app.css` and `grep "\.cy-team-violet" src/app.css` both match
    - `grep "\.cy-slot-empty" src/app.css` matches
    - `grep "padding: 40px 32px 24px" src/app.css` matches (cy-home verbatim spacing, UI-SPEC exception)
    - `grep -c "@font-face" src/app.css` still returns 4 (Phase 8 foundation untouched)
    - phase8-foundation.spec.js still exits 0
  </acceptance_criteria>
  <done>Home, Login, and Lobby Cyber CSS blocks are present verbatim in src/app.css; Phase 8 foundation assertions still pass; no border-radius introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Port Drafting + Chat + Review + Pause Cyber CSS into src/app.css</name>
  <read_first>
    - src/app.css (after Task 1 — continue appending)
    - design_handoff_pickban_cyber/prototype/variants/cyber.css (lines 303-463 — DRAFTING, CHAT, REVIEW, PAUSE). Lines 465+ are Phase 11/12 — DO NOT port.
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Interaction & State Contract — pick/ban/urgency/chat dock)
  </read_first>
  <action>
    Append the following cyber.css blocks to src/app.css VERBATIM, continuing the PHASE 10 section. Stop at cyber.css line 463 — DO NOT port the "NEW SCREENS" block (loading/gate/cancel) or the modal/stepper/script/host-console blocks (lines 465-600); those belong to Phases 11/12.

    Port these blocks from cyber.css:
    - DRAFTING (cyber.css lines 303-403): `.cy-draft`, `.cy-turn`, `.cy-turn-lime`, `.cy-turn-violet`, `.cy-turn-readout`, `.cy-turn-label`, `.cy-turn-team`, `.cy-turn-cap`, `.cy-turn-clock`, `.cy-turn-clock-num` (+ `span`), `.cy-turn-clock.is-urgent .cy-turn-clock-num`, `@keyframes cy-pulse`, `.cy-turn-clock-bar` (+ `> div`), `.cy-turn-pips`, `.cy-pip`, `.cy-pip.is-done.cy-pip-lime`, `.cy-pip.is-done.cy-pip-violet`, `.cy-pip.is-active`, `.cy-draft-grid`, `.cy-draft-col`, `.cy-draft-col-lime`, `.cy-draft-col-violet`, `.cy-draft-col-head`, `.cy-draft-col-section`, `.cy-draft-col-label`, `.cy-pickslot`, `.cy-pickslot-empty`, `.cy-pickslot-empty.is-active`, `.cy-pickslot-lime.is-active`, `.cy-pickslot-violet.is-active`, `.cy-pickslot-action`, `.cy-pickslot-empty.is-active .cy-pickslot-action`, `.cy-pickslot-fill`, `.cy-pickslot-empty.is-active .cy-pickslot-fill`, `.cy-pickslot-art`, `.cy-pickslot-name`, `.cy-pickslot.is-ban`, `.cy-pickslot.is-ban .cy-pickslot-name`, `.cy-roster`, `.cy-roster-head` (+ `h3`), `.cy-roster-filters`, `.cy-chip`, `.cy-chip.is-active`, `.cy-champ-grid`, `.cy-champ`, `.cy-champ:hover:not([disabled])`, `.cy-champ-selected`, `.cy-champ-banned`/`.cy-champ-picked`/`.cy-champ-disabled`, `.cy-champ-art`, `.cy-champ-art::before`, `.cy-champ-art-melee`, `.cy-champ-art-ranged`, `.cy-champ-art-support`, `.cy-champ-x`, `.cy-champ-meta`, `.cy-champ-name`, `.cy-champ-role`, `.cy-submit`, `.cy-submit-ban`.
    - CHAT (cyber.css lines 405-428): `.cy-chat`, `.cy-chat-right`, `.cy-chat-drawer`, `.cy-chat-head`, `.cy-chat-tabs` (+ `button`, `button.is-active`), `.cy-chat-list`, `.cy-chat-msg`, `.cy-chat-prefix`, `.cy-chat-sender`, `.cy-chat-msg.is-self .cy-chat-sender`, `.cy-chat-body`, `.cy-chat-cursor`, `.cy-chat-input` (+ `span`, `input`), `.cy-draft-chat-sidebar .cy-draft-grid` (the `padding-right: 300px` desktop reservation).
    - REVIEW (cyber.css lines 430-450): `.cy-review`, `.cy-review-head`, `.cy-review-eyebrow`, `.cy-review-head h2`, `.cy-review-receipt`, `.cy-review-grid`, `.cy-review-team`, `.cy-review-team-lime`, `.cy-review-team-violet`, `.cy-review-team-head`, `.cy-review-picks`, `.cy-review-pick`, `.cy-review-pick-art`, `.cy-review-pick-name`, `.cy-review-pick-role`, `.cy-review-bans`, `.cy-review-bans > span:first-child`, `.cy-review-ban`, `.cy-review-actions`.
    - PAUSE (cyber.css lines 452-463): `.cy-pause`, `.cy-pause-card`, `.cy-pause-eyebrow`, `.cy-pause-card h2`, `.cy-pause-log`, `.cy-pause-timer`, `.cy-pause-timer-num` (+ `span`), `.cy-pause-bar` (+ `> div`), `.cy-pause-card p`.

    The `cy-blink` keyframe used by the chat cursor and countdown-urgency fallback is ALREADY in src/app.css (Phase 8) — do NOT re-add it. The `@keyframes cy-pulse` is NEW (drafting only) — port it.

    Add a `@media (prefers-reduced-motion: reduce)` rule appended at the end of the PHASE 10 section that disables the urgency pulse only (D-04 motion-safety): `.cy-turn-clock.is-urgent .cy-turn-clock-num { animation: none; }` and `.cy-pip.is-active { animation: none; }`. Keep the color shift (red) intact under reduced motion — only the `animation` is suppressed. (Phase 8 already has a global reduced-motion block for cy-blink; this is an ADDITIONAL rule for the new cy-pulse usage.)
  </action>
  <verify>
    <automated>npm run test -- --run src/phase8-foundation.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\.cy-draft-grid {" src/app.css` matches
    - `grep "grid-template-columns: 240px 1fr 240px" src/app.css` matches (cy-draft-grid verbatim)
    - `grep "\.cy-pickslot.is-ban .cy-pickslot-name" src/app.css` matches AND the rule contains `text-decoration: line-through`
    - `grep "\.cy-submit-ban" src/app.css` matches AND contains `background: var(--cy-red)`
    - `grep "\.cy-turn-clock.is-urgent" src/app.css` matches AND contains `color: var(--cy-red)`
    - `grep "@keyframes cy-pulse" src/app.css` matches
    - `grep "repeat(7, 1fr)" src/app.css` matches (cy-champ-grid)
    - `grep "\.cy-chat-right" src/app.css` and `grep "\.cy-chat-drawer" src/app.css` both match
    - `grep "padding-right: 300px" src/app.css` matches (chat sidebar reservation)
    - `grep "\.cy-review-grid {" src/app.css` and `grep "\.cy-pause-card {" src/app.css` both match
    - `grep -c "cy-loading\|cy-gate\|cy-cancel\|cy-modal\|cy-stepper\|cy-script\|cy-hc-" src/app.css` returns 0 (Phase 11/12 blocks NOT ported)
    - `grep -c "border-radius" src/app.css` still returns 0
    - phase8-foundation.spec.js still exits 0
  </acceptance_criteria>
  <done>Drafting, Chat, Review, and Pause Cyber CSS blocks are present verbatim in src/app.css; no Phase 11/12 blocks leaked in; reduced-motion suppression for cy-pulse added; Phase 8 assertions still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write node string-grep spec proving the screen CSS ported verbatim</name>
  <read_first>
    - src/phase8-foundation.spec.js (mirror this exact pattern: readFileSync src/app.css, string `.toContain(...)` assertions)
    - src/app.css (after Tasks 1-2 — the assertions must match what was ported)
    - design_handoff_pickban_cyber/prototype/variants/cyber.css (the source values being asserted)
  </read_first>
  <behavior>
    - Test: src/app.css contains the per-screen anchor selectors `.cy-home`, `.cy-login-card`, `.cy-lobby`, `.cy-draft-grid`, `.cy-review-grid`, `.cy-pause-card`
    - Test: the load-bearing verbatim values are present — `padding: 40px 32px 24px` (cy-home), `grid-template-columns: 240px 1fr 240px` (cy-draft-grid), `repeat(7, 1fr)` (cy-champ-grid), `padding-right: 300px` (chat sidebar)
    - Test: ban treatment is intact — `.cy-pickslot.is-ban .cy-pickslot-name` rule contains `text-decoration: line-through`; `.cy-submit-ban` contains `var(--cy-red)`
    - Test: urgency state is intact — `.cy-turn-clock.is-urgent` block contains `var(--cy-red)` and `cy-pulse`; `@keyframes cy-pulse` exists
    - Test (guard): src/app.css does NOT contain any Phase 11/12 selector (`cy-loading`, `cy-gate`, `cy-cancel`, `cy-modal`, `cy-stepper`, `cy-script`, `cy-hc-`)
    - Test (guard): src/app.css has zero `border-radius` occurrences (DS-04)
  </behavior>
  <action>
    Create src/phase10-screens.spec.js as a NODE-project spec (plain `*.spec.js`, NOT `*.svelte.spec.js`, so it runs under the `server` vitest project — see vite.config.js projects). Mirror src/phase8-foundation.spec.js exactly: import `readFileSync, existsSync` from `node:fs`, `describe/it/expect` from vitest, define `const read = (p) => (existsSync(p) ? readFileSync(p, 'utf8') : '')`, read `src/app.css` once.

    Write assertions covering the behavior block above. Use `.toContain(...)` for substring checks and `.not.toContain(...)` for the Phase 11/12 guards. For the ban/urgency combined-rule checks, assert the css string contains both the selector text AND the property text (separate `.toContain` calls are sufficient — verbatim port means both exist).

    Group into describe blocks per screen: `'Phase 10 — Home/Login CSS'`, `'Phase 10 — Lobby CSS'`, `'Phase 10 — Drafting/Chat CSS'`, `'Phase 10 — Review/Pause CSS'`, `'Phase 10 — scope guards (no Phase 11/12, no radius)'`.

    Because Tasks 1-2 already ported the CSS, these tests are GREEN on first run (this is a verification spec for the verbatim port, not RED-first TDD — the CSS exists). If any assertion fails, the port in Tasks 1-2 is incomplete; fix src/app.css, not the spec.
  </action>
  <verify>
    <automated>npm run test -- --run src/phase10-screens.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/phase10-screens.spec.js` exists and is a plain `.spec.js` (runs under the `server` node project, not the browser project)
    - File imports `readFileSync` from `node:fs` and reads `src/app.css`
    - `npm run test -- --run src/phase10-screens.spec.js` exits 0
    - Spec contains `.not.toContain('cy-modal')` (or equivalent Phase 11/12 guard)
    - Spec asserts `text-decoration: line-through` and `cy-pulse` presence
  </acceptance_criteria>
  <done>src/phase10-screens.spec.js runs green under the node project, asserting all six screens' anchor selectors + load-bearing verbatim values are present and no Phase 11/12 styles leaked in.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/phase8-foundation.spec.js src/phase10-screens.spec.js` exits 0
- `grep -c "border-radius" src/app.css` returns 0
- No Phase 11/12 selectors present in src/app.css
</verification>

<success_criteria>
All per-screen Cyber CSS for Home, Login, Lobby, Drafting, Chat, Review, and Pause exists verbatim in src/app.css under .cy-app, the Phase 8/9 foundation is untouched and not duplicated, no Phase 11/12 styles leaked in, and a node string-grep spec proves the port.
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-screen-reskins/10-01-SUMMARY.md`
</output>
