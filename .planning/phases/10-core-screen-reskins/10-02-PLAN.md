---
phase: 10-core-screen-reskins
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/routes/+page.svelte
  - src/lib/components/molecules/Create.svelte
  - src/lib/components/molecules/Join.svelte
  - src/lib/components/effects/cyTypedLog.svelte.js
  - src/lib/components/effects/cyTypedLog.svelte.spec.js
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Home types out the D-03 boot log, then reveals the DRAFT ASCII wordmark, then shows CREATE_DRAFT() and join-by-code actions"
    - "The Home hero passes hot to the app-wide CyShader"
    - "Creating a room and joining by code still work unchanged (form POST ?/createRoom; Join parses code and navigates)"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Cyber Home: boot log -> wordmark -> action cards, no duplicate Header chrome, hot shader"
      contains: "CyLogo"
    - path: "src/lib/components/molecules/Create.svelte"
      provides: "SPAWN_ROOM card with EXECUTE action (unchanged createRoom form)"
      contains: "cy-card"
    - path: "src/lib/components/molecules/Join.svelte"
      provides: "JOIN_ROOM card with code input + CONNECT action (unchanged parse+goto)"
      contains: "cy-card"
    - path: "src/lib/components/effects/cyTypedLog.svelte.js"
      provides: "CY_BOOT_LINES updated to the D-03 draft-themed connect narrative"
      contains: "draft --connect"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "createTypedLog / CyLogo / CyShader hot"
      via: "boot.done gates wordmark reveal; hot passed to shader"
      pattern: "createTypedLog|CyLogo|hot"
---

<objective>
Reskin the Home screen (UI-01) to the Cyber direction: a terminal hero frame that types the D-03 boot log, reveals the shaded-ASCII DRAFT wordmark on completion, then surfaces the `CREATE_DRAFT()` and join-by-code action cards plus the Discord sign-in entry. Wire the `hot` shader to the Home hero. Draft-creation and join behavior stay byte-for-byte unchanged.

Purpose: First runtime consumer of the Phase 9 effects (createTypedLog, CyLogo, hot CyShader). Delivers UI-01.
Output: Cyber Home route + restyled Create/Join cards + finalized D-03 boot-log copy.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-screen-reskins/10-CONTEXT.md
@.planning/phases/10-core-screen-reskins/10-UI-SPEC.md

# Home structure reference (React → reimplement as Svelte 5; do not transcribe wholesale)
@design_handoff_pickban_cyber/prototype/variants/cyber.jsx

<interfaces>
<!-- Effects API the executor consumes directly — no exploration needed. -->

From src/lib/components/effects/cyTypedLog.svelte.js:
```js
export function createTypedLog(lines: string[], opts?: { speed?: number, lineGap?: number }): {
  get text(): string;   // reactive visible prefix
  get done(): boolean;  // reactive — true once full text typed
  stop(): void;         // dispose the $effect.root timer chain
}
export const CY_BOOT_LINES: string[]; // updated in this plan to the D-03 lines
```

From src/lib/components/effects/CyLogo.svelte:
```
<CyLogo rendered={boolean} />  // base wordmark ALWAYS visible; rendered=true fires the line-by-line reveal
```

From src/lib/components/effects/CyShader.svelte:
```
<CyShader intensity={number=1} hot={boolean=false} />  // app-wide ambient instance lives in CyShell; Home wires hot here
```

NOTE: The CyShell chrome (header/phases/room-meta + ambient CyShader + scanlines + .cy-app wrapper) is rendered by src/routes/+layout.svelte for EVERY route. Home must NOT render its own <Header>; remove that import/usage. Home content renders inside CyShell's .cy-body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize D-03 boot-log copy in cyTypedLog + update its spec</name>
  <read_first>
    - src/lib/components/effects/cyTypedLog.svelte.js (the CY_BOOT_LINES export to replace — keep the factory + docs intact)
    - src/lib/components/effects/cyTypedLog.svelte.spec.js (the verbatim assertion test 'exports CY_BOOT_LINES verbatim (6 lines)' must be updated to the new lines)
    - .planning/phases/10-core-screen-reskins/10-CONTEXT.md (D-03 — the exact 6 lines, captured verbatim)
  </read_first>
  <action>
    Replace the `CY_BOOT_LINES` array in src/lib/components/effects/cyTypedLog.svelte.js with the D-03 draft-themed connect narrative, VERBATIM (preserve every space in the dot-leaders — they are load-bearing for typed-log alignment):
    ```js
    export const CY_BOOT_LINES = [
      '$ draft --connect',
      '[ OK ] handshake with draftnet ...... ok',
      '[ OK ] syncing champion catalog ..... 28',
      '[ OK ] team channels A/B ............ open',
      '[ OK ] turn clock .................... 30s',
      '> ready_'
    ];
    ```
    Keep the JSDoc above the export but update the "copied VERBATIM from cyber.jsx" note to "finalized per Phase 10 CONTEXT D-03 (draft-themed connect narrative)". Leave `createTypedLog` and the TypedLog typedef unchanged.

    Update the spec assertion in cyTypedLog.svelte.spec.js — the test currently named `'exports CY_BOOT_LINES verbatim (6 lines)'` asserts the OLD lines (`'$ ./draftnet --init'`, `'28 found'`, `'> booting interface_'`). Replace those assertions with:
    - `expect(CY_BOOT_LINES).toHaveLength(6);`
    - `expect(CY_BOOT_LINES[0]).toBe('$ draft --connect');`
    - `expect(CY_BOOT_LINES[2]).toContain('syncing champion catalog');`
    - `expect(CY_BOOT_LINES[5]).toBe('> ready_');`
    Leave the other typed-log timing tests (character-by-character, lineGap, reduced-motion, stop) unchanged — they use literal `['ab']` etc., not CY_BOOT_LINES.
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/components/effects/cyTypedLog.svelte.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'\$ draft --connect'" src/lib/components/effects/cyTypedLog.svelte.js` matches
    - `grep "'> ready_'" src/lib/components/effects/cyTypedLog.svelte.js` matches
    - `grep -c "booting interface" src/lib/components/effects/cyTypedLog.svelte.js` returns 0 (old copy gone)
    - cyTypedLog.svelte.spec.js asserts `'$ draft --connect'` and `'> ready_'`
    - `npm run test -- --run src/lib/components/effects/cyTypedLog.svelte.spec.js` exits 0
  </acceptance_criteria>
  <done>CY_BOOT_LINES holds the 6 D-03 lines verbatim; the browser spec asserts the new copy and stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Create + Join as Cyber action cards</name>
  <read_first>
    - src/lib/components/molecules/Create.svelte (current Tailwind form — keep the `use:enhance` POST ?/createRoom unchanged)
    - src/lib/components/molecules/Join.svelte (current Tailwind form — keep parseRoomCode + goto navigation unchanged)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYHome cards ~259-285 — SPAWN_ROOM / JOIN_ROOM structure)
    - src/app.css (after Plan 01 — `.cy-card`, `.cy-card-head`, `.cy-card-num`, `.cy-card-title`, `.cy-card-prompt`, `.cy-card-rule`, `.cy-card-desc`, `.cy-card-action`, `.cy-card-btn`, `.cy-input-wrap`, `.cy-input-prompt`, `.cy-input-bare` exist)
  </read_first>
  <action>
    Restyle Create.svelte to the SPAWN_ROOM card (D-01: keep the `<form method="POST" action="?/createRoom" use:enhance>` and submit behavior; only change classes/markup). Structure per cyber.jsx CYHome card [01]:
    - Root: the existing `<form>` wraps a `<button type="submit" class="cy-card">` (the whole card is the create button, per cyber.jsx `<button className="cy-card">`).
    - Inside: `<div class="cy-card-head"><span class="cy-card-num">[ 01 ]</span><span class="cy-card-title"><span class="cy-card-prompt">&gt;</span> SPAWN_ROOM</span></div>`
    - `<div class="cy-card-rule" aria-hidden="true"></div>`
    - `<p class="cy-card-desc">Initialize a new draft session. You become host.</p>`
    - `<div class="cy-card-action"><span class="cy-card-btn">▸ EXECUTE</span></div>`
    Remove ALL Tailwind classes and the old `<h2>Create a Draft</h2>`/`<section>` wrapper. Replace `bg-amber-500` etc. with the cy-card styling.

    Restyle Join.svelte to the JOIN_ROOM card [02] (D-01: keep `draftId` state, `parseRoomCode`, `goto(resolve('/draft/[id]', { id: code }))` in handleSubmit unchanged). Structure per cyber.jsx CYHome card [02]:
    - Root `<form onsubmit={handleSubmit} class="cy-card">` (a div-style card containing an input + connect button; since it has an input, use a non-button card — `class="cy-card"` on the form is fine).
    - `<div class="cy-card-head"><span class="cy-card-num">[ 02 ]</span><span class="cy-card-title"><span class="cy-card-prompt">&gt;</span> JOIN_ROOM</span></div>`
    - `<div class="cy-card-rule" aria-hidden="true"></div>`
    - `<p class="cy-card-desc">Connect to an existing session via room code or URL.</p>`
    - `<div class="cy-card-action"><label class="cy-input-wrap"><span class="cy-input-prompt">&gt;</span><input class="cy-input cy-input-bare" placeholder="K7-MIRA" bind:value={draftId} /></label><button class="cy-card-btn" type="submit" disabled={disabled}>▸ CONNECT</button></div>`
    Keep the `disabled = $derived(draftId.length === 0)` gate on the connect button. Remove all Tailwind classes and the old `<h2>Join a Draft</h2>`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-card" src/lib/components/molecules/Create.svelte` matches
    - `grep "SPAWN_ROOM" src/lib/components/molecules/Create.svelte` matches
    - `grep "▸ EXECUTE" src/lib/components/molecules/Create.svelte` matches
    - `grep "?/createRoom" src/lib/components/molecules/Create.svelte` still matches (behavior unchanged)
    - `grep "JOIN_ROOM" src/lib/components/molecules/Join.svelte` matches
    - `grep "K7-MIRA" src/lib/components/molecules/Join.svelte` matches (placeholder)
    - `grep "▸ CONNECT" src/lib/components/molecules/Join.svelte` matches
    - `grep "parseRoomCode" src/lib/components/molecules/Join.svelte` still matches (behavior unchanged)
    - `grep -c "bg-amber\|rounded-" src/lib/components/molecules/Create.svelte src/lib/components/molecules/Join.svelte` returns 0 (Tailwind gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>Create renders the SPAWN_ROOM card with ▸ EXECUTE (still POSTs ?/createRoom); Join renders the JOIN_ROOM card with K7-MIRA input + ▸ CONNECT (still parses + navigates); no Tailwind remains.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin the Home route — hero frame, typed boot log, wordmark reveal, hot shader, action grid</name>
  <read_first>
    - src/routes/+page.svelte (current — remove the <Header> import/usage; replace Tailwind <main>)
    - src/lib/components/chrome/CyShell.svelte (chrome already provides header/shader/scanlines via +layout.svelte — confirm Home renders inside .cy-body)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYHome ~231-289 — hero frame, boot, logo-wrap, tagline, home-grid)
    - src/app.css (after Plan 01 — `.cy-home`, `.cy-hero`, `.cy-frame`, `.cy-frame-label`, `.cy-hero-tagline`, `.cy-prompt`, `.cy-slash`, `.cy-home-grid` exist; `.cy-boot`, `.cy-logo*` already from Phase 9)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Home sequence + hot shader rows in Interaction & State Contract; tagline copy)
  </read_first>
  <action>
    Rewrite src/routes/+page.svelte to the Cyber Home. Remove the `import Header` and its `<Header />` usage entirely (the layout's CyShell already renders the header chrome — rendering a second one duplicates the wordmark). Keep importing Create and Join.

    Script section:
    - `import CyLogo from '$lib/components/effects/CyLogo.svelte';`
    - `import CyShader from '$lib/components/effects/CyShader.svelte';`
    - `import { createTypedLog, CY_BOOT_LINES } from '$lib/components/effects/cyTypedLog.svelte.js';`
    - `import Create from '$lib/components/molecules/Create.svelte';`
    - `import Join from '$lib/components/molecules/Join.svelte';`
    - `const boot = createTypedLog(CY_BOOT_LINES);` (default opts — speed 9, lineGap 60)

    Markup (inside the layout's .cy-body — render a top-level `<div class="cy-home">`):
    - Hero: `<div class="cy-hero"><div class="cy-frame"><span class="cy-frame-label">DRAFT_NET · v1.0</span>` then
      - `<div class="cy-boot" aria-hidden="true">{boot.text}</div>`
      - `<CyLogo rendered={boot.done} />`
      - `<div class="cy-hero-tagline"><span class="cy-prompt">&gt;</span> REAL-TIME PICK<span class="cy-slash">/</span>BAN PROTOCOL</div>`
      - close cy-frame, cy-hero.
      (The full ASCII `.cy-frame-border` measurement component from cyber.jsx is discretionary — the `.cy-frame-label` + `.cy-frame` background already convey the terminal frame. Skip the dynamic CYFrameBorder canvas-measuring widget; it is not required by UI-01.)
    - Action grid: `<div class="cy-home-grid"><Create /><Join /></div>` (the two cards from Task 2 fill the 1fr 1fr grid).
    - Hot shader for the hero (D-02): render `<CyShader hot={true} />` as a sibling INSIDE `<div class="cy-home">` positioned behind the hero — OR, simpler and matching cyber.jsx `<CYChrome ... hot>`: since the ambient shader is app-wide in CyShell without hot, add one `<CyShader hot={true} />` at the top of the `.cy-home` div (it is absolute/inset:0 via `.cy-shader`, pointer-events none, sits behind content). This satisfies UI-SPEC "Home hero passes hot to the CyShader". Add `aria-hidden` is already on the canvas.

    Do NOT wrap content in `<main class="flex ...">` Tailwind. The `.cy-home` block (max-width 1100px, centered, padding 40px 32px 24px) is the layout container.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import Header" src/routes/+page.svelte` returns 0 (duplicate chrome removed)
    - `grep "createTypedLog" src/routes/+page.svelte` matches
    - `grep "CY_BOOT_LINES" src/routes/+page.svelte` matches
    - `grep "boot.text" src/routes/+page.svelte` matches (typed log rendered)
    - `grep "rendered={boot.done}" src/routes/+page.svelte` matches (wordmark reveal gated on boot completion)
    - `grep "CyLogo" src/routes/+page.svelte` matches
    - `grep "hot={true}" src/routes/+page.svelte` matches (hot shader wired — UI-SPEC / D-02)
    - `grep "REAL-TIME PICK" src/routes/+page.svelte` matches (tagline)
    - `grep "cy-home-grid" src/routes/+page.svelte` matches and renders `<Create />` and `<Join />`
    - `grep -c "flex \|md:flex-row\|max-w-4xl" src/routes/+page.svelte` returns 0 (Tailwind main wrapper gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>Home renders the terminal hero frame with the typed D-03 boot log, the DRAFT wordmark revealing on boot.done, the REAL-TIME PICK/BAN PROTOCOL tagline, the SPAWN_ROOM/JOIN_ROOM action grid, and a hot CyShader; the duplicate Header is gone; no Tailwind wrapper remains.</done>
</task>

</tasks>

<verification>
- `npm run check` passes
- `npm run test -- --run src/lib/components/effects/cyTypedLog.svelte.spec.js` exits 0
- Home sequence (boot log → wordmark on done → actions) wired; hot shader present; create/join behavior unchanged
</verification>

<success_criteria>
UI-01 met: Home shows the typed D-03 boot log, then the ASCII DRAFT wordmark reveal, then CREATE_DRAFT()/join-by-code action cards with the Discord sign-in path intact, the hero uses the hot shader, and room creation + join-by-code behave exactly as before.
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-screen-reskins/10-02-SUMMARY.md`
</output>
