---
phase: 10-core-screen-reskins
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/components/molecules/TurnIndicator.svelte
  - src/lib/components/atoms/TimerDisplay.svelte
  - src/lib/components/molecules/TeamDraftColumn.svelte
  - src/lib/components/atoms/DraftSlot.svelte
  - src/lib/components/molecules/ChampionGrid.svelte
  - src/lib/components/atoms/ChampionCard.svelte
  - src/lib/components/molecules/DraftBoard.svelte
  - src/lib/components/molecules/ChatPanel.svelte
  - src/lib/components/atoms/ChatMessage.svelte
  - src/lib/components/atoms/ChatInput.svelte
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "Drafting renders the turn readout (`$ TURN_NN/10`, `TEAM_x :: PICK|BAN`, `cap=>name`) + a big countdown clock that enters an urgency state (red + pulse) in the final 5 seconds, with a pip strip of done/active turns"
    - "The champion catalog renders as a `.cy-champ-grid` of `.cy-champ` cards (selected = lime border+glow; used = dimmed/not-allowed with a red ✕ on banned), and the lock-in CTA reads `$ {pick|ban}({champName}) [LOCK_IN]` (lime for pick, red `cy-submit-ban` for ban)"
    - "Both teams render `.cy-draft-col` (A lime / B violet) with `> bans` and `> picks` sections; ban slots are struck-through via `.cy-pickslot.is-ban`; filled slots show champ art + name"
    - "Team chat renders as a `.cy-chat` panel docked right as a sidebar on desktop (`.cy-chat-right` + `.cy-draft-grid` `padding-right: 300px`) and as a toggleable drawer (`.cy-chat-drawer`) on narrow widths (D-02)"
    - "Every frozen draft handler is untouched: pickBan via onPickBan/onSubmit, the activeChatStream/sendMessage wiring, the snapshot.draftState turn/timer reads, and isActiveCaptain gating all behave identically (D-01)"
    - "No Tailwind utility classes remain in any of the 10 restyled drafting/chat files"
  artifacts:
    - path: "src/lib/components/molecules/TurnIndicator.svelte"
      provides: "Cyber turn readout + clock host (.cy-turn lime/violet, .cy-turn-readout, .cy-turn-clock, .cy-turn-pips)"
      contains: "cy-turn-readout"
    - path: "src/lib/components/atoms/TimerDisplay.svelte"
      provides: "Cyber countdown clock-num + bar with urgency state (.cy-turn-clock.is-urgent in final 5s, D-04)"
      contains: "cy-turn-clock"
    - path: "src/lib/components/molecules/TeamDraftColumn.svelte"
      provides: "Cyber team pick/ban column (.cy-draft-col lime/violet, > bans / > picks sections)"
      contains: "cy-draft-col"
    - path: "src/lib/components/atoms/DraftSlot.svelte"
      provides: "Cyber pickslot (.cy-pickslot, struck .cy-pickslot.is-ban, empty .cy-pickslot-empty)"
      contains: "cy-pickslot"
    - path: "src/lib/components/molecules/ChampionGrid.svelte"
      provides: "Cyber catalog grid (.cy-champ-grid) + lock-in submit (.cy-submit / .cy-submit-ban) — unchanged onSubmit"
      contains: "cy-champ-grid"
    - path: "src/lib/components/atoms/ChampionCard.svelte"
      provides: "Cyber champ card (.cy-champ, .cy-champ-art role tints, .cy-champ-x ban overlay) — unchanged onclick"
      contains: "cy-champ"
    - path: "src/lib/components/molecules/DraftBoard.svelte"
      provides: "Cyber draft board wrapper (.cy-draft, .cy-draft-grid; chat dock toggle for D-02) — unchanged onPickBan/pause wiring"
      contains: "cy-draft-grid"
    - path: "src/lib/components/molecules/ChatPanel.svelte"
      provides: "Cyber chat panel (.cy-chat right/drawer, .cy-chat-head, .cy-chat-tabs, .cy-chat-list) — unchanged activeTab/onSend"
      contains: "cy-chat"
  key_links:
    - from: "src/lib/components/molecules/ChampionGrid.svelte"
      to: "pickBan RPC (via onSubmit)"
      via: "$ {action}({name}) [LOCK_IN] button onclick preserved"
      pattern: "onSubmit"
    - from: "src/lib/components/atoms/TimerDisplay.svelte"
      to: "snapshot.draftState turnEndsAt/timerMs"
      via: "setInterval countdown + is-urgent in final 5s"
      pattern: "turnEndsAt"
    - from: "src/lib/components/molecules/ChatPanel.svelte"
      to: "sendMessage / activeChatStream (route)"
      via: "onSend + bindable activeTab preserved"
      pattern: "onSend|activeTab"
---

<objective>
Reskin the Drafting screen + team chat (UI-04) to the Cyber direction: the turn readout + countdown clock with a final-5s urgency state (D-04), the pip strip, both teams' pick (lime) / ban (red, struck) columns, the champion catalog grid with the `[LOCK_IN]` submit, and the team chat docked as a sidebar on desktop / drawer on narrow widths (D-02). Restyle in place (D-01) — `pickBan`, the chat stream/`sendMessage` wiring, the `snapshot.draftState` timer reads, and the captain gating all stay byte-for-byte.

Purpose: Delivers UI-04. This is the densest screen; it owns all ten drafting/chat presentational files (DraftBoard + its children + ChatPanel + its atoms). The PauseOverlay component that DraftBoard renders is owned by Plan 06 — leave the `<PauseOverlay … />` invocation in DraftBoard untouched.
Output: Cyber drafting board (turn/clock/pips, lime/violet columns, champ grid, lock-in) + responsive Cyber chat.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-screen-reskins/10-CONTEXT.md
@.planning/phases/10-core-screen-reskins/10-UI-SPEC.md

# Drafting + Chat structure reference (React → reimplement as Svelte 5; do not transcribe wholesale, D-01)
@design_handoff_pickban_cyber/prototype/variants/cyber.jsx

<interfaces>
<!-- The frozen wiring these components consume — DO NOT change handler bodies, props, or RPC calls. -->
<!-- All .cy-* classes referenced below already exist in src/app.css after Plan 01 (verbatim port). -->

DraftBoard.svelte props: { snapshot, userId, onPickBan, justReconnected }.
  - currentTurn = snapshot.draftState.script[snapshot.draftState.turnIndex]  (has { team:'A'|'B', action:'pick'|'ban' })
  - usedIds = snapshot.actions where champion_id != null
  - isActiveCaptain = userId is a captain of currentTurn.team  (gates submit)
  - Renders, in order: StatusBanner reconnect/promotion banners (KEEP), {#if paused && phase!=='cancelled'}<PauseOverlay …/>{/if} (Plan 06 owns PauseOverlay — leave call AS-IS),
    cancelled branch (KEEP), else: <TurnIndicator>, then a grid with <TeamDraftColumn team="A">, <ChampionGrid onSubmit={onPickBan}>, <TeamDraftColumn team="B">.
  - onPickBan -> pickBan(code, {championId, action}) is the FROZEN draft RPC. Never change it.

TurnIndicator props: { draftState, teams }. activeTeam = currentTurn.team; captainName derive; renders <TimerDisplay turnEndsAt timerMs/>.
TimerDisplay props: { turnEndsAt, timerMs }. setInterval ticks secondsLeft; urgency = secondsLeft <= 10 TODAY — change threshold to <= 5 for D-04. barWidth derive from secondsLeft/timerMs.
TeamDraftColumn props: { team:'A'|'B', label, script, actions, members }. banSlots()/pickSlots() derive {scriptIndex, championName} arrays; renders <DraftSlot action championName team/> per slot.
DraftSlot props: { action:'pick'|'ban', championName, team }. isEmpty when championName==null.
ChampionGrid props: { champions, usedIds, isActiveCaptain, currentAction:'pick'|'ban', onSubmit }. selectedId state; cardState()→'disabled'|'selected'|'default'; showSubmit when isActiveCaptain && selectedId; handleSubmit→onSubmit({championId, action}). champion = { id, name, role }.
ChampionCard props: { champion:{id,name,role}, state:'default'|'selected'|'picked'|'banned'|'disabled', onclick }. isInteractive/isDisabled derives gate onclick.
ChatPanel props: { phase, role:'player'|'guest', messages, currentUserName, onSend, activeTab(bindable) }. showAllTab=phase==='lobby'; roleTabLabel/roleTabKey; auto-scroll $effect on messages.length; renders <ChatMessage> per msg + <ChatInput onSend bind:error/>.
ChatMessage props: { sender, body, ts, isSelf }. ChatInput props: { onSend, disabled, error(bindable) }; Enter-to-send.

Verbatim Cyber copy/markup (from UI-SPEC + cyber.jsx CYDrafting/CYChamp/CYChat ~437-569):
  Turn readout: `$ TURN_{NN}/10` (label), `TEAM_{X} :: {ACTION_UPPER}` (team), `cap=>{name}` (cap).
  Clock: `{NN}<span>s</span>` in .cy-turn-clock-num; .cy-turn-clock gets `is-urgent` when seconds<=5.
  Pips: one .cy-pip per script turn, glyph `▲` (ban) / `■` (pick); class `cy-pip cy-pip-{lime|violet} cy-pip-{action}` + `is-done` (i<turnIndex) | `is-active` (i===turnIndex).
  Column head: `// TEAM_A` / `// TEAM_B`; section labels `> bans` / `> picks`.
  Empty pickslot: `<span class="cy-pickslot-action">{action}_</span><span class="cy-pickslot-fill">{isActive ? '█▓░ ACTIVE' : '──────'}</span>`.
  Filled pickslot: `.cy-pickslot-art cy-champ-art-{role}` with first-2-letters, then `.cy-pickslot-name` + `.cy-pickslot-action`.
  Champ card: `<button class="cy-champ cy-champ-{state}" disabled={state!=='default'&&state!=='selected'}>` with `.cy-champ-art cy-champ-art-{role}`, first-2-letters, `{#if banned}<div class="cy-champ-x">✕</div>{/if}`, `.cy-champ-meta`(`.cy-champ-name` + `.cy-champ-role` `.{role}`).
  Roster head: `> SELECT_TARGET[ {avail} / {total} available ]` + filter chips `all/.melee/.ranged/.support` (chips are presentational — discretionary, keep `.cy-chip`/`.cy-chip.is-active`).
  Submit: `$ {action}({name}) [LOCK_IN]` on `cy-submit cy-submit-{pick|ban}`.
  Chat head: `// chat.{tab}` + `.cy-chat-tabs` buttons (all/team/spec). Msg: `.cy-chat-prefix` (`>` self / `<` other) + `.cy-chat-sender` (`name:`) + `.cy-chat-body`. Cursor `.cy-chat-cursor` `$ _`. Input: `.cy-chat-input` `<span>></span><input/>`.
  Dock: sidebar => root `.cy-chat cy-chat-right`; drawer => `.cy-chat cy-chat-drawer`. Sidebar reservation rule `.cy-draft-chat-sidebar .cy-draft-grid { padding-right: 300px }` already in app.css — apply `cy-draft-chat-sidebar` modifier on the .cy-draft wrapper in desktop mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin TurnIndicator + TimerDisplay (Cyber turn readout, clock, pips, D-04 urgency)</name>
  <read_first>
    - src/lib/components/molecules/TurnIndicator.svelte (current Tailwind — KEEP the draftState/teams props, currentTurn/activeTeam/captainName derives; it composes TimerDisplay)
    - src/lib/components/atoms/TimerDisplay.svelte (current — KEEP the setInterval tick reading turnEndsAt, the secondsLeft state, barWidth derive; only change the urgency threshold + markup)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYDrafting ~466-483 — `.cy-turn`/`.cy-turn-readout`/`.cy-turn-clock`/`.cy-turn-pips` markup)
    - src/app.css (after Plan 01 — `.cy-turn`, `.cy-turn-lime`, `.cy-turn-violet`, `.cy-turn-readout`, `.cy-turn-label`, `.cy-turn-team`, `.cy-turn-cap`, `.cy-turn-clock`, `.cy-turn-clock-num`, `.cy-turn-clock-bar`, `.cy-turn-pips`, `.cy-pip*`, `@keyframes cy-pulse` all exist; the `is-urgent` rule shifts to `--cy-red` + `cy-pulse`)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Interaction & State Contract — Drafting countdown urgency D-04: final 5s color shift + pulse; reduced-motion = color only)
  </read_first>
  <action>
    Rewrite both files. Remove ALL Tailwind. Preserve every prop, derive, and the setInterval tick.

    TurnIndicator.svelte — emit the `.cy-turn` readout host. Add an accent derive `const accent = $derived(activeTeam === 'A' ? 'lime' : 'violet');`. Build a turn-number readout and a pip strip from `draftState.script` + `draftState.turnIndex`:
    - Root `<div class="cy-turn cy-turn-{accent}">`.
    - Readout: `<div class="cy-turn-readout"><span class="cy-turn-label">$ TURN_{String(draftState.turnIndex + 1).padStart(2, '0')}/{draftState.script.length}</span><span class="cy-turn-team">TEAM_{activeTeam} :: {currentTurn.action.toUpperCase()}</span><span class="cy-turn-cap">cap=&gt;{captainName()}</span></div>` (keep the existing `captainName()` derive — it returns the active team's captain displayName).
    - Clock: render `<TimerDisplay turnEndsAt={draftState.turnEndsAt} timerMs={draftState.timerMs} {accent} />` (pass accent through so the clock uses the team accent var).
    - Pips: `<div class="cy-turn-pips">{#each draftState.script as t, i}<span class="cy-pip cy-pip-{t.team === 'A' ? 'lime' : 'violet'} cy-pip-{t.action} {i < draftState.turnIndex ? 'is-done' : i === draftState.turnIndex ? 'is-active' : ''}">{t.action === 'ban' ? '▲' : '■'}</span>{/each}</div>`.

    TimerDisplay.svelte — add `accent = 'lime'` to props (`let { turnEndsAt, timerMs = 30000, accent = 'lime' } = $props();`). KEEP the `$effect` setInterval tick and `secondsLeft` state EXACTLY. Change `const urgency = $derived(secondsLeft <= 10);` to `const urgency = $derived(secondsLeft <= 5);` (D-04: final 5s). KEEP the `barWidth` derive. Replace markup:
    - `<div class="cy-turn-clock {urgency ? 'is-urgent' : ''}" style="--accent: var(--cy-{accent})"><div class="cy-turn-clock-num" aria-live="polite" aria-label="{secondsLeft} seconds remaining">{String(secondsLeft).padStart(2, '0')}<span>s</span></div><div class="cy-turn-clock-bar"><div style="width: {barWidth}%"></div></div></div>`.
    - The `is-urgent` color shift + pulse come from app.css (`.cy-turn-clock.is-urgent .cy-turn-clock-num { color: var(--cy-red); … animation: cy-pulse … }`), and the Plan-01 reduced-motion rule suppresses only the animation (D-04). Do NOT add component-scoped motion — rely on the global rule. The `--accent` inline var drives the non-urgent color.

    Validate both with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-turn-readout" src/lib/components/molecules/TurnIndicator.svelte` matches
    - `grep '\$ TURN_' src/lib/components/molecules/TurnIndicator.svelte` matches AND `grep ' :: ' src/lib/components/molecules/TurnIndicator.svelte` matches AND `grep 'cap=&gt;' src/lib/components/molecules/TurnIndicator.svelte` matches
    - `grep "cy-turn-pips" src/lib/components/molecules/TurnIndicator.svelte` matches AND `grep "cy-pip" src/lib/components/molecules/TurnIndicator.svelte` matches
    - `grep "cy-turn-clock" src/lib/components/atoms/TimerDisplay.svelte` matches AND `grep "is-urgent" src/lib/components/atoms/TimerDisplay.svelte` matches
    - `grep "secondsLeft <= 5" src/lib/components/atoms/TimerDisplay.svelte` matches (D-04 final-5s threshold)
    - `grep "turnEndsAt" src/lib/components/atoms/TimerDisplay.svelte` still matches AND `grep "setInterval" src/lib/components/atoms/TimerDisplay.svelte` still matches (countdown behavior preserved)
    - `grep -cE "text-5xl|bg-amber|text-red-600|rounded-full|text-text-|bg-bg-secondary" src/lib/components/atoms/TimerDisplay.svelte` returns 0 AND `grep -cE "text-xl|text-text-|py-4" src/lib/components/molecules/TurnIndicator.svelte` returns 0 (Tailwind gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>TurnIndicator renders the `$ TURN_NN/10` readout + `TEAM_x :: ACTION` + `cap=>name` + a pip strip in `.cy-turn` lime/violet; TimerDisplay renders the big `.cy-turn-clock` that goes red+pulse in the final 5s (color-only under reduced motion via the global rule); countdown tick + props preserved; no Tailwind.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin TeamDraftColumn + DraftSlot + ChampionGrid + ChampionCard (pick/ban columns, catalog, lock-in)</name>
  <read_first>
    - src/lib/components/molecules/TeamDraftColumn.svelte (current Tailwind — KEEP the banSlots()/pickSlots() derives and props team/label/script/actions/members)
    - src/lib/components/atoms/DraftSlot.svelte (current — KEEP action/championName/team props + isEmpty derive)
    - src/lib/components/molecules/ChampionGrid.svelte (current — KEEP selectedId state, cardState(), handleCardClick, showSubmit, handleSubmit→onSubmit({championId, action}), props champions/usedIds/isActiveCaptain/currentAction/onSubmit)
    - src/lib/components/atoms/ChampionCard.svelte (current — KEEP champion/state/onclick props + isInteractive/isDisabled gating)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYDraftSlot ~419-435, CYChamp ~404-417, CYDrafting columns/roster/submit ~485-531)
    - src/app.css (after Plan 01 — `.cy-draft-col*`, `.cy-draft-col-head`, `.cy-draft-col-section`, `.cy-draft-col-label`, `.cy-pickslot*`, `.cy-pickslot.is-ban`, `.cy-roster*`, `.cy-chip*`, `.cy-champ-grid`, `.cy-champ*`, `.cy-champ-art-{melee|ranged|support}`, `.cy-champ-x`, `.cy-champ-meta`, `.cy-submit`, `.cy-submit-ban` all exist)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Pick column lime / Ban column red+struck; champ grid states; lock-in copy `$ {action}({name}) [LOCK_IN]`)
  </read_first>
  <action>
    Rewrite all four files. Remove ALL Tailwind. Preserve every prop, derive, and handler.

    DraftSlot.svelte — KEEP `{ action, championName = null, team }` props + `isEmpty` derive. Add `const accent = $derived(team === 'A' ? 'lime' : 'violet');`. Markup:
    - Empty: `<div class="cy-pickslot cy-pickslot-empty cy-pickslot-{accent}" aria-label="Empty {action} slot for Team {team}"><span class="cy-pickslot-action">{action}_</span><span class="cy-pickslot-fill">──────</span></div>` (this component renders historic/empty script slots, never the active turn — so omit the `is-active` variant here; the active-turn affordance lives in the catalog/submit).
    - Filled: `<div class="cy-pickslot cy-pickslot-{accent} {action === 'ban' ? 'is-ban' : ''}" aria-label="{championName} — {action} for Team {team}"><div class="cy-pickslot-art"><span>{championName.slice(0, 2)}</span></div><div><div class="cy-pickslot-name">{championName}</div><div class="cy-pickslot-action">{action}</div></div></div>`.
      (The struck-through ban treatment comes from `.cy-pickslot.is-ban .cy-pickslot-name { text-decoration: line-through }` in app.css.)

    TeamDraftColumn.svelte — KEEP `banSlots()`/`pickSlots()` exactly. Add `const accent = $derived(team === 'A' ? 'lime' : 'violet');`. Markup:
    - `<aside class="cy-draft-col cy-draft-col-{accent}"><div class="cy-draft-col-head">// TEAM_{team}</div>`
    - Bans section: `<div class="cy-draft-col-section"><div class="cy-draft-col-label">&gt; bans</div>{#each banSlots() as slot (slot.scriptIndex)}<DraftSlot action="ban" championName={slot.championName} {team} />{/each}</div>`
    - Picks section: same with `&gt; picks` and `action="pick"`.
    - Render both sections unconditionally (drop the `{#if length>0}` guards so empty `──────` slots show — matches cyber.jsx which always renders the slot rows).

    ChampionCard.svelte — KEEP `{ champion, state, onclick }` + isInteractive/isDisabled. Markup:
    - `<button type="button" class="cy-champ cy-champ-{state}" disabled={isDisabled} aria-pressed={state === 'selected' ? 'true' : undefined} onclick={isInteractive ? onclick : undefined}><div class="cy-champ-art cy-champ-art-{champion.role}"><span>{champion.name.slice(0, 2)}</span>{#if state === 'banned'}<div class="cy-champ-x">✕</div>{/if}</div><div class="cy-champ-meta"><span class="cy-champ-name">{champion.name}</span><span class="cy-champ-role">.{champion.role}</span></div></button>`.
    - NOTE: `champion.role` values come from the catalog (`$lib/catalog/classes.json`); the `.cy-champ-art-{melee|ranged|support}` tints exist in app.css. If a role has no matching tint rule it simply renders the base art — acceptable.

    ChampionGrid.svelte — KEEP selectedId/cardState/handleCardClick/showSubmit/handleSubmit + `onSubmit({championId, action})`. Add an available-count derive for the roster head: `const total = $derived(champions.length);` `const available = $derived(champions.length - usedIds.length);`. Change `submitLabel` to the lock-in copy: `const selectedName = $derived(champions.find((c) => c.id === selectedId)?.name ?? '');` and render the submit text inline as `$ {currentAction}({selectedName}) [LOCK_IN]`. Markup:
    - Wrap in `<main class="cy-roster"><div class="cy-roster-head"><h3>&gt; SELECT_TARGET[ {available} / {total} available ]</h3><div class="cy-roster-filters"><button type="button" class="cy-chip is-active">all</button><button type="button" class="cy-chip">.melee</button><button type="button" class="cy-chip">.ranged</button><button type="button" class="cy-chip">.support</button></div></div>` — the filter chips are presentational only (no filter behavior is in scope; keep them static/inert, not wired to any new state).
    - `<div class="cy-champ-grid">{#each champions as champion (champion.id)}<ChampionCard {champion} state={cardState(champion)} onclick={() => handleCardClick(champion)} />{/each}</div>`
    - `{#if showSubmit}<button type="button" class="cy-submit cy-submit-{currentAction}" onclick={handleSubmit}>$ {currentAction}({selectedName}) [LOCK_IN]</button>{/if}` — `cy-submit-ban` makes the ban submit red (app.css); `cy-submit-pick` falls back to the base lime `.cy-submit`.
    - Close `</main>`.

    Validate all four with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-draft-col cy-draft-col-" src/lib/components/molecules/TeamDraftColumn.svelte` matches AND `grep "// TEAM_" src/lib/components/molecules/TeamDraftColumn.svelte` matches AND `grep "&gt; bans" src/lib/components/molecules/TeamDraftColumn.svelte` matches AND `grep "&gt; picks" src/lib/components/molecules/TeamDraftColumn.svelte` matches
    - `grep "banSlots" src/lib/components/molecules/TeamDraftColumn.svelte` still matches AND `grep "pickSlots" src/lib/components/molecules/TeamDraftColumn.svelte` still matches (derives preserved)
    - `grep "cy-pickslot" src/lib/components/atoms/DraftSlot.svelte` matches AND `grep "is-ban" src/lib/components/atoms/DraftSlot.svelte` matches AND `grep "cy-pickslot-empty" src/lib/components/atoms/DraftSlot.svelte` matches
    - `grep "cy-champ cy-champ-" src/lib/components/atoms/ChampionCard.svelte` matches AND `grep "cy-champ-x" src/lib/components/atoms/ChampionCard.svelte` matches AND `grep "cy-champ-art-" src/lib/components/atoms/ChampionCard.svelte` matches
    - `grep "onclick" src/lib/components/atoms/ChampionCard.svelte` still matches (card click preserved)
    - `grep "cy-champ-grid" src/lib/components/molecules/ChampionGrid.svelte` matches AND `grep "SELECT_TARGET" src/lib/components/molecules/ChampionGrid.svelte` matches
    - `grep '\[LOCK_IN\]' src/lib/components/molecules/ChampionGrid.svelte` matches AND `grep "cy-submit-" src/lib/components/molecules/ChampionGrid.svelte` matches
    - `grep "onSubmit" src/lib/components/molecules/ChampionGrid.svelte` still matches AND `grep "handleSubmit" src/lib/components/molecules/ChampionGrid.svelte` still matches AND `grep "isActiveCaptain" src/lib/components/molecules/ChampionGrid.svelte` still matches (submit + captain gating preserved)
    - `grep -cE "rounded-md|bg-amber|grid-cols-4|md:grid-cols-7|text-text-|border-bg-secondary|min-h-\[44px\]" src/lib/components/molecules/ChampionGrid.svelte src/lib/components/atoms/ChampionCard.svelte src/lib/components/molecules/TeamDraftColumn.svelte src/lib/components/atoms/DraftSlot.svelte` returns 0 (Tailwind gone across all four)
    - `npm run check` passes
  </acceptance_criteria>
  <done>Team columns render `.cy-draft-col` (A lime / B violet) with `> bans` / `> picks` sections of `.cy-pickslot`s (bans struck via `is-ban`); the catalog renders `.cy-champ-grid` of `.cy-champ` cards (selected lime, banned red ✕, used dimmed); the lock-in CTA reads `$ {action}({name}) [LOCK_IN]` (red for ban); selectedId/cardState/onSubmit/captain gating all preserved; no Tailwind.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin DraftBoard wrapper + ChatPanel/ChatMessage/ChatInput (responsive sidebar↔drawer, D-02)</name>
  <read_first>
    - src/lib/components/molecules/DraftBoard.svelte (current Tailwind — KEEP every derive/handler: currentTurn, usedIds, isActiveCaptain, pausedCaptainName, the promotion/reconnect $effects + StatusBanner calls, the `{#if paused && phase!=='cancelled'}<PauseOverlay …/>{/if}` block (Plan 06 owns PauseOverlay — leave the invocation AS-IS), the cancelled branch, and the TurnIndicator/TeamDraftColumn/ChampionGrid composition + onPickBan passthrough)
    - src/lib/components/molecules/ChatPanel.svelte (current Tailwind — KEEP showAllTab/roleTabLabel/roleTabKey derives, the activeTab bindable + the $effect that switches off 'all' in draft phase, the auto-scroll $effect, props phase/role/messages/currentUserName/onSend, the ChatMessage + ChatInput composition)
    - src/lib/components/atoms/ChatMessage.svelte (current Tailwind — KEEP sender/body/ts/isSelf props + timeStr derive)
    - src/lib/components/atoms/ChatInput.svelte (current Tailwind — KEEP onSend/disabled/error props, body state, canSend, handleSend, Enter-to-send handleKeydown)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYDrafting wrapper ~463-536 `.cy-draft cy-draft-chat-{mode}` + `.cy-draft-grid`; CYChat ~540-569 `.cy-chat cy-chat-{dock}`/head/tabs/list/msg/cursor/input)
    - src/app.css (after Plan 01 — `.cy-draft`, `.cy-draft-grid`, `.cy-draft-chat-sidebar .cy-draft-grid { padding-right: 300px }`, `.cy-chat`, `.cy-chat-right`, `.cy-chat-drawer`, `.cy-chat-head`, `.cy-chat-tabs`, `.cy-chat-list`, `.cy-chat-msg`, `.cy-chat-prefix`, `.cy-chat-sender`, `.cy-chat-msg.is-self`, `.cy-chat-body`, `.cy-chat-cursor`, `.cy-chat-input` all exist)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Drafting chat D-02 responsive: sidebar on desktop, drawer on narrow; breakpoint = Claude's discretion)
  </read_first>
  <action>
    Rewrite all four files. Remove ALL Tailwind. Preserve every derive, $effect, handler, prop, and the PauseOverlay invocation.

    DraftBoard.svelte — wrap the active-draft (non-cancelled) render in the Cyber `.cy-draft` shell so the `.cy-draft-grid` columns get the chat reservation. KEEP the StatusBanner banners, the PauseOverlay `{#if}` block (untouched), and the cancelled branch (restyle the cancelled branch wrapper to bare non-Tailwind divs only — its full SIGKILL reskin is Phase 12; just strip Tailwind here and keep the `goto('/')` button + copy). Replace the active branch:
    - Root `<div class="cy-draft cy-draft-chat-sidebar">` (the `cy-draft-chat-sidebar` modifier triggers the `padding-right: 300px` reservation on `.cy-draft-grid` so the fixed chat sidebar does not overlap the right column on desktop).
    - `{#if currentTurn && snapshot.draftState && !snapshot.draftState.paused}<TurnIndicator draftState={snapshot.draftState} teams={snapshot.teams} />{/if}` (unchanged condition).
    - `<div class="cy-draft-grid"><TeamDraftColumn team="A" … /><ChampionGrid … onSubmit={onPickBan} /><TeamDraftColumn team="B" … /></div>` (keep ALL existing props on the three children; ChampionGrid's `currentAction={currentTurn?.action ?? 'pick'}` passthrough stays).
    - Leave the `<ChatPanel>` rendering to the route (the route composes ChatPanel as a sibling, not DraftBoard) — DraftBoard does NOT render ChatPanel. Confirm against the route: ChatPanel is a sibling of the DraftBoard wrapper in `+page.svelte`; this task does NOT edit the route file. The `cy-chat-right` fixed positioning + the `.cy-draft-grid` padding reservation together produce the desktop sidebar layout without the route changing.

    ChatPanel.svelte — emit the Cyber `.cy-chat` panel with responsive dock (D-02). KEEP showAllTab/roleTabKey/roleTabLabel, the activeTab bindable + the draft-phase tab-switch $effect, and the auto-scroll $effect EXACTLY. Implement responsive dock:
    - Add a media-query-driven dock: use a `$state` + a `window.matchMedia('(max-width: 1100px)')` listener inside an `$effect` to set `let narrow = $state(false)`, and a `let drawerOpen = $state(false)` toggle for the drawer on narrow widths. (Breakpoint 1100px is discretionary per D-02; choose a value where the 280px sidebar + 240/1fr/240 grid stops fitting.)
    - Desktop (`!narrow`): root `<aside class="cy-chat cy-chat-right" aria-label="Chat">…</aside>` (fixed sidebar; the `.cy-draft-chat-sidebar` reservation in DraftBoard keeps the board clear).
    - Narrow (`narrow`): render a toggle button `<button type="button" class="cy-btn cy-btn-sm" onclick={() => (drawerOpen = !drawerOpen)} aria-expanded={drawerOpen} aria-controls="cy-chat-drawer">// chat</button>` and, when `drawerOpen`, `<aside id="cy-chat-drawer" class="cy-chat cy-chat-drawer" aria-label="Chat">…</aside>` (board goes full-width because no reservation applies on narrow — keep the `.cy-draft-chat-sidebar` reservation desktop-only; if needed, gate the reservation with a media query in app.css is Plan 01's domain — here, the narrow drawer is `position: fixed` and overlays, so the board does not need to shrink).
    - Panel body (shared between both docks — factor into the same markup): `<div class="cy-chat-head"><span>// chat.{activeTab}</span><div class="cy-chat-tabs">{#if showAllTab}<button type="button" class={activeTab === 'all' ? 'is-active' : ''} onclick={() => (activeTab = 'all')}>all</button>{/if}<button type="button" class={activeTab === roleTabKey ? 'is-active' : ''} onclick={() => (activeTab = roleTabKey)}>{role === 'player' ? 'team' : 'spec'}</button></div></div>` (preserve the role/showAllTab gating; keep `role="tab"`/`aria-selected`/`tabindex` a11y attributes on the buttons as today).
    - List: `<div class="cy-chat-list" role="log" aria-live="polite" aria-label="Messages" bind:this={listEl}>{#if messages.length === 0}<div class="cy-chat-msg"><span class="cy-chat-body">// no messages yet</span></div>{:else}{#each messages as msg (msg.ts + msg.sender)}<ChatMessage sender={msg.sender} body={msg.body} ts={msg.ts} isSelf={currentUserName != null && msg.sender === currentUserName} />{/each}{/if}<div class="cy-chat-cursor">$ _</div></div>` (KEEP `bind:this={listEl}` so auto-scroll keeps working).
    - Input: keep `<ChatInput {onSend} bind:error={inputError} />` but wrap so the cyber input chrome (`.cy-chat-input`) lives in ChatInput itself (see below). Render `<ChatInput … />` directly under the list.

    ChatMessage.svelte — `<div class="cy-chat-msg {isSelf ? 'is-self' : ''}"><span class="cy-chat-prefix">{isSelf ? '&gt;' : '&lt;'}</span><span class="cy-chat-sender">{isSelf ? 'you' : sender}:</span><span class="cy-chat-body">{body}</span></div>` (KEEP sender/body/ts/isSelf props; the `timeStr` derive may be dropped if the Cyber row omits the timestamp — removing it is acceptable since the cyber.jsx row has no time; if you keep it, append `<span class="cy-chat-prefix">{timeStr}</span>` — either is fine, but prefer the cyber.jsx shape with no timestamp).

    ChatInput.svelte — KEEP onSend/disabled/error/body/canSend/handleSend/handleKeydown EXACTLY. Replace markup with the Cyber input row: `<div class="cy-chat-input"><span>&gt;</span><input type="text" aria-label="Message" placeholder="enter message" bind:value={body} onkeydown={handleKeydown} {disabled} /></div>{#if error}<div class="cy-chat-msg"><span class="cy-chat-body">{error}</span></div>{/if}`. (Switch the textarea to a single-line `<input>` to match `.cy-chat-input input` — Enter-to-send already handled by handleKeydown; the char-count UI is dropped as the validation error still surfaces via the existing `error` binding. KEEP the `onSend({ body: body.trim() })` call and the `body = ''` reset.)

    Validate all four with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-draft cy-draft-chat-sidebar" src/lib/components/molecules/DraftBoard.svelte` matches AND `grep "cy-draft-grid" src/lib/components/molecules/DraftBoard.svelte` matches
    - `grep "PauseOverlay" src/lib/components/molecules/DraftBoard.svelte` still matches (Plan 06's component still composed, untouched)
    - `grep "onPickBan" src/lib/components/molecules/DraftBoard.svelte` still matches AND `grep "isActiveCaptain" src/lib/components/molecules/DraftBoard.svelte` still matches (draft wiring preserved)
    - `grep -E "cy-chat-right|cy-chat-drawer" src/lib/components/molecules/ChatPanel.svelte` matches (both docks present) AND `grep "cy-chat-tabs" src/lib/components/molecules/ChatPanel.svelte` matches AND `grep "// chat\." src/lib/components/molecules/ChatPanel.svelte` matches
    - `grep "matchMedia" src/lib/components/molecules/ChatPanel.svelte` matches (responsive dock, D-02)
    - `grep "onSend" src/lib/components/molecules/ChatPanel.svelte` still matches AND `grep "activeTab" src/lib/components/molecules/ChatPanel.svelte` still matches AND `grep "bind:this={listEl}" src/lib/components/molecules/ChatPanel.svelte` still matches (send + tab + auto-scroll preserved)
    - `grep "cy-chat-msg" src/lib/components/atoms/ChatMessage.svelte` matches AND `grep "cy-chat-sender" src/lib/components/atoms/ChatMessage.svelte` matches
    - `grep "cy-chat-input" src/lib/components/atoms/ChatInput.svelte` matches AND `grep "handleKeydown" src/lib/components/atoms/ChatInput.svelte` still matches AND `grep "onSend" src/lib/components/atoms/ChatInput.svelte` still matches (Enter-to-send + send preserved)
    - `grep -cE "rounded-md|border-bg-secondary|text-text-|bg-amber|w-\[280px\]|flex-shrink-0|min-h-\[44px\]" src/lib/components/molecules/DraftBoard.svelte src/lib/components/molecules/ChatPanel.svelte src/lib/components/atoms/ChatMessage.svelte src/lib/components/atoms/ChatInput.svelte` returns 0 (Tailwind gone across all four)
    - `npm run check` passes
  </acceptance_criteria>
  <done>DraftBoard wraps the board in `.cy-draft cy-draft-chat-sidebar` with the `.cy-draft-grid`; the PauseOverlay invocation + all draft derives/handlers are untouched; ChatPanel renders the Cyber `.cy-chat` panel docked right on desktop (`cy-chat-right`) and as a toggleable `cy-chat-drawer` on narrow widths (matchMedia, D-02) with tabs/list/cursor/input; ChatMessage + ChatInput are Cyber-styled with send/Enter behavior preserved; no Tailwind.</done>
</task>

</tasks>

<verification>
- `npm run check` passes
- Drafting renders: turn readout + clock (red+pulse final 5s) + pips; A-lime/B-violet columns with struck bans; champ grid + `$ {action}({name}) [LOCK_IN]`; chat sidebar on desktop / drawer on narrow
- pickBan / chat sendMessage / snapshot timer reads / captain gating all unchanged; PauseOverlay invocation untouched; no Tailwind in the ten files
- Existing browser specs still green (`npm run test`)
</verification>

<success_criteria>
UI-04 met: Drafting renders the turn readout + countdown clock with the final-5s urgency state (D-04, color-only under reduced motion), the champion catalog grid, both teams' pick (lime) / ban (red, struck) columns, and team chat as a responsive sidebar↔drawer (D-02) — all in Cyber style with the verbatim copy, while every frozen draft/chat handler and the snapshot reads stay byte-for-byte unchanged (D-01).
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-screen-reskins/10-05-SUMMARY.md`
</output>
