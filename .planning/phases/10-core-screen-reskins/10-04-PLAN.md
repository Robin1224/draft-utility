---
phase: 10-core-screen-reskins
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/routes/draft/[id]/+page.svelte
  - src/lib/components/molecules/TeamColumn.svelte
  - src/lib/components/molecules/LobbyHostBar.svelte
  - src/lib/components/molecules/SpectatorsPanel.svelte
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - "Lobby renders the LOBBY.INIT() banner, two team columns (TEAM_A lime / TEAM_B violet) showing filled slots and <EMPTY> placeholders with the captain marked, a spectators strip, and the host console bar in Cyber style"
    - "Joining a team still calls joinTeam (JOIN_TEAM_A()/JOIN_TEAM_B()); host move/start/cancel still call movePlayer/startDraft/cancelRoom; mute/unmute still call muteMember/unmuteMember — all behavior unchanged (D-01)"
    - "No Tailwind utility classes remain on the lobby route wrapper or the three restyled molecules"
  artifacts:
    - path: "src/lib/components/molecules/TeamColumn.svelte"
      provides: "Cyber team column (.cy-team lime/violet, slot list, <EMPTY> slots, captain tag, JOIN_TEAM_x() CTA — unchanged onJoin)"
      contains: "cy-team"
    - path: "src/lib/components/molecules/LobbyHostBar.svelte"
      provides: "Cyber host console (.cy-host-panel: EXEC/CONFIG/CANCEL/▶ START_DRAFT() — unchanged move/start/cancel handlers)"
      contains: "START_DRAFT()"
    - path: "src/lib/components/molecules/SpectatorsPanel.svelte"
      provides: "Cyber spectators strip (.cy-spec pills — unchanged mute/unmute)"
      contains: "cy-spec"
    - path: "src/routes/draft/[id]/+page.svelte"
      provides: "Lobby branch wrapped in .cy-lobby with the banner; route-level Tailwind wrappers replaced (no duplicate Header)"
      contains: "cy-lobby"
  key_links:
    - from: "src/lib/components/molecules/TeamColumn.svelte"
      to: "joinTeam RPC (via onJoin)"
      via: "JOIN_TEAM_x() button onclick preserved"
      pattern: "onJoin"
    - from: "src/lib/components/molecules/LobbyHostBar.svelte"
      to: "movePlayer / startDraft / cancelRoom RPCs"
      via: "onMove/onStartDraft/onCancelRoom handlers preserved"
      pattern: "onStartDraft|onCancelRoom|onMove"
---

<objective>
Reskin the Lobby screen (UI-03) to the Cyber direction: the `LOBBY.INIT()` banner with stat readout, two team columns (`TEAM_A` lime / `TEAM_B` violet) rendering filled slots vs `<EMPTY>` placeholders with the captain marked, the spectators strip, and the host console bar (`EXEC` · `CONFIG` · `CANCEL` · `▶ START_DRAFT()`). Restyle in place (D-01) — every `$live` handler (joinTeam, movePlayer, startDraft, cancelRoom, muteMember/unmuteMember) stays untouched.

Purpose: Delivers UI-03. This plan OWNS `src/routes/draft/[id]/+page.svelte` for the whole phase (the single phase-switched route file), replacing its route-level Tailwind wrappers so Plans 05-07 only touch their molecules.
Output: Cyber lobby route branch + restyled TeamColumn / LobbyHostBar / SpectatorsPanel.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-screen-reskins/10-CONTEXT.md
@.planning/phases/10-core-screen-reskins/10-UI-SPEC.md

# Lobby structure reference (React → reimplement as Svelte 5; do not transcribe wholesale, D-01)
@design_handoff_pickban_cyber/prototype/variants/cyber.jsx

<interfaces>
<!-- The frozen $live wiring the lobby consumes — DO NOT change handler bodies or RPC calls. -->

src/routes/draft/[id]/+page.svelte is the SINGLE phase-switched route. It derives `snapshot`, `phase`, `isHost`, `isGuest`, `canJoin`, `fullA`, `fullB`, `loading`, `loadError`, and wires:
  handleJoinA/handleJoinB -> joinTeam(code, 'A'|'B')
  handleMove -> movePlayer(code, {userId, toTeam});  handleStart -> startDraft(code, {script, timerMs});  handleCancel -> cancelRoom(code)
  handleMute/handleUnmute -> muteMember/unmuteMember;  copyLink -> navigator.clipboard
It renders <Header><Phases …/></Header> at the top, then branches on snapshot.phase into: drafting/cancelled, review, else (lobby).

Lobby branch currently composes: <LobbyHostBar … bind:script bind:timerSeconds/>, a copy-link button, <TeamColumn label members teamKey isGuest canJoin full onJoin/> x2, <SpectatorsPanel spectators isHost mutedIds onMute onUnmute/>, and <ChatPanel … bind:activeTab/>.

TeamColumn props: { label, members, teamKey:'A'|'B', isGuest, canJoin, full, onJoin }. members[i] = { displayName, isCaptain, isHost, userId?, guestId? }.
LobbyHostBar props: { isHost, snapshot, onKick, onMove, onStartDraft, onCancelRoom, script (bindable), timerSeconds (bindable) }. startDisabled = phase!=='lobby' || !hasCaptainA || !hasCaptainB. Settings toggle opens DraftSettingsPanel (its modal reskin is Phase 11 — leave DraftSettingsPanel alone here).
SpectatorsPanel props: { spectators, isHost, mutedIds, onMute, onUnmute }. Renders MuteButton per row when isHost.

NOTE: ChatPanel is reskinned by Plan 05 (Drafting+Chat). Do NOT edit ChatPanel here. The layout CyShell renders the persistent header — but this route renders its OWN <Header><Phases/></Header>. Keep the Phases tracker working, but remove the inner <Header> wrapper duplication if it double-renders the brand (see Task 4); confirm against CyShell before deleting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin TeamColumn as the Cyber team column (.cy-team, slots, captain tag, JOIN_TEAM CTA)</name>
  <read_first>
    - src/lib/components/molecules/TeamColumn.svelte (current Tailwind — KEEP the `onJoin` prop + the canJoin/full/isGuest gating logic and the loginHref guest fallback)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYTeamLobby ~313-345 — exact slot/EMPTY/captain/JOIN_TEAM markup)
    - src/app.css (after Plan 01 — `.cy-team`, `.cy-team-lime`, `.cy-team-violet`, `.cy-team-head`, `.cy-bracket`, `.cy-team-status`, `.cy-slot-list`, `.cy-slot`, `.cy-slot-empty`, `.cy-slot-num`, `.cy-slot-name`, `.cy-tag`, `.cy-tag-cap`, `.cy-btn`, `.cy-btn-lime`, `.cy-btn-violet`, `.cy-btn-block` exist)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Empty slot row: render dashed `cy-slot-empty` with `<EMPTY>`; Captain marker `cy-tag cy-tag-cap`; Lobby join CTA `JOIN_TEAM_A()`/`JOIN_TEAM_B()`)
  </read_first>
  <action>
    Rewrite TeamColumn.svelte to the Cyber team column. Remove ALL Tailwind classes. Add an accent derive: `const accent = $derived(teamKey === 'A' ? 'lime' : 'violet');`

    Root: `<div class="cy-team cy-team-{accent}">`
    - Head: `<div class="cy-team-head"><span class="cy-bracket">[</span>TEAM_{teamKey}<span class="cy-bracket">]</span><span class="cy-team-status">{members.length}/3</span></div>`
    - Slot list over `slotIndices` [0,1,2]: for each `slot`:
      - if `members[slot]` exists:
        `<li class="cy-slot"><span class="cy-slot-num">[{String(slot).padStart(2,'0')}]</span><span class="cy-slot-name">{members[slot].displayName}</span>{#if members[slot].isCaptain}<span class="cy-tag cy-tag-cap">CAPTAIN</span>{/if}{#if members[slot].isHost}<span class="cy-tag">HOST</span>{/if}</li>`
      - else (empty): `<li class="cy-slot cy-slot-empty"><span class="cy-slot-num">[{String(slot).padStart(2,'0')}]</span><span class="cy-slot-name">&lt;EMPTY&gt;</span></li>`
      Wrap in `<ul class="cy-slot-list"> … </ul>`.
    - Join CTA — PRESERVE the existing gating exactly (only the canJoin && !full && !isGuest branch shows the button; isGuest shows the sign-in link; full shows "team full"):
      - `<button type="button" class="cy-btn cy-btn-{accent} cy-btn-block" onclick={onJoin}>JOIN_TEAM_{teamKey}()</button>`
      - Keep the guest fallback link using the existing `loginHref` derive (style it as a `cy-btn cy-btn-ghost cy-btn-block` link or keep an inline anchor — but the `loginHref` value/logic stays).
      - Keep a `cy-foot`-style "// team full" note for the full branch.
    Keep `joinLabel` only if still referenced; otherwise remove it (the CTA text is now `JOIN_TEAM_{teamKey}()`).

    Validate with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-team cy-team-" src/lib/components/molecules/TeamColumn.svelte` matches
    - `grep "cy-slot-empty" src/lib/components/molecules/TeamColumn.svelte` matches
    - `grep "&lt;EMPTY&gt;" src/lib/components/molecules/TeamColumn.svelte` matches (empty slot label)
    - `grep "cy-tag cy-tag-cap" src/lib/components/molecules/TeamColumn.svelte` matches AND `grep "CAPTAIN" src/lib/components/molecules/TeamColumn.svelte` matches
    - `grep "JOIN_TEAM_" src/lib/components/molecules/TeamColumn.svelte` matches
    - `grep "onJoin" src/lib/components/molecules/TeamColumn.svelte` still matches (join behavior preserved)
    - `grep "loginHref" src/lib/components/molecules/TeamColumn.svelte` still matches (guest fallback preserved)
    - `grep -cE "rounded-|bg-amber|text-text-|min-h-11|border-bg-secondary" src/lib/components/molecules/TeamColumn.svelte` returns 0 (Tailwind gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>TeamColumn renders the Cyber `.cy-team` column with lime/violet accent, `[NN]` numbered slots, `<EMPTY>` placeholders, the CAPTAIN tag, and a JOIN_TEAM_x() CTA that still fires onJoin; guest/full gating preserved; no Tailwind.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin LobbyHostBar as the Cyber host console (EXEC/CONFIG/CANCEL/▶ START_DRAFT())</name>
  <read_first>
    - src/lib/components/molecules/LobbyHostBar.svelte (current Tailwind — KEEP every handler/derive: moveUserId/moveTarget state, submitMove→onMove, onStartDraft, onCancelRoom, settingsOpen→DraftSettingsPanel, the kick list→onKick, startDisabled, showCaptainHint, hasCaptainA/B)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYLobbyBody host panel ~365-376 — `.cy-host-panel`/`.cy-host-head` [HOST_CONSOLE] / `.cy-host-row` with EXEC/CONFIG/CANCEL/▶ START_DRAFT())
    - src/app.css (after Plan 01 — `.cy-host-panel`, `.cy-host-head`, `.cy-host-row`, `.cy-grow`, `.cy-input`, `.cy-btn`, `.cy-btn-sm`, `.cy-btn-danger`, `.cy-btn-primary` exist)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Lobby host bar copy: `EXEC` · `CONFIG` · `CANCEL` (danger) · `▶ START_DRAFT()`; destructive uses cy-btn-danger; confirmation behavior unchanged; modals are Phase 11)
  </read_first>
  <action>
    Rewrite the `{#if isHost}` host section of LobbyHostBar.svelte to the Cyber host console. Remove ALL Tailwind classes. Preserve EVERY existing handler and the bind:script/bind:timerSeconds passthrough to DraftSettingsPanel (do NOT restyle DraftSettingsPanel — that's Phase 11; just keep the toggle + render).

    Structure:
    - `<section class="cy-host-panel" aria-label="Host controls">`
    - `<div class="cy-host-head">[HOST_CONSOLE]</div>`
    - `<div class="cy-host-row">` containing, IN ORDER:
      - The move-player `<select class="cy-input" bind:value={moveUserId}>` (keep its `--move--`/player options) and the to-team `<select class="cy-input" bind:value={moveTarget}>` (A/B). Keep their labels (can be visually-hidden or inline; preserve the `for`/`id` association for a11y).
      - `<button type="button" class="cy-btn cy-btn-sm" onclick={submitMove} disabled={!moveUserId || snapshot.phase !== 'lobby'}>EXEC</button>`
      - `<button type="button" class="cy-btn cy-btn-sm" aria-expanded={settingsOpen} aria-controls="draft-settings-panel" onclick={() => (settingsOpen = !settingsOpen)}>CONFIG</button>` (keep the `{#if snapshot.phase === 'lobby'}` guard around it as today)
      - `<div class="cy-grow"></div>`
      - `<button type="button" class="cy-btn cy-btn-sm cy-btn-danger" onclick={onCancelRoom}>CANCEL</button>` (destructive — cy-btn-danger; do NOT change existing confirm behavior)
      - `<button type="button" class="cy-btn cy-btn-primary" disabled={startDisabled} aria-disabled={startDisabled} onclick={onStartDraft}>▶ START_DRAFT()</button>`
    - Keep the captain hint as a `cy-foot`-style line: `{#if showCaptainHint}<span class="cy-foot">// both teams need a captain</span>{/if}`
    - Keep the `{#if snapshot.phase === 'lobby' && settingsOpen}<DraftSettingsPanel bind:script bind:timerSeconds />{/if}` block unchanged.
    - Keep the kick list (`rosterForKick` → onKick) but restyle to cyber: a `cy-spec`-style block with each kickable non-host member as a row showing name + `Team {side}` + a `<button class="cy-btn cy-btn-sm cy-btn-danger" onclick={() => onKick(kickPayload(m))}>KICK</button>`. Preserve `kickPayload` + the `!m.isHost` filter.

    Validate with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-host-panel" src/lib/components/molecules/LobbyHostBar.svelte` matches
    - `grep "\[HOST_CONSOLE\]" src/lib/components/molecules/LobbyHostBar.svelte` matches
    - `grep "▶ START_DRAFT()" src/lib/components/molecules/LobbyHostBar.svelte` matches
    - `grep "EXEC" src/lib/components/molecules/LobbyHostBar.svelte` matches AND `grep "CONFIG" src/lib/components/molecules/LobbyHostBar.svelte` matches
    - `grep "cy-btn-danger" src/lib/components/molecules/LobbyHostBar.svelte` matches (CANCEL + KICK destructive styling)
    - `grep "onStartDraft" src/lib/components/molecules/LobbyHostBar.svelte` still matches AND `grep "onCancelRoom" src/lib/components/molecules/LobbyHostBar.svelte` still matches AND `grep "submitMove" src/lib/components/molecules/LobbyHostBar.svelte` still matches (behavior preserved)
    - `grep "startDisabled" src/lib/components/molecules/LobbyHostBar.svelte` still matches (captain-gating preserved)
    - `grep "DraftSettingsPanel" src/lib/components/molecules/LobbyHostBar.svelte` still matches (Phase 11 panel left intact)
    - `grep "onKick" src/lib/components/molecules/LobbyHostBar.svelte` still matches AND `grep "kickPayload" src/lib/components/molecules/LobbyHostBar.svelte` still matches
    - `grep -cE "rounded-|bg-amber|text-text-|hover:bg-bg-secondary" src/lib/components/molecules/LobbyHostBar.svelte` returns 0 (Tailwind gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>LobbyHostBar renders the Cyber [HOST_CONSOLE] panel with EXEC/CONFIG/CANCEL(danger)/▶ START_DRAFT(), still firing movePlayer/startDraft/cancelRoom/kick and gating Start on both captains; DraftSettingsPanel toggle intact; no Tailwind.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin SpectatorsPanel as the Cyber spectators strip (.cy-spec pills)</name>
  <read_first>
    - src/lib/components/molecules/SpectatorsPanel.svelte (current Tailwind collapsible — KEEP the `open` toggle state and the MuteButton wiring → onMute/onUnmute, the isMuted derive, the mutedIds membership check)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYLobbyBody spectators ~383-390 — `.cy-spec`/`.cy-spec-head` `// SPECTATORS [n]` / `.cy-spec-list` / `.cy-spec-pill`)
    - src/app.css (after Plan 01 — `.cy-spec`, `.cy-spec-head`, `.cy-spec-list`, `.cy-spec-pill` exist)
  </read_first>
  <action>
    Rewrite SpectatorsPanel.svelte to the Cyber spectators strip. Remove ALL Tailwind classes (and the chevron SVG rotate utility classes). You MAY keep the collapse toggle (`open` state) — render the head as the toggle button — or render the strip always-open per cyber.jsx; either is acceptable, but PRESERVE the host mute controls.

    Structure:
    - Root `<div class="cy-spec">`
    - `<div class="cy-spec-head">// SPECTATORS [{spectators.length}]</div>` (this is the toggle button if you keep collapse; keep `aria-expanded`/`aria-controls` in that case)
    - `<div class="cy-spec-list">` with one `cy-spec-pill` per spectator: `<div class="cy-spec-pill">{row.displayName}{#if isMuted} (muted){/if}{#if isHost}<MuteButton …/>{/if}</div>` — PRESERVE the `MuteButton` props (`isMuted`, `displayName`, `onMute`/`onUnmute` calling onMute/onUnmute with `{userId, guestId}`) and the `isMuted` derive (`mutedIds.includes(...)`).
    - Empty state: `{#if spectators.length === 0}<div class="cy-spec-pill">// no_spectators</div>{/if}` (terminal-style empty per UI-SPEC).

    Validate with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-spec-head" src/lib/components/molecules/SpectatorsPanel.svelte` matches
    - `grep "// SPECTATORS" src/lib/components/molecules/SpectatorsPanel.svelte` matches
    - `grep "cy-spec-pill" src/lib/components/molecules/SpectatorsPanel.svelte` matches
    - `grep "MuteButton" src/lib/components/molecules/SpectatorsPanel.svelte` still matches AND `grep "onMute" src/lib/components/molecules/SpectatorsPanel.svelte` still matches (mute behavior preserved)
    - `grep "mutedIds" src/lib/components/molecules/SpectatorsPanel.svelte` still matches
    - `grep -cE "rounded-|border-bg-secondary|text-text-|h-5 w-5" src/lib/components/molecules/SpectatorsPanel.svelte` returns 0 (Tailwind gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>SpectatorsPanel renders the Cyber `// SPECTATORS [n]` strip of `.cy-spec-pill`s with host mute/unmute preserved; no Tailwind.</done>
</task>

<task type="auto">
  <name>Task 4: Wrap the lobby route branch in .cy-lobby + the LOBBY.INIT() banner; strip route-level Tailwind</name>
  <read_first>
    - src/routes/draft/[id]/+page.svelte (current — the lobby `{:else}` branch ~324-389 wraps TeamColumn x2 + SpectatorsPanel + ChatPanel in Tailwind grids; the `<main class={mainClass}>` and `<Header>` at top; the drafting/cancelled and review branches also have Tailwind wrappers)
    - src/lib/components/chrome/CyShell.svelte (confirm whether the layout already renders the header brand; if it does, the route's inner <Header> double-renders — see action)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYLobbyBody ~348-391 — `.cy-lobby` wraps the banner + host panel + `.cy-lobby-grid` (two teams) + spectators)
    - src/app.css (after Plan 01 — `.cy-lobby`, `.cy-banner`, `.cy-banner-eyebrow`, `.cy-banner-stats`, `.cy-lobby-grid` exist)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Lobby banner copy: `// status: AWAITING_HOST_SIGNAL`, `LOBBY.INIT()`, `> assemble both teams…`; banner-stats PLAYERS/SPECTATORS/TURNS)
  </read_first>
  <action>
    Edit ONLY the route-level layout/wrappers in src/routes/draft/[id]/+page.svelte. Do NOT change any handler, derive, or `$live` import. Do NOT edit ChatPanel usage (Plan 05 owns ChatPanel; leave the `<ChatPanel … bind:activeTab/>` element as-is, including in the lobby and drafting branches).

    1. Header/chrome: Inspect CyShell. The persistent header is rendered by the layout. If the route's `<Header><Phases roomPhase=…/></Header>` duplicates the brand, replace it so the Phases tracker still renders without a second brand wordmark: prefer passing the phase to the layout's tracker if available; otherwise keep `<Phases roomPhase={roomPhaseForStrip} />` but remove the `<Header>` wrapper that duplicates the brand. Match the approach Plan 02 used for Home (`grep -c "import Header" src/routes/+page.svelte` was driven to 0). Confirm against CyShell before deleting — if CyShell does NOT render its own header, leave `<Header>` in place. (Decision criterion: avoid a duplicate bracketed wordmark on screen.)

    2. Replace `<main class={mainClass}>` (and the `mainClass` derive `'flex flex-row items-start gap-4 px-4 py-6 text-text-primary'`) with a plain `<main>` (no Tailwind). Remove the `mainClass` derive.

    3. Lobby `{:else}` branch: replace the Tailwind wrappers with the `.cy-lobby` structure:
       - Root `<div class="cy-lobby">` (replacing `<div class="flex-1 min-w-0 mx-auto w-full max-w-3xl">`).
       - Banner FIRST: `<div class="cy-banner"><div><div class="cy-banner-eyebrow">// status: AWAITING_HOST_SIGNAL</div><h2>LOBBY.INIT()</h2><p>&gt; assemble both teams. captains required. configure script via [SETTINGS].</p></div><div class="cy-banner-stats"><div><b>{snapshot.teams.A.length + snapshot.teams.B.length}</b><span>PLAYERS</span></div><div><b>{snapshot.spectators.length}</b><span>SPECTATORS</span></div><div><b>{snapshot.draftState?.script?.length ?? 10}</b><span>TURNS</span></div></div></div>` (use real snapshot values; if script length is unavailable in lobby, fall back to 10).
       - Then `<LobbyHostBar … />` (unchanged props/binds).
       - Replace the copy-link Tailwind button with `<button type="button" class="cy-btn cy-btn-sm" onclick={copyLink} aria-label="Copy room link">$ COPY_LINK()</button>{#if copied}<span class="cy-foot">// copied</span>{/if}` (keep copyLink + copied behavior).
       - Two team columns in `<div class="cy-lobby-grid"><TeamColumn label="Team A" … /><TeamColumn label="Team B" … /></div>` (keep ALL existing props: members, teamKey, isGuest, canJoin, full, onJoin).
       - `<SpectatorsPanel … />` unchanged props.
       - Leave the `<ChatPanel … bind:activeTab/>` element exactly as it is.

    4. Drafting/cancelled + review branches: replace ONLY their Tailwind wrapper `class="…"` strings on the outer container divs with semantic non-Tailwind wrappers (e.g. plain `<div>` — Plans 05/07 restyle the inner board/review content). Do NOT remove the branches or change the components they render. Specifically: the drafting branch's `<div class="flex-1 min-w-0"><div class="mx-auto max-w-6xl">` → plain `<div>`s; the review branch's `<div class="flex w-full flex-col items-center gap-8 px-4 py-8 text-text-primary">` and its inner `<h2 class="text-2xl …">Draft complete</h2>` + Tailwind buttons → leave the elements but strip Tailwind classes to bare (Plan 07 fully restyles the review content + copy strings).

    After edits, `grep -cE "flex|grid-cols|max-w-|text-text-|rounded-|bg-bg-|px-4|py-6" src/routes/draft/[id]/+page.svelte` should be 0 for the wrappers you touched. (Some Tailwind may remain ONLY inside `<ChatPanel>`-internal code — but ChatPanel is a separate file, so the route file itself must be Tailwind-free after this task.)

    Validate with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-lobby" src/routes/draft/[id]/+page.svelte` matches
    - `grep "LOBBY.INIT()" src/routes/draft/[id]/+page.svelte` matches AND `grep "// status: AWAITING_HOST_SIGNAL" src/routes/draft/[id]/+page.svelte` matches
    - `grep "cy-banner-stats" src/routes/draft/[id]/+page.svelte` matches AND `grep "PLAYERS" src/routes/draft/[id]/+page.svelte` matches
    - `grep "cy-lobby-grid" src/routes/draft/[id]/+page.svelte` matches
    - `grep "joinTeam" src/routes/draft/[id]/+page.svelte` still matches AND `grep "startDraft" src/routes/draft/[id]/+page.svelte` still matches AND `grep "pickBan" src/routes/draft/[id]/+page.svelte` still matches (frozen $live wiring untouched)
    - `grep "ChatPanel" src/routes/draft/[id]/+page.svelte` still matches (Plan 05's component still composed)
    - `grep -cE "flex-row|grid-cols-|max-w-3xl|max-w-6xl|text-text-primary|mainClass" src/routes/draft/[id]/+page.svelte` returns 0 (route-level Tailwind + mainClass derive gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>The lobby branch renders inside `.cy-lobby` with the LOBBY.INIT() banner + stats, the host console, two Cyber team columns, and the spectators strip; route-level Tailwind wrappers and the mainClass derive are gone; all `$live` handlers and the ChatPanel composition are untouched.</done>
</task>

</tasks>

<verification>
- `npm run check` passes
- Lobby renders banner → host console → two team columns (lime/violet, <EMPTY> slots, CAPTAIN tag) → spectators strip, all in Cyber style
- joinTeam / movePlayer / startDraft / cancelRoom / mute behavior unchanged; ChatPanel still composed; no route-level Tailwind
</verification>

<success_criteria>
UI-03 met: Lobby renders the two team columns (filled / `<EMPTY>`, captain marked), the spectators strip, and the host bar in Cyber style with the verbatim banner/console copy, while every frozen `$live` handler (join/move/start/cancel/mute) and the snapshot wiring stay byte-for-byte unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-screen-reskins/10-04-SUMMARY.md`
</output>
