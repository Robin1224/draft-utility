---
phase: 10-core-screen-reskins
plan: 07
type: execute
wave: 3
depends_on: [01, 04]
files_modified:
  - src/lib/components/molecules/DraftReview.svelte
  - src/routes/draft/[id]/+page.svelte
autonomous: true
requirements: [UI-06]
must_haves:
  truths:
    - "Review renders the Cyber `.cy-review` layout: a `.cy-review-head` (`// status: COMPLETE` eyebrow, `$ DRAFT.RESULT()` heading, a receipt block) and a two-column `.cy-review-grid` of both teams' final compositions"
    - "Each team column (`.cy-review-team` lime/violet) shows its roster head, its final picks as `.cy-review-pick` cards (art + name + role), and its full ban list as struck `.cy-review-ban` pills under a `BANS:` label"
    - "The review actions render `$ COPY_LINK()` (default) and `$ NEW_DRAFT()` (primary) — `$ COPY_LINK()` still calls the existing copyLink handler"
    - "Review stays viewable without auth — the existing review branch render path and data source (data.actions || snapshot.actions, data.teams || snapshot.teams) are unchanged (D-01, UI-06)"
    - "No Tailwind utility classes remain in DraftReview.svelte or the review branch of the route file"
  artifacts:
    - path: "src/lib/components/molecules/DraftReview.svelte"
      provides: "Cyber review compositions (.cy-review-grid, .cy-review-team lime/violet, .cy-review-pick cards, struck .cy-review-ban list) — unchanged actions/teams props + champion resolution"
      contains: "cy-review-grid"
    - path: "src/routes/draft/[id]/+page.svelte"
      provides: "Review branch wrapped in .cy-review with head + receipt + Cyber actions ($ COPY_LINK() / $ NEW_DRAFT()) — copyLink + data source unchanged"
      contains: "cy-review"
  key_links:
    - from: "src/routes/draft/[id]/+page.svelte"
      to: "navigator.clipboard (copyLink)"
      via: "$ COPY_LINK() button onclick={copyLink} preserved"
      pattern: "copyLink"
    - from: "src/lib/components/molecules/DraftReview.svelte"
      to: "actions / teams props + classes catalog"
      via: "resolvedPicks/resolvedBans champion-name resolution unchanged"
      pattern: "resolvedPicks|resolvedBans|classes"
---

<objective>
Reskin the Review screen (UI-06) to the Cyber direction: the `.cy-review-head` (`// status: COMPLETE` → `$ DRAFT.RESULT()` → receipt), the two-column `.cy-review-grid` of both teams' final compositions (picks as `.cy-review-pick` cards, full ban list as struck `.cy-review-ban` pills), and the `$ COPY_LINK()` / `$ NEW_DRAFT()` actions. Restyle in place (D-01): DraftReview keeps its `{ actions, teams }` props + champion-name resolution, the route's review branch keeps its data source and the `copyLink` handler, and Review remains viewable without auth (UI-06).

Purpose: Delivers UI-06. This plan edits BOTH DraftReview.svelte and the review branch of `src/routes/draft/[id]/+page.svelte`. The route file is owned by Plan 04 (which stripped its Tailwind and left the review content as a placeholder for this plan), so this plan depends on [01, 04] and runs in Wave 3 to avoid a write conflict.
Output: Cyber review screen (head + receipt + two team compositions + ban lists + actions).
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-screen-reskins/10-CONTEXT.md
@.planning/phases/10-core-screen-reskins/10-UI-SPEC.md

# Review structure reference (React → reimplement as Svelte 5; D-01)
@design_handoff_pickban_cyber/prototype/variants/cyber.jsx

<interfaces>
<!-- The frozen wiring the review screen consumes — DO NOT change props, data source, or copyLink. -->
<!-- All .cy-review* classes referenced below already exist in src/app.css after Plan 01 (verbatim port). -->

DraftReview.svelte props: { actions: Array<{team, action, champion_id}>, teams: { A: any[], B: any[] } }.
  - resolvedBansA/resolvedPicksA/resolvedBansB/resolvedPicksB derives filter actions by team+action and resolve champion_id → name via `classes` catalog (KEEP these EXACTLY — they are the champion resolution).
  - isEmpty derive (all four empty). KEEP it; render a terminal empty state when true.
  - Today it renders a Tailwind grid of <DraftSlot> rows. Replace with `.cy-review-grid` markup; STOP using DraftSlot here (DraftSlot is owned by Plan 05 for the draft board — review uses its own `.cy-review-pick`/`.cy-review-ban` markup). Remove the `import DraftSlot` from THIS file (DraftReview), not from the atom.
  - The teams roster: teams.A / teams.B are LobbyMember[] each with `.displayName`. Use for the roster head line.

Route review branch (src/routes/draft/[id]/+page.svelte, the `{:else if snapshot.phase === 'review'}` branch):
  - After Plan 04, this branch is Tailwind-stripped placeholder markup still containing: a heading, a Back-to-home link, a Copy-link button (onclick={copyLink}), a `{#if copied}` indicator, and <DraftReview actions={data.actions?.length ? data.actions : (snapshot.actions ?? [])} teams={data.teams ?? snapshot.teams} />.
  - `copyLink` (writes fullUrl to clipboard, toggles `copied`) and the `data.actions || snapshot.actions` / `data.teams || snapshot.teams` data source are FROZEN — keep them. This branch renders for guests too (no auth gate) — do NOT add any auth check (UI-06: viewable without auth).
  - `$ NEW_DRAFT()` should navigate home/create — the existing branch has a Back-to-home `<a href="/">`; reuse that link as the `$ NEW_DRAFT()` primary action (the home route hosts CREATE_DRAFT()). Keep `$ COPY_LINK()` wired to copyLink.

Verbatim Cyber copy/markup (UI-SPEC + cyber.jsx CYReview ~571-622):
  Head: `.cy-review-head` > `<div class="cy-review-eyebrow">// status: COMPLETE</div>` + `<h2>$ DRAFT.RESULT()</h2>` + `<pre class="cy-review-receipt">code: {code}\nturns: {n}\n…</pre>`.
  Grid: `.cy-review-grid` of two `.cy-review-team cy-review-team-{lime|violet}` (A=lime, B=violet).
  Team head: `> TEAM_{X}.roster = ["name", …]`.
  Picks: `.cy-review-picks` > per pick `.cy-review-pick` > `.cy-review-pick-art cy-champ-art-{role}` (first-2-letters) + `.cy-review-pick-name` + `.cy-review-pick-role` `.{role}`.
  Bans: `.cy-review-bans` > `<span>BANS:</span>` + per ban `<span class="cy-review-ban">{name}</span>` (struck via CSS).
  Actions: `.cy-review-actions` > `<button class="cy-btn">$ COPY_LINK()</button>` + `<button class="cy-btn cy-btn-primary">$ NEW_DRAFT()</button>`.
  NOTE on role: review picks need a role for the art tint. DraftReview currently resolves only champion NAMES. Resolve the full champ object (id→{name, role}) so `.cy-champ-art-{role}` can be applied; the `classes` catalog has `.role`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin DraftReview as the Cyber compositions grid (.cy-review-grid, picks + struck ban list)</name>
  <read_first>
    - src/lib/components/molecules/DraftReview.svelte (current Tailwind — KEEP the actions/teams props; KEEP the action-filtering logic but extend it to resolve full champ objects {name, role}; KEEP isEmpty; REMOVE the DraftSlot import + usage)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYReview team columns ~590-613 — `.cy-review-team`/`.cy-review-team-head`/`.cy-review-picks`/`.cy-review-pick`/`.cy-review-bans`/`.cy-review-ban`)
    - src/lib/catalog/classes.json (champion catalog — id/name/role, already imported)
    - src/app.css (after Plan 01 — `.cy-review-grid`, `.cy-review-team`, `.cy-review-team-lime`, `.cy-review-team-violet`, `.cy-review-team-head`, `.cy-review-picks`, `.cy-review-pick`, `.cy-review-pick-art`, `.cy-review-pick-name`, `.cy-review-pick-role`, `.cy-review-bans`, `.cy-review-ban`, `.cy-champ-art-{role}` all exist)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Review: both compositions + full ban list + recap; bans struck; viewable without auth)
  </read_first>
  <action>
    Rewrite DraftReview.svelte. Remove ALL Tailwind and the `import DraftSlot`. KEEP `{ actions, teams }` props and the `classes` import.

    Change the four derives to resolve full champ objects (so the art tint can use `.role`), keeping the same filter logic:
    ```js
    const lookup = (id) => classes.find((c) => c.id === id) ?? { id, name: id, role: 'melee' };
    const picksA = $derived(actions.filter((a) => a.team === 'A' && a.action === 'pick' && a.champion_id != null).map((a) => lookup(a.champion_id)));
    const bansA  = $derived(actions.filter((a) => a.team === 'A' && a.action === 'ban'  && a.champion_id != null).map((a) => lookup(a.champion_id)));
    const picksB = $derived(actions.filter((a) => a.team === 'B' && a.action === 'pick' && a.champion_id != null).map((a) => lookup(a.champion_id)));
    const bansB  = $derived(actions.filter((a) => a.team === 'B' && a.action === 'ban'  && a.champion_id != null).map((a) => lookup(a.champion_id)));
    const isEmpty = $derived(picksA.length === 0 && bansA.length === 0 && picksB.length === 0 && bansB.length === 0);
    ```
    Add a roster-names helper: `const roster = (t) => (teams[t] ?? []).map((m) => '"' + m.displayName + '"').join(', ');`

    Markup — render the `.cy-review-grid` (this component renders ONLY the grid; the head/receipt/actions live in the route branch, Task 2):
    - `<div class="cy-review-grid">`
    - `{#if isEmpty}<div class="cy-review-team"><div class="cy-review-team-head">// no_data — draft ended without picks or bans</div></div>{:else}` then a small loop over the two teams. Build a config array `const cols = $derived([{ team: 'A', accent: 'lime', picks: picksA, bans: bansA }, { team: 'B', accent: 'violet', picks: picksB, bans: bansB }]);` and iterate:
      `{#each cols as col (col.team)}<div class="cy-review-team cy-review-team-{col.accent}"><div class="cy-review-team-head">&gt; TEAM_{col.team}.roster = [{roster(col.team)}]</div><div class="cy-review-picks">{#each col.picks as c, i (i)}<div class="cy-review-pick"><div class="cy-review-pick-art cy-champ-art-{c.role}"><span>{c.name.slice(0, 2)}</span></div><div class="cy-review-pick-name">{c.name}</div><div class="cy-review-pick-role">.{c.role}</div></div>{/each}</div><div class="cy-review-bans"><span>BANS:</span>{#each col.bans as c, i (i)}<span class="cy-review-ban">{c.name}</span>{/each}</div></div>{/each}`
    - `{/if}</div>`

    Validate with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-review-grid" src/lib/components/molecules/DraftReview.svelte` matches
    - `grep "cy-review-team cy-review-team-" src/lib/components/molecules/DraftReview.svelte` matches AND `grep "roster = \[" src/lib/components/molecules/DraftReview.svelte` matches
    - `grep "cy-review-pick-art" src/lib/components/molecules/DraftReview.svelte` matches AND `grep "cy-review-pick-name" src/lib/components/molecules/DraftReview.svelte` matches AND `grep "cy-review-pick-role" src/lib/components/molecules/DraftReview.svelte` matches
    - `grep "cy-review-ban" src/lib/components/molecules/DraftReview.svelte` matches AND `grep "BANS:" src/lib/components/molecules/DraftReview.svelte` matches
    - `grep "classes" src/lib/components/molecules/DraftReview.svelte` still matches (champion resolution preserved) AND `grep -c "import DraftSlot" src/lib/components/molecules/DraftReview.svelte` returns 0 (DraftSlot decoupled — owned by Plan 05)
    - `grep "actions" src/lib/components/molecules/DraftReview.svelte` still matches AND `grep "teams" src/lib/components/molecules/DraftReview.svelte` still matches (props preserved)
    - `grep -cE "grid-cols-1|md:grid-cols-2|max-w-3xl|text-text-|rounded-md|text-xl" src/lib/components/molecules/DraftReview.svelte` returns 0 (Tailwind gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>DraftReview renders the `.cy-review-grid` with both `.cy-review-team` columns (A lime / B violet): roster head, `.cy-review-pick` cards (art tint + name + role), and the full struck `.cy-review-ban` list under `BANS:`; champion resolution + actions/teams props preserved; DraftSlot decoupled; no Tailwind.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap the review branch in .cy-review with head + receipt + Cyber actions; strip its Tailwind</name>
  <read_first>
    - src/routes/draft/[id]/+page.svelte (after Plan 04 — the `{:else if snapshot.phase === 'review'}` branch; KEEP `copyLink`, `copied`, `actionError`, the `data.actions?.length ? data.actions : (snapshot.actions ?? [])` + `data.teams ?? snapshot.teams` data source, the `<a href="/">` link, and the fact that this branch renders for guests with NO auth gate — UI-06)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx (CYReview ~582-619 — `.cy-review`/`.cy-review-head`/`.cy-review-eyebrow`/`h2`/`.cy-review-receipt`/`.cy-review-actions`)
    - src/app.css (after Plan 01 — `.cy-review`, `.cy-review-head`, `.cy-review-eyebrow`, `.cy-review-head h2`, `.cy-review-receipt`, `.cy-review-actions`, `.cy-btn`, `.cy-btn-primary`, `.cy-foot` all exist)
    - .planning/phases/10-core-screen-reskins/10-UI-SPEC.md (Review actions `$ COPY_LINK()` / `$ NEW_DRAFT()`; receipt/recap; viewable without auth)
  </read_first>
  <action>
    Edit ONLY the review branch (`{:else if snapshot.phase === 'review'}`) of the route file. Do NOT change any handler, derive, `$live` import, or the lobby/drafting branches (those are settled by Plans 04/05). Do NOT add an auth gate — guests must keep seeing review (UI-06).

    Replace the branch's placeholder wrapper + heading + actions with the Cyber `.cy-review` head and actions, keeping `<DraftReview …/>` (Task 1) in the middle:
    - Root `<div class="cy-review">`.
    - Head: `<div class="cy-review-head"><div><div class="cy-review-eyebrow">// status: COMPLETE</div><h2>$ DRAFT.RESULT()</h2></div><pre class="cy-review-receipt">code: {code}
turns: {(data.actions?.length ? data.actions : (snapshot.actions ?? [])).length}
status: complete</pre></div>` — bind the receipt to real values: `code` (already derived in the route) and the actions count. Keep it a small recap; do NOT invent fake elapsed/hash values — use only real derived data (code + turn/action count + status).
    - Middle: keep the existing DraftReview element verbatim: `<DraftReview actions={data.actions?.length ? data.actions : (snapshot.actions ?? [])} teams={data.teams ?? snapshot.teams} />`.
    - Actions: `<div class="cy-review-actions"><button type="button" class="cy-btn" onclick={copyLink} aria-label="Copy room link">$ COPY_LINK()</button>{#if copied}<span class="cy-foot">// copied</span>{/if}{#if actionError}<span class="cy-foot">{actionError}</span>{/if}<a href="/" class="cy-btn cy-btn-primary">$ NEW_DRAFT()</a></div>` — `$ COPY_LINK()` keeps `onclick={copyLink}`; `$ NEW_DRAFT()` reuses the existing home link (home hosts CREATE_DRAFT()).
    - Close `</div>`.
    - Remove the old Tailwind `class="…"` strings (`flex w-full flex-col items-center gap-8 px-4 py-8 text-text-primary`, the `text-2xl` heading, the `rounded-md border …` buttons, the `text-green-600`/`text-red-600` spans). Drop the old `<h2>Draft complete</h2>` (replaced by `$ DRAFT.RESULT()`).

    After editing, the review branch must contain NO Tailwind utility classes. Validate with the Svelte MCP `svelte-autofixer` until clean.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cy-review" src/routes/draft/\[id\]/+page.svelte` matches AND `grep "cy-review-head" src/routes/draft/\[id\]/+page.svelte` matches
    - `grep "// status: COMPLETE" src/routes/draft/\[id\]/+page.svelte` matches AND `grep '\$ DRAFT.RESULT()' src/routes/draft/\[id\]/+page.svelte` matches AND `grep "cy-review-receipt" src/routes/draft/\[id\]/+page.svelte` matches
    - `grep '\$ COPY_LINK()' src/routes/draft/\[id\]/+page.svelte` matches AND `grep '\$ NEW_DRAFT()' src/routes/draft/\[id\]/+page.svelte` matches
    - `grep "onclick={copyLink}" src/routes/draft/\[id\]/+page.svelte` still matches (copy behavior preserved)
    - `grep "data.actions" src/routes/draft/\[id\]/+page.svelte` still matches AND `grep "snapshot.actions" src/routes/draft/\[id\]/+page.svelte` still matches AND `grep "data.teams" src/routes/draft/\[id\]/+page.svelte` still matches (review data source unchanged)
    - `grep "DraftReview" src/routes/draft/\[id\]/+page.svelte` still matches (component still composed)
    - The review branch contains NO auth gate added by this task (UI-06): `grep -c "redirect=/draft" src/routes/draft/\[id\]/+page.svelte` is unchanged from before this task within the review branch (do not add a sign-in wall to review)
    - `grep -cE "text-2xl|gap-8 px-4 py-8|rounded-md border|text-green-600" src/routes/draft/\[id\]/+page.svelte` returns 0 (review-branch Tailwind gone)
    - `npm run check` passes
  </acceptance_criteria>
  <done>The review branch renders inside `.cy-review` with the `// status: COMPLETE` → `$ DRAFT.RESULT()` head + a real-data receipt, the DraftReview compositions grid, and the `$ COPY_LINK()` (copyLink) / `$ NEW_DRAFT()` (home) actions; the data source + copyLink are unchanged, no auth gate added, review-branch Tailwind gone.</done>
</task>

</tasks>

<verification>
- `npm run check` passes
- Review renders: head (`// status: COMPLETE` → `$ DRAFT.RESULT()` → receipt) → two team compositions (lime/violet, picks + struck ban list) → `$ COPY_LINK()` / `$ NEW_DRAFT()`
- copyLink + data.actions/snapshot.actions data source unchanged; review still renders for guests (no auth gate); no Tailwind in DraftReview or the review branch
- Existing DraftReview.svelte.spec.js + browser specs still green (`npm run test`)
</verification>

<success_criteria>
UI-06 met: Review renders both final compositions, the full ban list, and the draft-order recap in Cyber style with the verbatim copy and remains viewable without auth, while DraftReview's champion resolution + props and the route's review data source + copyLink handler stay unchanged (D-01).
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-screen-reskins/10-07-SUMMARY.md`
</output>
