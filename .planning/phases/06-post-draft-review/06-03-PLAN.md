---
phase: 06-post-draft-review
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified: []
autonomous: false
requirements:
  - POST-01
  - POST-02

must_haves:
  truths:
    - "After a real draft completes, all browser sessions transition to the review UI showing picks and bans"
    - "Opening the draft URL as an unauthenticated visitor shows the review UI (shareable link works)"
    - "The 'Copy link' button copies the URL and shows 'Copied' feedback"
    - "The 'Back to home' link navigates to '/'"
    - "Phases strip shows 'Review' as the active (third) step"
    - "No ChatPanel sidebar visible in the review layout"
  artifacts:
    - path: "src/lib/components/molecules/DraftReview.svelte"
      provides: "Review molecule — verified visually"
    - path: "src/routes/draft/[id]/+page.svelte"
      provides: "Review branch — verified in browser"
  key_links:
    - from: "Browser session (participant)"
      to: "review UI"
      via: "Live stream snapshot.phase === 'review' triggers branch switch"
    - from: "Browser session (unauthenticated visitor)"
      to: "review UI"
      via: "SSR load returns actions+teams without throwing 404"
---

<objective>
Manual verification of the complete post-draft review flow across two browser sessions: one as a participant who watched the draft end, one as an unauthenticated visitor opening the shareable link cold.

Purpose: POST-01 and POST-02 require human confirmation that the review renders correctly in real browser conditions that automated tests cannot cover (live stream transition, actual clipboard, visual layout, Phases strip).

Output: Human approval or a list of issues to fix.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-post-draft-review/06-CONTEXT.md
@.planning/phases/06-post-draft-review/06-01-SUMMARY.md
@.planning/phases/06-post-draft-review/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm tests pass</name>
  <files></files>
  <read_first>
    - src/routes/draft/[id]/+page.svelte (confirm review branch is present)
    - src/lib/components/molecules/DraftReview.svelte (confirm file exists)
  </read_first>
  <action>
Run the full test suite and start the dev server:

1. `npm run test` — must exit 0 with all tests passing
2. `npm run check` — must exit 0 with no errors
3. `npm run dev` — start dev server (note the port, typically http://localhost:5173)

Report the dev server URL to the user.
  </action>
  <verify>
    <automated>npm run test && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0
    - `npm run check` exits 0
    - Dev server starts without errors on http://localhost:5173 (or reported port)
  </acceptance_criteria>
  <done>All tests green, type-check clean, dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — post-draft review flow</name>
  <action>
Pause for human to verify the complete post-draft review flow in a real browser using the dev server started in Task 1. Three scenarios must be confirmed: participant transition (POST-01), shareable link for unauthenticated visitor (POST-02), and CTA behaviour (copy link, back to home).
  </action>
  <verify>
    <automated>MISSING — human visual and interaction verification required; see how-to-verify below</automated>
  </verify>
  <done>Human types "approved" confirming all three scenarios pass.</done>
  <what-built>
Complete post-draft review flow:
- completeDraft now sets phase='review' (no ended_at)
- +page.server.js loads draft_action rows for review phase
- DraftReview.svelte renders two-column pick/ban summary
- +page.svelte has review branch with "Draft complete" heading, CTAs, and DraftReview component
  </what-built>
  <how-to-verify>
**Scenario A — Participant transition (POST-01):**

1. Open http://localhost:5173 in two browser tabs (Tab 1: signed-in user A, Tab 2: signed-in user B or spectator)
2. Create a room in Tab 1, join a team in both tabs (or leave one as spectator)
3. Start a draft with a short script (1–2 bans + 1–2 picks per team) and a short timer (e.g. 5 seconds)
4. Let the draft run to completion — either pick/ban all turns or let the timer auto-advance all turns
5. **Verify in both tabs:** The page transitions to the review UI automatically (no page refresh needed)
6. **Verify:** "Draft complete" heading is visible at the top
7. **Verify:** Phases strip shows "Review" as the active (highlighted) third step
8. **Verify:** Two columns visible — "Team A" and "Team B"
9. **Verify:** Each column shows "BANS" label (if any bans occurred) and "PICKS" label (if any picks occurred) with the correct champion names beneath each
10. **Verify:** No ChatPanel sidebar visible
11. **Verify:** "Back to home" link and "Copy link" button are present below the heading

**Scenario B — Shareable link (POST-02):**

12. Copy the current draft URL from the browser address bar (format: /draft/XXXXXXX)
13. Open a new private/incognito browser window (unauthenticated)
14. Paste the URL and navigate to it
15. **Verify:** The review UI loads — "Draft complete" heading, two-column summary with picks/bans, no auth prompt or redirect to /login

**Scenario C — CTAs:**

16. In any session, click "Copy link"
17. **Verify:** "Copied" text appears briefly in green then disappears after ~2 seconds
18. Click "Back to home"
19. **Verify:** Navigates to the home page (/)

**Edge case (optional):** If you have a draft where all turns timed out (no champion_id), verify the review shows "Draft ended without picks or bans." rather than empty columns.
  </how-to-verify>
  <resume-signal>Type "approved" if all scenarios pass. Describe any issues if failures are found.</resume-signal>
  <acceptance_criteria>
    - Human confirms "Draft complete" heading visible
    - Human confirms two-column Team A / Team B layout with Bans and Picks sections
    - Human confirms unauthenticated shareable link loads review without auth prompt
    - Human confirms "Copied" feedback appears and disappears after clicking Copy link
    - Human confirms "Back to home" navigates to /
  </acceptance_criteria>
</task>

</tasks>

<verification>
Human verification covers:
- POST-01: Summary view visible to all participants after draft completes
- POST-02: Shareable link opens without auth
- D-01: Two-column layout with Bans then Picks
- D-07: Back to home + Copy link CTAs present and functional
- D-09: No ChatPanel in review
- Phases strip activates "Review" step correctly
</verification>

<success_criteria>
- Human approves all three verification scenarios (A, B, C)
- No visual defects reported in the two-column layout
- Shareable link opens without sign-in prompt
- Copy link feedback works
</success_criteria>

<output>
After completion, create `.planning/phases/06-post-draft-review/06-03-SUMMARY.md`
</output>
