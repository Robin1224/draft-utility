---
phase: 11-terminal-modals
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/lib/components/atoms/ScriptTurnRow.svelte
  - src/lib/components/molecules/DraftSettingsPanel.svelte
  - src/lib/components/molecules/DraftSettingsPanelHost.test.svelte
  - src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js
autonomous: true
requirements: [MOD-01]

must_haves:
  truths:
    - "Draft Settings opens as a terminal modal (title ~/draft/config.sh) with a timer stepper clamped 10–120s in steps of 5 (MOD-01)"
    - "The script editor reorders turns via drag AND [↑]/[↓] buttons, adds and removes turns, preserving the { id, team, action } shape (MOD-01, D-11)"
    - "Edits hit a local draft copy; SAVE_CONFIG() commits to page state, any dismissal discards (D-04)"
    - "Dismissing a dirty modal swaps the footer to // discard unsaved config? with [DISCARD]/[KEEP_EDITING]; esc again discards; a clean modal closes silently (D-05, D-06)"
    - "The script row fits without horizontal overflow at 320px viewport width"
  artifacts:
    - path: "src/lib/components/molecules/DraftSettingsPanel.svelte"
      provides: "Full Settings modal composing CyModal: stepper + script editor + draft-copy/dirty state machine + footer swap"
      exports: ["props: open ($bindable), script ($bindable), timerSeconds ($bindable)"]
    - path: "src/lib/components/atoms/ScriptTurnRow.svelte"
      provides: "Cyber script row: ⠿ grip drag, zero-padded index, team/action selects, ↑/↓ move buttons, rm"
      min_lines: 60
    - path: "src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js"
      provides: "Rewritten browser spec: stepper clamp, add/remove/reorder, save/discard, dirty footer, 320px fit"
  key_links:
    - from: "src/lib/components/molecules/DraftSettingsPanel.svelte"
      to: "src/lib/components/atoms/CyModal.svelte"
      via: "CyModal composition with onAttemptClose dirty veto"
      pattern: "onAttemptClose"
    - from: "src/lib/components/molecules/DraftSettingsPanel.svelte"
      to: "page state script/timerSeconds"
      via: "$bindable commit on SAVE_CONFIG() only"
      pattern: "\\$bindable"
    - from: "src/lib/components/atoms/ScriptTurnRow.svelte"
      to: "src/app.css"
      via: "cy-script-row / cy-script-move classes"
      pattern: "cy-script-move"
---

<objective>
Rebuild Draft Settings as the `~/draft/config.sh` terminal modal (MOD-01): ScriptTurnRow gets the Cyber row treatment plus D-11 ↑/↓ buttons; DraftSettingsPanel becomes the full Settings modal composing CyModal with D-04 draft-copy semantics and the D-05/D-06 dirty-dismiss footer swap; the existing spec (which asserts a spinbutton the stepper removes) is rewritten.

Purpose: MOD-01 acceptance — stepper clamped 10–120 step 5, drag-to-reorder + add/remove script editor, over the dimmed lobby.
Output: reworked ScriptTurnRow + DraftSettingsPanel, host fixture, rewritten green spec. All dead Tailwind classes in both files deleted.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-terminal-modals/11-CONTEXT.md
@.planning/phases/11-terminal-modals/11-UI-SPEC.md
@.planning/phases/11-terminal-modals/11-PATTERNS.md
@src/lib/components/atoms/CyModal.svelte
@design_handoff_pickban_cyber/prototype/variants/cyber.jsx
</context>

<interfaces>
<!-- Contract Plan 03 builds against. LobbyHostBar will invoke: -->

```svelte
<DraftSettingsPanel bind:open={settingsOpen} bind:script bind:timerSeconds />
```

DraftSettingsPanel props (all $bindable):
- `open?: boolean` — default false; parent launcher sets true; component/CyModal set false on close
- `script?: ScriptTurn[]` — `{ id: string, team: 'A'|'B', action: 'pick'|'ban' }[]`; ONLY written on SAVE_CONFIG()
- `timerSeconds?: number` — default 30; ONLY written on SAVE_CONFIG()

The component renders its own `<CyModal>` (always mounted, closed by default). It has NO phase awareness and makes NO server calls.

From src/lib/components/atoms/CyModal.svelte (Plan 01):
- props `open ($bindable), title, onAttemptClose (() => boolean, false = veto), children, footer` snippets
- `close` event sets `open = false` — hang all cleanup (reset confirmingDiscard) on `open` flipping false, never on cancel
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rework ScriptTurnRow + DraftSettingsPanel into the Settings modal</name>
  <files>src/lib/components/atoms/ScriptTurnRow.svelte, src/lib/components/molecules/DraftSettingsPanel.svelte</files>
  <read_first>
    - src/lib/components/atoms/ScriptTurnRow.svelte (current drag wiring on the draggable li — lines 13–37 stay logically identical; Tailwind classes and the SVG grip button get deleted)
    - src/lib/components/molecules/DraftSettingsPanel.svelte (current addTurn/removeTurn/updateTurn/handleDragStart/Over/Drop — logic ports verbatim onto the draft copy; all Tailwind markup deleted)
    - src/lib/components/atoms/CyModal.svelte (the Plan 01 wrapper being composed — prop contract in this plan's <interfaces>)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx lines 726–830 (CYScriptEditor row markup + CYSettings modal anatomy/stepper/footer — the verbatim copy source)
    - .planning/phases/11-terminal-modals/11-RESEARCH.md (Pattern 4 — $state.snapshot/structuredClone draft copy + canon dirty check; Pattern 5 — dirty-dismiss state machine; Pattern 6 — ↑/↓ splice)
    - .planning/phases/11-terminal-modals/11-UI-SPEC.md (Copywriting Contract — exact strings; Component Contracts §3/§4)
  </read_first>
  <action>
    **ScriptTurnRow.svelte (D-11):** Replace all Tailwind markup with the Cyber row per cyber.jsx 758–776. Root `<li class="cy-script-row" class:is-drag={dragging || dragOver} draggable="true">` keeping the existing ondragstart/ondragend/ondragover/ondragleave/ondrop wiring and local dragging/dragOver $state byte-for-byte in behavior. Cells in grid order: (1) `<span class="cy-script-grip" aria-hidden="true">⠿</span>` (replaces the SVG grip button; grip is the drag affordance only — the whole li stays draggable), (2) `<span class="cy-script-idx">{String(index + 1).padStart(2, '0')}</span>`, (3) team select `class="{turn.team === 'A' ? 'team-a' : 'team-b'}"` with `aria-label="Team for turn {index + 1}"`, options `TEAM_A` (value A) / `TEAM_B` (value B), onchange → `onUpdate(index, 'team', value)`, (4) action select `class="{turn.action === 'ban' ? 'is-ban' : 'is-pick'}"` with `aria-label="Action for turn {index + 1}"`, options `ban()` (value ban) / `pick()` (value pick), onchange → `onUpdate(index, 'action', value)`, (5) `<button type="button" class="cy-script-move" aria-label="move turn {index + 1} up" disabled={isFirst}>↑</button>` → `onMoveUp(index)`, (6) same with `↓`/`down`/`disabled={isLast}` → `onMoveDown(index)`, (7) `<button type="button" class="cy-script-rm" aria-label="remove turn {index + 1}">rm</button>` → `onRemove(index)`. Extend the JSDoc props type with `onMoveUp: (i: number) => void, onMoveDown: (i: number) => void, isFirst: boolean, isLast: boolean`. Delete the `sr-only` labels (that class no longer exists — Tailwind is gone); aria-labels on the selects replace them.

    **DraftSettingsPanel.svelte (D-04, D-05, D-06):** Becomes the full Settings modal. New props per <interfaces> (open/script/timerSeconds all $bindable — the old live `$bindable` pass-through editing is REMOVED per D-04). Internal state: `draft` ($state, `{ script: [], timerSeconds: 30 }`), plain `base` (non-reactive), `confirmingDiscard` ($state false). On open flipping true ($effect watching `open`): `base = structuredClone($state.snapshot({ script, timerSeconds }))`, `draft.script = structuredClone(base.script)`, `draft.timerSeconds = base.timerSeconds`, `confirmingDiscard = false`. On open flipping false: reset `confirmingDiscard = false` (draft is simply abandoned — RESEARCH Pattern 5: cleanup on close, never on cancel). Dirty ($derived): compare `JSON.stringify(draft.script.map(t => [t.team, t.action]))` vs base's, OR timer differs — id churn is NOT dirty (ids are stripped before startDraft). Veto: `function attemptClose() { if (!dirty || confirmingDiscard) return true; confirmingDiscard = true; return false; }` passed as `onAttemptClose` AND used by the CANCEL button (`if (attemptClose()) open = false;`). All existing mutation handlers (addTurn with `nanoid(8)` + `{ team: 'A', action: 'ban' }`, removeTurn, updateTurn, handleDragStart/Over/Drop splice) port verbatim but now mutate `draft.script`. Add `moveUp(i)`/`moveDown(i)` reusing the exact handleDrop splice body (one data path, three input methods — D-11).

    Markup: `<CyModal bind:open title="~/draft/config.sh" onAttemptClose={attemptClose}>`. Body: `.cy-modal-head` with `<h3>&gt; DRAFT_SETTINGS</h3>` + `<p>// configure turn timer and the pick/ban execution order</p>`; stepper field per cyber.jsx 807–814 — `.cy-field` > `<span class="cy-field-label">turn_timer</span>` + `.cy-stepper` with `−` button (U+2212 glyph, `aria-label="decrease"`, `draft.timerSeconds = Math.max(10, draft.timerSeconds - 5)`), `<div class="cy-stepper-val">{draft.timerSeconds}<span>sec</span></div>`, `+` button (`aria-label="increase"`, `Math.min(120, draft.timerSeconds + 5)`); script field — `.cy-field` with label `pick / ban order — drag ⠿ to reorder`, empty state `<p class="cy-foot">// empty script — add at least one turn</p>` when draft.script is empty, else `<ul class="cy-script" aria-label="Pick/ban turn order">` of ScriptTurnRow (keyed by turn.id, passing isFirst={i === 0}, isLast={i === draft.script.length - 1} and all callbacks), then `<button type="button" class="cy-btn cy-btn-sm cy-script-add">+ ADD_TURN</button>`. Footer snippet with the D-06 swap: normal state = `<div class="cy-grow"></div>` + `CANCEL` (`cy-btn cy-btn-sm`) + `SAVE_CONFIG()` (`cy-btn cy-btn-primary cy-btn-sm`, commits `script = draft.script.map(t => ({ ...t }))`, `timerSeconds = draft.timerSeconds`, then `open = false`); confirmingDiscard state = `// discard unsaved config?` text + `[DISCARD]` (`cy-btn cy-btn-sm cy-btn-danger`, sets `open = false`) + `[KEEP_EDITING]` (`cy-btn cy-btn-sm`, sets `confirmingDiscard = false`). SAVE_CONFIG() stays enabled on an empty script (server validation is frozen/authoritative — UI-SPEC default). No server calls anywhere; no `{@html}`; keep JSDoc typedefs per project convention.

    Run the Svelte MCP `svelte-autofixer` on both files until clean; fallback `npx prettier --write` + `npm run check` (Phase 9 precedent).
  </action>
  <verify>
    <automated>grep -q "cy-script-move" src/lib/components/atoms/ScriptTurnRow.svelte && grep -q "structuredClone" src/lib/components/molecules/DraftSettingsPanel.svelte && grep -q "~/draft/config.sh" src/lib/components/molecules/DraftSettingsPanel.svelte && ! grep -q "spinbutton\|type=\"number\"" src/lib/components/molecules/DraftSettingsPanel.svelte && ! grep -qE "rounded-md|text-text-|bg-bg-" src/lib/components/molecules/DraftSettingsPanel.svelte src/lib/components/atoms/ScriptTurnRow.svelte && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - ScriptTurnRow.svelte contains `⠿`, `String(index + 1).padStart(2, '0')`, options `TEAM_A`/`TEAM_B`/`ban()`/`pick()`, buttons with aria-labels `move turn {index + 1} up`/`down` and `remove turn {index + 1}`, and no Tailwind utility classes (no `rounded-md`, `text-text-`, `bg-bg-`, `sr-only`)
    - DraftSettingsPanel.svelte contains `Math.max(10,` and `Math.min(120,` with step 5, the `−` U+2212 glyph, `structuredClone($state.snapshot(`, `// discard unsaved config?`, `[DISCARD]`, `[KEEP_EDITING]`, `SAVE_CONFIG()`, `+ ADD_TURN`, and `// empty script — add at least one turn`
    - DraftSettingsPanel.svelte contains no `<input` (stepper replaced the number input) and no fetch/RPC import from `$live` (D-04: no new server calls)
    - `script =` / `timerSeconds =` assignments to the bindable props appear ONLY inside the SAVE_CONFIG() handler
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Both components are pure Cyber (zero Tailwind remnants), the Settings modal composes CyModal with draft-copy/dirty semantics, and svelte-check passes. (Note: the browser suite is expected RED until Task 2 rewrites the spec — the old spinbutton assertions are guaranteed-red by design.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite DraftSettingsPanel.svelte.spec.js (stepper/reorder/save-discard/dirty/320px)</name>
  <files>src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js, src/lib/components/molecules/DraftSettingsPanelHost.test.svelte</files>
  <read_first>
    - src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js (current spec — keep the describe structure and the `defaultScript()` factory at lines 11–24; every spinbutton/Remove/`draft-settings-panel`-id assertion is replaced per the PATTERNS replacement table)
    - src/lib/components/molecules/DraftSettingsPanel.svelte (the Task 1 result under test)
    - src/lib/components/atoms/CyModal.svelte.spec.js (Plan 01 — reuse its interaction idioms for dialog open/esc)
    - .planning/phases/11-terminal-modals/11-PATTERNS.md (spec replacement table + fixture note: .test.svelte files are safe from the client-project glob `*.svelte.{test,spec}.{js,ts}`)
  </read_first>
  <behavior>
    - Test 1: open via fixture launcher → `page.getByRole('dialog')` visible, title `~/draft/config.sh` rendered, heading `> DRAFT_SETTINGS` present
    - Test 2: stepper shows `30` + `sec`; `decrease` click → 25; clamp: from 10, `decrease` leaves 10; from 120, `increase` leaves 120 (drive to bounds by repeated clicks or by rendering with timerSeconds 10/120)
    - Test 3: `+ ADD_TURN` on an empty script replaces `// empty script — add at least one turn` with one listitem; new row shows TEAM_A + ban() (shape `{ team:'A', action:'ban' }`)
    - Test 4: `remove turn 1` removes a row (10 → 9 listitems)
    - Test 5: `move turn 2 up` swaps rows 1↔2 (assert via first row's select values); `move turn 1 up` and `move turn {last} down` buttons are disabled
    - Test 6 (D-04 commit): edit stepper to 35 → SAVE_CONFIG() → dialog closes AND fixture's committed readout shows timerSeconds 35; edit then esc-discard → committed readout unchanged (still 30)
    - Test 7 (D-05/D-06): dirty (one stepper click) → Escape → dialog still open, footer shows `// discard unsaved config?` + `[DISCARD]` + `[KEEP_EDITING]`; `[KEEP_EDITING]` → normal footer (SAVE_CONFIG() visible again); Escape → Escape again → dialog closed, nothing committed
    - Test 8 (D-05 clean): open, touch nothing, Escape → closes immediately with no confirm footer
    - Test 9 (320px fit — UI checker gate): `await page.viewport(320, 640)`; open with the 10-turn script; first `.cy-script-row` satisfies `el.scrollWidth <= el.clientWidth + 1` and `el.getBoundingClientRect().right <= window.innerWidth` (7-column grid fits the D-10 takeover width)
  </behavior>
  <action>
    Create `DraftSettingsPanelHost.test.svelte`: local $state `open = false`, `script = defaultScript-like array (pass as prop)`, `timerSeconds = 30`; a launcher `<button type="button" onclick={() => (open = true)}>CONFIG()</button>`; `<DraftSettingsPanel bind:open bind:script bind:timerSeconds />`; and a committed-state readout `<pre data-testid="committed">{JSON.stringify({ script: script.map((t) => [t.team, t.action]), timerSeconds })}</pre>` so specs can assert commit-on-save vs discard without reaching into component internals. Accept initial `script`/`timerSeconds` as props.

    Rewrite `DraftSettingsPanel.svelte.spec.js` implementing the behavior list, keeping the existing `defaultScript()` factory and listitem-count technique. Use role-based locators: `button { name: 'decrease' | 'increase' }`, `button { name: '+ ADD_TURN' }`, `button { name: 'remove turn 1', exact: true }`, `button { name: 'move turn 2 up' }`, `button { name: 'SAVE_CONFIG()' }`, `button { name: '[DISCARD]' }`, `button { name: '[KEEP_EDITING]' }`. Escape via `userEvent.keyboard('{Escape}')`. Direct DOM reads (`document.querySelector('.cy-script-row')`, `.cy-stepper-val` text) are accepted per CyShell spec precedent. Drop the old `document.getElementById('draft-settings-panel')` test entirely.
  </action>
  <verify>
    <automated>npx vitest run --project client src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project client src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js` exits 0 covering all 9 behaviors, including the explicit 320px script-row width check (viewport 320, scrollWidth/right-edge assertions)
    - Spec asserts the exact strings `// discard unsaved config?`, `~/draft/config.sh`, `// empty script — add at least one turn`
    - Spec contains no `getByRole('spinbutton'` occurrences
    - `npm test` exits 0 — full node + client suite green (the frozen 175-test constraint holds; this spec rewrite is the only sanctioned test change this plan)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Rewritten spec green, full suite green, MOD-01 behaviors (stepper clamp, reorder via three input paths' shared data path, save/discard, dirty confirm, 320px fit) all machine-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → frozen RPCs | Settings never call the server; the draft copy commits to page state only, consumed later by the frozen `startDraft(code, { script, timerMs })` (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | script/timer payload (client clamp bypass via devtools) | accept | Client clamp 10–120 and select-constrained team/action are UX only; server-side startDraft validation is existing/frozen and authoritative (RESEARCH §Security V5) |
| T-11-02 | Tampering (XSS) | script rows / stepper rendering | mitigate | All strings are static prototype copy or numeric state via Svelte interpolation; no `{@html}`, no free-text inputs added |
| T-11-01 | Elevation of privilege | Settings modal exposure | mitigate | Rendered only via LobbyHostBar inside `{#if isHost}` (Plan 03 wires it; this component adds no route/mount of its own) |
| T-11-SC | Tampering | package installs | accept | Zero new packages (nanoid already installed and already used by addTurn) |
</threat_model>

<verification>
- `npx vitest run --project client src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js` exits 0
- `npm test` exits 0 (both projects)
- `npm run lint` + `npm run check` exit 0
- 320px script-row width check present and green (behavior Test 9 — explicit per UI checker recommendation)
- grep: no Tailwind utility classes remain in ScriptTurnRow.svelte / DraftSettingsPanel.svelte
</verification>

<success_criteria>
- MOD-01 truths hold in a real chromium: terminal modal, stepper clamped 10–120 step 5, drag + ↑/↓ + add/remove preserving `{ id, team, action }`, D-04 commit/discard, D-05/D-06 dirty footer swap, 320px fit
- The `<DraftSettingsPanel bind:open bind:script bind:timerSeconds />` contract is stable for Plan 03
</success_criteria>

<output>
Create `.planning/phases/11-terminal-modals/11-02-SUMMARY.md` when done.
</output>
