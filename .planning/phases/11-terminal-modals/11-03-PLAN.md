---
phase: 11-terminal-modals
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/lib/components/molecules/LobbyHostBar.svelte
  - src/lib/components/molecules/LobbyHostBar.svelte.spec.js
  - src/routes/draft/[id]/+page.svelte
autonomous: true
requirements: [MOD-02]

must_haves:
  truths:
    - "Host Console opens as a terminal modal (title ~/draft/host_console) with working move-player and kick controls plus the amber captain-gating hint (MOD-02)"
    - "The inline host bar is a slim launcher: CONFIG(), HOST_CONSOLE(), ▶ START_DRAFT() + captain hint; inline move/kick/settings UI is gone from the bar (D-01)"
    - "▶ START_DRAFT() renders in both the slim bar and the Console footer, both disabled from the single startDisabled $derived until both teams have a captain (D-02, ROADMAP SC-4)"
    - "CANCEL_ROOM exists only in the Console modal footer, styled cy-btn-danger, left side (D-03)"
    - "Guests and players never see launchers or modals — everything sits inside {#if isHost} (host-only guard)"
  artifacts:
    - path: "src/lib/components/molecules/LobbyHostBar.svelte"
      provides: "Slim launcher bar + Host Console modal (CyModal composition) + Settings modal invocation"
      contains: "~/draft/host_console"
    - path: "src/lib/components/molecules/LobbyHostBar.svelte.spec.js"
      provides: "Browser spec: launcher set, console move/kick callbacks, dual START gating, hint, host-only guard"
    - path: "src/routes/draft/[id]/+page.svelte"
      provides: "Unchanged frozen wiring + new code prop + [CONFIG()] banner copy"
  key_links:
    - from: "src/lib/components/molecules/LobbyHostBar.svelte"
      to: "src/lib/components/atoms/CyModal.svelte"
      via: "Host Console CyModal composition"
      pattern: "~/draft/host_console"
    - from: "src/lib/components/molecules/LobbyHostBar.svelte"
      to: "src/lib/components/molecules/DraftSettingsPanel.svelte"
      via: "bind:open={settingsOpen} bind:script bind:timerSeconds"
      pattern: "bind:open=\\{settingsOpen\\}"
    - from: "src/routes/draft/[id]/+page.svelte"
      to: "src/lib/components/molecules/LobbyHostBar.svelte"
      via: "frozen callback/bindable invocation + code prop"
      pattern: "onStartDraft=\\{handleStart\\}"
---

<objective>
Split the lobby host surface per D-01..D-03: LobbyHostBar becomes a slim launcher bar, the Host Console becomes a `~/draft/host_console` terminal modal (MOD-02) housing move-player, kick, the amber captain hint, CANCEL_ROOM, and a second gated ▶ START_DRAFT(); the Plan 02 Settings modal is wired to the CONFIG() launcher. Frozen RPC wiring and the page invocation's callbacks/bindables stay byte-identical.

Purpose: MOD-02 acceptance + ROADMAP success criteria 3 and 4.
Output: reworked LobbyHostBar, new LobbyHostBar spec, minimal +page.svelte touch (code prop + banner copy), full suite + lint green — phase gate.
</objective>

<execution_context>
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinvanderheijden/Documents/GitHub/draft-utility/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-terminal-modals/11-CONTEXT.md
@.planning/phases/11-terminal-modals/11-UI-SPEC.md
@.planning/phases/11-terminal-modals/11-PATTERNS.md
@src/lib/components/atoms/CyModal.svelte
@src/lib/components/molecules/DraftSettingsPanel.svelte
@design_handoff_pickban_cyber/prototype/variants/cyber.jsx
</context>

<interfaces>
<!-- Contracts consumed (produced by Plans 01/02 — use directly, no exploration needed): -->

From src/lib/components/atoms/CyModal.svelte:
```
props: open ($bindable), title: string, onAttemptClose?: () => boolean, children: Snippet, footer?: Snippet
```
Host Console passes NO onAttemptClose — it has no dirty state and always dismisses freely (D-05).

From src/lib/components/molecules/DraftSettingsPanel.svelte:
```svelte
<DraftSettingsPanel bind:open={settingsOpen} bind:script bind:timerSeconds />
```

Frozen (must survive byte-identical in behavior — RESEARCH Pitfall 9):
```javascript
// LobbyHostBar derives (reused unchanged):
hasCaptainA/B, startDisabled, showCaptainHint, rosterForKick, movableUsers
// helpers: kickPayload(m) -> { userId } | { guestId } | {}, submitMove() -> onMove(moveUserId, moveTarget)
// callback signatures: onKick(p), onMove(userId, toTeam), onStartDraft(), onCancelRoom()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: LobbyHostBar → slim launcher + Host Console modal; wire Settings modal; minimal page touch</name>
  <files>src/lib/components/molecules/LobbyHostBar.svelte, src/routes/draft/[id]/+page.svelte</files>
  <read_first>
    - src/lib/components/molecules/LobbyHostBar.svelte (current file — typedefs lines 4–31, derives 40–52, kickPayload/submitMove 54–64, and the {#if isHost} guard at line 67 ALL survive; the inline selects/EXEC/CONFIG-toggle/CANCEL/kick-pill markup is what gets replaced)
    - src/routes/draft/[id]/+page.svelte lines 106–143 (frozen handlers — do not touch) and 309–336 (lobby banner copy + LobbyHostBar invocation — the only touchpoints)
    - design_handoff_pickban_cyber/prototype/variants/cyber.jsx lines 833–907 (CYHostControls — verbatim markup target for the console body/footer)
    - src/lib/components/molecules/DraftSettingsPanel.svelte (Plan 02 result — confirm the bind:open contract)
    - .planning/phases/11-terminal-modals/11-UI-SPEC.md (Component Contracts §2/§5, Copywriting Contract — exact strings)
  </read_first>
  <action>
    **LobbyHostBar.svelte:** Keep the script block's typedefs, props, `moveUserId`/`moveTarget` state, all five derives, `kickPayload`, and `submitMove` verbatim. Add prop `code: string` (for the console head), state `consoleOpen = $state(false)`, and keep `settingsOpen`. Everything below stays inside the existing `{#if isHost}` (T-11-01 — launchers AND both modals; guests/players get zero DOM).

    **Slim bar (D-01):** Keep the `.cy-host-panel` shell and `[HOST_CONSOLE]` amber `.cy-host-head` label (UI-SPEC discretion resolved: reuse shipped Phase 10 classes). The `.cy-host-row` contains, gated by `{#if snapshot.phase === 'lobby'}` where the current CONFIG toggle was: `CONFIG()` button (`cy-btn cy-btn-sm`, opens `settingsOpen = true` — drop the old `aria-expanded`/`aria-controls` attributes, it launches a dialog now), `HOST_CONSOLE()` button (`cy-btn cy-btn-sm`, opens `consoleOpen = true`), then `.cy-grow`, then `▶ START_DRAFT()` (`cy-btn cy-btn-primary`, `disabled={startDisabled} aria-disabled={startDisabled}` — the SAME startDisabled derive as the modal footer, D-02). Keep the captain hint `{#if showCaptainHint}<span class="cy-foot">// both teams need a captain</span>{/if}`. REMOVE from the bar: both move selects, EXEC, the CANCEL button (D-03), the inline `<DraftSettingsPanel>` expansion, and the entire `.cy-spec` KICK pill block. COPY_LINK: the existing page-level `$ COPY_LINK()` button below the bar keeps its current behavior untouched (D-01 "existing lobby banner behavior stays").

    **Settings modal wiring:** `<DraftSettingsPanel bind:open={settingsOpen} bind:script bind:timerSeconds />` (always mounted inside the isHost guard; its own CyModal is closed by default).

    **Host Console modal (MOD-02, D-03):** `<CyModal bind:open={consoleOpen} title="~/draft/host_console">` — no onAttemptClose. Body per cyber.jsx 856–901: `.cy-modal-head` with `<h3>&gt; HOST_CONSOLE</h3>` + `<p>// root@{code} — manage rosters before launch</p>`; `.cy-hc-section` containing (a) move_player: `.cy-field` (label `move_player`) wrapping the existing player select (keep `class="cy-input"` + `aria-label="Move player"`, `--move--` placeholder option, movableUsers options bound to moveUserId) inside a `.cy-hc-row` with the target select (keep `aria-label="To team"`, bound to moveTarget, options `→ A` value A / `→ B` value B, inline `style="flex: 0 0 90px"` per prototype) and the `EXEC` button (`cy-btn cy-btn-sm`, onclick submitMove, keep `disabled={!moveUserId || snapshot.phase !== 'lobby'}`); (b) kick: `.cy-field-label` `kick` + `<ul class="cy-kick-list">` of non-host `rosterForKick` members — each `.cy-kick-item` with `<span class="cy-kick-name">{m.displayName}</span>`, `<span class="cy-kick-team">team_{m.side}{m.isCaptain ? ' · cap' : ''}</span>`, `<span class="cy-grow"></span>`, `<button type="button" class="cy-kick-btn" aria-label="kick {m.displayName}" onclick={() => onKick(kickPayload(m))}>kick()</button>`; when no non-host members exist render `<p class="cy-foot">// no removable players</p>` instead of the list; (c) `{#if showCaptainHint}<div class="cy-hc-hint">both teams need a captain before START_DRAFT() unlocks</div>{/if}`. Footer snippet: `CANCEL_ROOM` (`cy-btn cy-btn-sm cy-btn-danger`, onclick onCancelRoom, left) + `<div class="cy-grow"></div>` + `▶ START_DRAFT()` (`cy-btn cy-btn-primary cy-btn-sm`, `disabled={startDisabled} aria-disabled={startDisabled}`, onclick onStartDraft).

    **+page.svelte (two minimal edits, nothing else):** (1) add `{code}` to the LobbyHostBar invocation (callbacks and bind:script/bind:timerSeconds stay byte-identical — frozen wiring untouched); (2) line ~316 banner copy: `configure script via [SETTINGS].` → `configure script via [CONFIG()].` (RESEARCH Open Question 3, aligned to the new launcher name).

    Run the Svelte MCP `svelte-autofixer` on LobbyHostBar.svelte until clean; fallback `npx prettier --write` + `npm run check` (Phase 9 precedent).
  </action>
  <verify>
    <automated>grep -q "~/draft/host_console" src/lib/components/molecules/LobbyHostBar.svelte && grep -q "both teams need a captain before START_DRAFT() unlocks" src/lib/components/molecules/LobbyHostBar.svelte && grep -c "START_DRAFT()" src/lib/components/molecules/LobbyHostBar.svelte | grep -q 2 && ! grep -q "cy-spec-pill" src/lib/components/molecules/LobbyHostBar.svelte && grep -q "bind:open={settingsOpen}" src/lib/components/molecules/LobbyHostBar.svelte && grep -q "\[CONFIG()\]" "src/routes/draft/[id]/+page.svelte" && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - LobbyHostBar.svelte contains exactly two `▶ START_DRAFT()` buttons, both with `disabled={startDisabled}` (D-02); `startDisabled` is declared once
    - LobbyHostBar.svelte contains exactly one `CANCEL_ROOM` string, located inside the Console CyModal footer snippet with class `cy-btn-danger`; the slim bar markup contains no CANCEL button (D-03)
    - The `.cy-spec` kick-pill block, and any `<select>` outside the CyModal composition, are gone from LobbyHostBar.svelte (D-01); `{#if isHost}` still wraps all rendered output
    - LobbyHostBar.svelte contains `// root@{code}`, `move_player`, `kick()`, `// no removable players`, `→ A`, `→ B`, `EXEC`, and `.cy-hc-hint` with the exact hint string
    - src/routes/draft/[id]/+page.svelte: handleStart/handleKick/handleMove/handleCancel bodies are byte-identical to before (git diff shows no hunk touching lines 106–143); the invocation diff adds only `{code}`; banner reads `configure script via [CONFIG()].`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Slim launcher + Console modal + Settings wiring in place behind {#if isHost}; frozen handlers and invocation callbacks untouched; svelte-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New LobbyHostBar.svelte.spec.js + full phase gate</name>
  <files>src/lib/components/molecules/LobbyHostBar.svelte.spec.js</files>
  <read_first>
    - src/lib/components/molecules/LobbyHostBar.svelte (Task 1 result under test)
    - src/lib/components/chrome/CyShell.svelte.spec.js (canonical spec shape: vi.fn callback assertions, toBeDisabled, direct DOM access precedent)
    - src/lib/components/molecules/DraftSettingsPanel.svelte.spec.js (Plan 02 — snapshot-fixture factory style and dialog interaction idioms)
  </read_first>
  <behavior>
    - Test 1 (host-only guard): render with isHost=false → no button named `HOST_CONSOLE()`, no button named `CONFIG()`, no element with role dialog, no `.cy-host-panel` in document
    - Test 2 (slim bar, D-01): isHost=true lobby snapshot → buttons `CONFIG()`, `HOST_CONSOLE()`, `▶ START_DRAFT()` present; no button named `EXEC`, no button whose name starts with `Kick`/`kick(`, no `CANCEL_ROOM` visible before opening the console (inline UI removed)
    - Test 3 (D-02/SC-4 gating): snapshot missing a B captain → bar `▶ START_DRAFT()` disabled; open console → footer `▶ START_DRAFT()` also disabled AND hint `both teams need a captain before START_DRAFT() unlocks` visible; snapshot with both captains → both instances enabled and hint absent; clicking enabled footer START calls onStartDraft once
    - Test 4 (move, MOD-02): open console → select player (aria-label `Move player`), select team B (aria-label `To team`), click `EXEC` → onMove called with (userId, 'B'); EXEC disabled while no player selected
    - Test 5 (kick, MOD-02): console kick list shows non-host members with `team_A · cap` style meta; clicking `kick {name}` calls onKick with `{ userId: ... }` (registered member) — host member absent from the list; all-host roster renders `// no removable players`
    - Test 6 (D-03): `CANCEL_ROOM` button exists in the open console footer with class `cy-btn-danger`; clicking it calls onCancelRoom once
    - Test 7 (console chrome): open console → dialog visible with aria-label `~/draft/host_console`, head shows `root@K7-MIRA` (code prop), Escape closes it freely (no confirm — D-05)
    - Test 8 (launcher wiring): clicking `CONFIG()` opens a dialog titled `~/draft/config.sh` (Plan 02 Settings modal mounts through the bar)
  </behavior>
  <action>
    Create `LobbyHostBar.svelte.spec.js` implementing the behavior list. Build an inline snapshot factory (PATTERNS precedent) returning `{ phase: 'lobby', teams: { A: [...], B: [...] } }` variants: `bothCaptains()`, `missingBCaptain()`, `allHosts()` — members shaped `{ userId, guestId: null, displayName, isCaptain, isHost }` (include one guest member `{ userId: null, guestId: 'g1', ... }` to exercise the guestId kick payload if convenient). Render with `render(LobbyHostBar, { isHost, snapshot, code: 'K7-MIRA', onKick: vi.fn(), onMove: vi.fn(), onStartDraft: vi.fn(), onCancelRoom: vi.fn(), script: [], timerSeconds: 30 })`. Closed dialogs are display:none, so `getByRole('dialog')` resolves only the open one — open the console before asserting its contents. Use `toBeDisabled()` for gating assertions and vi.fn `toHaveBeenCalledWith` for callback signatures (frozen — Pitfall 9).

    Then run the phase gate: full suite + lint + check must be green.
  </action>
  <verify>
    <automated>npx vitest run --project client src/lib/components/molecules/LobbyHostBar.svelte.spec.js && npm test && npm run lint && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project client src/lib/components/molecules/LobbyHostBar.svelte.spec.js` exits 0 covering all 8 behaviors
    - Spec asserts `onMove` called with `('u2', 'B')`-style tuple and `onKick` called with `{ userId: 'u2' }`-style payload (exact frozen signatures)
    - Spec asserts BOTH `▶ START_DRAFT()` instances disabled under a missing captain and enabled with both captains (D-02)
    - `npm test` exits 0 — full node + client suite green (frozen 175 node tests intact; browser suite = CyModal + DraftSettingsPanel + DraftReview + CyShell + CyLogo + new LobbyHostBar specs)
    - `npm run lint` and `npm run check` exit 0
  </acceptance_criteria>
  <done>MOD-02 machine-verified end-to-end; whole-phase suite, lint, and check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → frozen RPCs | Console buttons re-house EXISTING calls (kickMember/movePlayer/startDraft/cancelRoom via page handlers); zero new calls |
| non-host client → host controls | `{#if isHost}` UI guard; authoritative host checks live server-side in the frozen RPCs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation of privilege | Launchers + both modals | mitigate | All rendered output stays inside the existing `{#if isHost}` guard (spec Test 1 asserts non-hosts get zero DOM); server-side authz frozen and authoritative |
| T-11-02 | Tampering (XSS) | `{m.displayName}` in kick list / console head code interpolation | mitigate | Svelte text interpolation only — no `{@html}` (grep-checkable); display names never hit attribute-injection sinks beyond aria-label interpolation which Svelte escapes |
| T-11-04 | Spoofing | Non-host invoking host RPCs via devtools | accept | Pre-existing surface, unchanged; server rejects non-host RPC calls (frozen behavior, RESEARCH §Security) |
| T-11-SC | Tampering | package installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npm test` exits 0 (full node + client suite — phase gate)
- `npm run lint` and `npm run check` exit 0
- grep gates: two `▶ START_DRAFT()` in LobbyHostBar.svelte; one `CANCEL_ROOM` (in modal footer only); no `cy-spec-pill`; `bind:open={settingsOpen}` present; `[CONFIG()]` in +page.svelte
- <human-check>Manual (per 11-VALIDATION.md, non-blocking, for /gsd:verify-work): (1) with OS reduced-motion on, open both modals — no translate/scale entrance animation; (2) drag ⠿ rows with a real mouse in Draft Settings — order changes persist through SAVE_CONFIG(); (3) at a narrow window (≤640px) both modals take over full-screen with pinned titlebar/footer</human-check>
</verification>

<success_criteria>
- ROADMAP SC-3: Host Console opens as a terminal modal with working move/kick + amber captain hint — proven by spec Tests 3–7
- ROADMAP SC-4: `START_DRAFT()` disabled until both teams have a captain, in both the bar and the console footer (D-02)
- D-01/D-03 bar split complete: no inline move/kick/settings/cancel UI outside the modals
- Frozen wiring intact: page handlers byte-identical, callback signatures asserted by spec, full suite green
</success_criteria>

<output>
Create `.planning/phases/11-terminal-modals/11-03-SUMMARY.md` when done.
</output>
